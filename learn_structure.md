● 有办法，但要说得精确：JSON 能做候选生成器，做不了发现的全过程。先说原料——每个 atom 里记录的是：名字、kind、依赖边（type/term 分开）、验证状态、以及指向源码的文件+行号区间。也就是说图结构是现成的,定理的陈述内容不在 JSON
  里，但顺着行号指针可以取回。基于这些原料，我认为有四条真正有原理支撑的路：

  1. 形式概念分析（FCA / 极大双团挖掘）——最接近"从图上直接读出概念"的方法。 把"定理 × 它引用的引理"做成二部图，其中的极大双团（一组定理恰好共享一组引理，且两边都不能再扩大）在 FCA 里就叫"概念"——这不是比喻，是该理论的正式定义。我手工发现的
  gf216_simp 词汇表其实就是一个这样的双团：30 多条 spec 定理 × {UScalar.val_xor, toGF216 展开, natToBinaryPoly_xor, …}。这类结构可以完全自动地枚举和按面积排序。它发现的是"隐性词汇表 / 缺失的中间引理"这个级别的结构。注意一个坑：probe-lean
  的依赖边从不指向定理，所以这张二部图得先用源码 grep 重建（JSON 的行号指针正好够用）。

  2. 反合一（anti-unification）——能合成出新陈述的那一步。 图上的近重复检测（六个 COMPLETE_POINTS_POLYS 子图同构）只给出候选对；把候选对的陈述文本取回来做反合一——找两条定理陈述的最小公共泛化，差异处提成参数——机器就能提议出那条参数化的通用
  spec。这已经是在产生此前不存在的数学语句了，虽然选出的参数化未必是最好的那个。

  3. 跨库图对齐——发现"你的结构是已知抽象的实例"。 把本地某个定义的依赖邻域模式和 mathlib 的图做匹配：如果 GF16 上那堆 add/mul/inv 定理的引用模式和 mathlib 里 Field 实例周边的模式同构，就说明这套 ad-hoc
  开发其实在重复一个已命名的抽象。认出"这是一个已知结构的实例"也是货真价实的发现——数学史上一半的进展是这种。

  4. 理论探索（theory exploration，Isabelle 的 QuickSpec/Hipster 范式）——图定词汇，证明器定真假。 图分析告诉你哪些是枢纽运算（值得围绕它们找规律的词汇表），然后在这个词汇表上枚举小尺寸的候选等式/性质，扔给 Lean
  判真伪。这是目前学术界真正在做的"自动发现引理"，图的作用是把无穷的候选空间砍到枢纽词汇附近。

  根本的限制在最后一步：JSON 是已存在事实的记录，而新结构 = 新定义 + 关于它的真定理，"真"这个属性必须由 Lean 内核裁定，任何图分析都给不了。所以诚实的结论是一个流水线：图（找位置、生成候选、排序）→ 陈述合成（反合一/枚举）→ Lean
  在环验证（裁定真假）。只有 JSON，答案是否；JSON 作为流水线的前端，答案是可以，而且这个流水线里最贵的探索步骤恰恰是图分析最能省钱的地方。

  如果你想试，本项目里性价比最高的实验就是第 1 条：用已重建的定理引用关系跑一次极大双团枚举，看看除了 gf216_simp 之外还有没有第二个没被命名的词汇表。这一步纯属分析、不碰 spec，随时可以做。


● 跑完了。挖掘脚本和完整报告已存到 .verilib/analysis/fca_bicliques.py 和 fca-report-2026-08-02.txt。结果比我预期的更有意思——它没有重新发现 gf216_simp，却发现了三个别的没被命名的结构。

  做法：从源码切出全部手写定理（probe-lean 的边不指向定理，JSON 只提供了骨架），对象 = 344 条证明里有 tactic 词汇的定理，属性 = 证明中 simp/rw/unfold 引用的 698 个引理名；对行集合做交集闭包枚举出全部 FCA 概念（极大双团），过滤掉 tactic
  关键字、局部假设名（hnext 这类）、默认 simp 清理引理（ne_eq、true_and——这些是 simp? 倾倒出来的噪音）和每个文件各自不同的局部定义名（body），按面积排序。

  最重要的发现是个反例：gf216_simp 不是稠密双团。 34 条定理的证明触及桥词汇，但每条只引用其中 1–2 个；五个引理同时出现的证明只剩 1 处（const_sub_spec）。也就是说这个词汇表在共引矩阵里是弥散的"星形"，按面积排序的 FCA
  根本看不见它——我当初把它组装起来靠的是"这些引理都属于同一座桥"的语义判断，不是共现统计。这直接回答了你上一个问题的方法论部分：FCA 找的是"共引概念"，它和"语义连贯的概念"是两回事，两者只部分重叠。

  它真正挖出的三个稠密结构（都是此前没人命名的）：

  1. Aeneas 循环体证明惯用式（面积最大：16–19 条定理 × {bind_tc_ok + unfold body + hnext}）。遍布 Poly/AddAssign、ComputeAt、Deserialize、FromCompletePoints、LagrangeInterpolate 等所有循环 body_spec，开场动作一模一样。它不该做成 simp
  set，而是一个自定义 tactic/宏的候选（类似 step* 之于 progress）——这是"证明模式"级别的概念。
  2. Aeneas 下标桥词汇表（核心 3×6：{getElem!_Nat_eq, getElem!_eq_getElem?_getD, getElem!_pos, length, length_val, ofNatCore_val_eq}，外围 9×2）。集中在 FromPb/IntoPb/EncodeBytesBase/LagrangeInterpolatePrepare
  这些序列化和索引密集的证明里。这是货真价实的第二个 simp set 候选（机器下标 ↔ 列表下标的桥），和 gf216_simp 完全平行。
  3. 线性因子系数展开簇（4–6 条 Math 层定理 × {coeff_C_mul, coeff_X_mul, coeff_sub, sub_mul}），出现在 Identities/Basic、ExpectedTrailing、LinearFactors/Degree、MultXdiff 里。这四条 mathlib 引理总是成组出现，说明每条证明都在原地重新展开"(X - C a)
  * p 的第 n 项系数"——这个 intent 是一条缺失的中间引理的投影，把它写出来四处证明都会缩短。这是三者里最接近"新数学结构"的：不是打包改写规则，而是一条没人陈述过的引理。

  顺带的小信号：{degree, toGF216Poly} × 6 条 Lagrange 相关 spec——Poly.toGF216Poly 的次数上界推理也在被反复手工重做，缺一条 degree API 引理。

  所以对你最初的问题现在有了实验答案：这条路能发现结构，但发现的层级取决于结构在矩阵里的形态——稠密双团（惯用式、下标词汇表、缺失引理）它一抓一个准；弥散的语义词汇表（gf216_simp）它抓不住。三个发现里，第 3 个（缺失的系数引理）最值得做且不涉及 spec
  语句，想做的话我可以直接尝试把它陈述并证明出来。
