---
phase: 01-gates-provenance-and-target-list
plan: 09
type: execute
wave: 8
depends_on: ["01-08"]
files_modified: []
autonomous: false
requirements: [CAT-03, REV-01, DEV-01, DEV-02, DEV-03, DEV-04]
allowed_sorries: 0
pr_boundary: B
pr_boundary_terminal: true
lean_files_touched: 0
src_touched: false
srctranslated_touched: false

must_haves:
  truths:
    - "PR B carries the target list, the review machinery, the two canonicity rows and the D1-D5 decisions"
    - "GSD stopped at the boundary without creating a pull request"
    - "The report states the PR B change set over the full base-to-head range, not a bare working-tree diff"
    - "The report states the phase outcome against all six ROADMAP criteria, including any left unmet"
    - "The report names the 22 targets and the first phase that will prove each"
  artifacts:
    - path: ".planning/phases/01-gates-provenance-and-target-list/01-09-SUMMARY.md"
      provides: "The PR B boundary report and the Phase 1 outcome against all six success criteria"
      contains: "PR B implementation tip:"
  key_links:
    - from: "the boundary report"
      to: ".planning/ROADMAP.md Phase 1 success criteria"
      via: "a per-criterion met/unmet verdict with evidence"
      pattern: "criterion"
---

<objective>
Stop at the PR B boundary, report the phase outcome, and hand the pull request to the user.

PR B is the catalog-content half of Phase 1: the target list, the statement-review machinery,
the two new canonicity rows and the D1-D5 decisions. This plan also closes Phase 1, so its
report doubles as the phase verdict against all six ROADMAP criteria.
Output: a boundary report. **No files are modified and no PR is created.**
</objective>

<phase_type>
Reporting plan; `files_modified` is empty. The only artifact is this plan's SUMMARY.
</phase_type>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@docs/proof-targets.md
@.planning/phases/01-gates-provenance-and-target-list/01-05-SUMMARY.md
@.planning/phases/01-gates-provenance-and-target-list/01-06-SUMMARY.md
@.planning/phases/01-gates-provenance-and-target-list/01-07-SUMMARY.md
@.planning/phases/01-gates-provenance-and-target-list/01-08-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Assemble the PR B boundary report and the Phase 1 verdict, then stop</name>
  <what-was-done>
    **PR B deliverables**, each against the requirement it discharges:
    - `docs/proof-targets.md` - per-row band and C1/C2/C3 verdicts (CAT-03)
    - `docs/rubrics/spqr-statement-review.md`, `.claude/skills/spqr-statement-review/SKILL.md`,
      `docs/spec-review-log.md` (REV-01)
    - STRUCT-02a and STRUCT-02b in the catalog with their statement records and reviews
      persisted; PROP-35's derived `Domain:` correction; PROP-37's `Finding:` annotation with
      its restatement deferred to Phase 7 (REV-01, PROV-01, CAT-01). STRUCT-04 is **not** a
      deliverable - it was retired into STRUCT-03 on 2026-09-15 and the target list records the
      retirement
    - section 11's Decision/Status table and the five restated statements (DEV-01 to DEV-04)

    **Record commands**, with output pasted:
    - `./scripts/check-gates.sh spqr.kdf.hkdf_to_slice_spec` - the final five-gate verdict for
      the whole phase. **Explicit `#print axioms` targets are mandatory**: a bare
      `./scripts/check-gates.sh` skips gate 4 and `01-VALIDATION.md:48` forbids citing a
      partial run as a boundary check. Only an `ALL GATES PASSED` summary line discharges the
      boundary; `PASSED WITH SKIPS` does not
    - `python3 scripts/check-provenance.py` - expect pass, or a named `Unsourced` residue
    - `git status --porcelain -- src SrcTranslated` - expect empty
    - `git diff --stat <PR B base>...HEAD`, where `<PR B base>` is the literal `PR B base:` ref recorded in
      `01-05-SUMMARY.md` - **not** its `PR B head:` branch, which is where these commits live and
      would make the range empty. **Not a bare `git diff --stat`**: the work is committed by now, so a
      bare diff compares the working tree to the index and reports nothing - which would read
      as "no changes" for a PR that rewrites section 11 and adds two rows. Include the tracked
      `.planning/` artifacts and say so
    - `git diff --name-only <PR B base>...HEAD -- src SrcTranslated '*.lean'` - expect empty,
      showing the code freeze held across the committed range, not just the working tree

    **Phase 1 verdict.** Take all six ROADMAP Phase 1 criteria in order and give each a
    met/unmet verdict with the evidence. State any unmet criterion plainly with what remains -
    in particular, if gate 5 is red on user-ruled `Unsourced` rows, criterion 2 is partially
    met and must say so. A boundary report that omits an unmet criterion is the failure this
    plan exists to prevent.

    **Handoff to the proof phases.** List the 22 targets from `docs/proof-targets.md` with the
    phase that will prove each, and state the two facts the next phases most need:
    - Phase 3's PROP-35 needs a `varintBytes`-to-`varintBlockAt` agreement lemma that does not
      exist - the old "composed from the existing component specs" reading was wrong - and it
      proves the **corrected** statement: PROP-35's row already carries its domain, so Phase 3
      quotes it rather than re-deriving it
    - Phase 7 inherits **design work, not just proof work**: PROP-37's state-validity invariant
      is unspecified on purpose. Phase 1 established the row is false and why, with the
      counterexample, but representability alone was shown insufficient in plan review, so the
      invariant must be designed there before the row can be restated. STRUCT-03's corollary
      (the retired STRUCT-04) inherits whatever domain that invariant fixes
    - every proof phase must run `/spqr-statement-review <PROP-ID>` and get ACCEPT **before**
      writing the proof; the log's dates make the ordering checkable afterwards
  </what-was-done>
  <user-action-required>
    **Open PR B yourself.** GSD does not create pull requests in this repository.

    Open it as a **draft** from the `PR B head:` branch recorded in `01-05-SUMMARY.md`,
    targeting the `PR B base:` ref recorded there - the head is where these commits live, the
    base is what the PR is opened against, and they are different refs. Body of one sentence
    plus the deliverable list, following the #537-#541 shape. There is no issue to close.

    If PR A has not merged, PR B is stacked on it: say so in the PR body so a reviewer knows
    the base is not `main`.

    One thing to decide before the proof phases start: **Phase 2 and Phase 3 are independent**
    and the roadmap permits them to run concurrently. Phase 2 is build-heavy (merging branch
    proofs, replacing three liveness axioms); Phase 3 is the encoding layer and needs the
    varint agreement lemma. Running them in parallel shortens the path to the refinement work,
    but doubles the review load on you. Sequential Phase 2 then Phase 3 is the safer default.
  </user-action-required>
  <resume-signal>Tell GSD the PR B number, and whether Phase 2 and Phase 3 run sequentially or in parallel</resume-signal>
  <action>
    Run the record commands, assemble the report and the six-criterion verdict, present it,
    and STOP. Do not run `gh pr create`, `gh pr ready`, `gh pr edit` or `git push`.

    Do not mark a criterion met on the strength of a plan having run. Criterion 4, for
    instance, is met only because plan 01-07 produced persisted review artifacts - cite the
    artifact paths, not the plan number.
  </action>
  <acceptance_criteria>
    - The SUMMARY lists all four PR B deliverables against their requirements
    - It carries the literal output of the five record commands
    - It gives a met/unmet verdict for each of the six ROADMAP Phase 1 criteria, each with
      evidence cited as a file path or command output rather than a plan number
    - Any unmet or partially met criterion is stated plainly with what remains
    - It carries a literal `PR B implementation tip: <sha>` line
    - It lists the 22 targets with the phase that will prove each
    - It states the two handoff facts: the missing varint agreement lemma, and the REV-01
      precondition on every proof phase
    - `git diff --name-only <base>...HEAD -- src SrcTranslated '*.lean'` is empty across the
      committed range
    - No `gh pr` command and no `git push` anywhere in the transcript
  </acceptance_criteria>
  <done>The PR B boundary report is presented with a per-criterion Phase 1 verdict and a handoff to the proof phases, GSD created no pull request, and the user has chosen the Phase 2/Phase 3 ordering.</done>
</task>

</tasks>

<verification>
1. The SUMMARY exists with deliverables, record-command output and a six-criterion verdict
2. `PR B implementation tip:` line present
3. The 22 targets listed with their phase
4. `git diff --name-only <base>...HEAD -- src SrcTranslated '*.lean'` empty
5. No `gh pr` command and no `git push` in the transcript
</verification>

<success_criteria>
- Phase 1 closes with an honest verdict on all six criteria, unmet ones included
- The proof phases start from a written target list, a binding review process and a decided
  set of deviations - so no later theorem has to be restated because its statement was
  ambiguous when it was proved
</success_criteria>
