---
phase: 01-gates-provenance-and-target-list
plan: 05
type: execute
wave: 4
depends_on: ["01-01", "01-02", "01-03", "01-04"]
files_modified: []
autonomous: false
requirements: [INFRA-01, INFRA-03, PROV-01]
allowed_sorries: 0
pr_boundary: A
pr_boundary_terminal: true
lean_files_touched: 0
src_touched: false
srctranslated_touched: false

must_haves:
  truths:
    - "PR A carries the gate documentation, the five-gate runner, the provenance checker and the catalog Source: retrofit"
    - "GSD stopped at the boundary without creating a pull request"
    - "The report states the PR A change set over the full base-to-head range, not a bare working-tree diff"
    - "The report answers the base-branch question and records PR B's head branch and its base ref as two separate values"
    - "Any gate left red is named with its reason, not omitted"
  artifacts:
    - path: ".planning/phases/01-gates-provenance-and-target-list/01-05-SUMMARY.md"
      provides: "The PR A boundary report: deliverables, gate verdicts, the change set, the PR A implementation tip SHA and the answered branch questions"
      contains: "PR A implementation tip:"
    - path: ".planning/phases/01-gates-provenance-and-target-list/01-05-SUMMARY.md"
      provides: "The recorded PR B head and base refs: 01-06's precondition and 01-09's range base"
      contains: "PR B head:"
  key_links:
    - from: "the boundary report"
      to: "the user opening PR A"
      via: "the deliverable list and the measured change set the user pastes into the PR body"
      pattern: "PR A implementation tip:"
---

<objective>
Stop at the PR A boundary, report, and hand the pull request to the user.

Purpose: D-16 and the project's process constraint - GSD does not create pull requests in
this repository. PR A is the infrastructure half of Phase 1: the corrected gate
documentation, the five-gate runner, the provenance checker and the catalog `Source:`
retrofit. PR B (plans 01-06 to 01-09) is the catalog-content half.
Output: a boundary report. **No files are modified and no PR is created.**
</objective>

<phase_type>
Reporting plan; `files_modified` is empty. The only artifact is this plan's SUMMARY.
</phase_type>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-gates-provenance-and-target-list/01-01-SUMMARY.md
@.planning/phases/01-gates-provenance-and-target-list/01-02-SUMMARY.md
@.planning/phases/01-gates-provenance-and-target-list/01-03-SUMMARY.md
@.planning/phases/01-gates-provenance-and-target-list/01-04-SUMMARY.md

`.planning/config.json` sets `branching_strategy: none`, so execution commits to whatever
branch is checked out - currently `la/spec-catalog`.
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Assemble the PR A boundary report and stop</name>
  <what-was-done>
    List the four PR A deliverables with the requirement each discharges:
    - `docs/ISSUE_TEMPLATE.md` as a per-property PR checklist, plus the phantom gate removed
      from the rubric and CLAUDE.md (INFRA-01)
    - `scripts/check-gates.sh`, `scripts/axiom-allowlist.txt`, `scripts/check-provenance.py`,
      the `check-lint.sh` shim and the README section (INFRA-03, PROV-01)
    - `docs/spqr-properties.md` `Source:` retrofit plus `docs/spec-sections.txt` and
      `docs/scka-refs.txt` (PROV-01)
    - the five-gate run, the CI cross-check and the nine negative controls (INFRA-03)

    Run the record commands and paste their output:
    - `./scripts/check-gates.sh spqr.kdf.hkdf_to_slice_spec` - the final verdict for the record
    - `git grep -nE 'check[_]no[_]sorry' -- ':!.planning'` (expect exit 1, no output)
    - `python3 scripts/check-provenance.py` (expect pass, or a named `Unsourced` list)
    - `git status --porcelain -- src SrcTranslated` (expect empty)
    - for the whole PR A change set: `git diff --stat <base>...HEAD` against the base branch
      the user chooses below. **Not a bare `git diff --stat`**: by this point the work is
      committed, so a bare `git diff` compares the working tree to the index and reports
      nothing, which would read as "no changes" for a PR that changes a dozen files. Include
      the tracked `.planning/` artifacts in that range and say so - `.planning/` is committed
      on this project (`commit_docs: true`), so the plan set and the SUMMARYs are real PR
      scope.

    **State every gate's colour, including any that is red.** If gate 5 is red because of
    user-ruled `Unsourced` rows, say so with the row list; a boundary report that omits a red
    gate is the failure this plan exists to prevent.

    Record the **PR A implementation tip** - the SHA of the last task commit - and note
    separately that the tracked `01-05-SUMMARY.md` commit lands after it, so the two SHAs
    differ and the PR A range must include both.
  </what-was-done>
  <user-action-required>
    **Open PR A yourself.** GSD does not create pull requests in this repository.

    Before you do, answer the base-branch question. `origin/main` contains **none** of the
    files this phase edits: the catalog, the template, the rubric, `CLAUDE.md` and all of
    `.planning/` are new on the unmerged `la/spec-catalog` branch. Re-measure the range
    rather than quoting a figure from this plan - it grows with every planning commit - and
    report the number you measured, with the date.

    The substantive finding is what matters: `origin/main` has no `docs/spqr-properties.md`
    and no `.planning/` at all.

    **Re-measure the Lean/toolchain diff; do not quote it from this plan.** An earlier draft
    asserted `git diff --name-only origin/main..HEAD -- '*.lean' lakefile.toml lean-toolchain
    lake-manifest.json` was empty. That is **no longer true** — `origin/main` has moved and on
    2026-09-15 the command returned `Spqr.lean`, `Spqr/Specs/Chain/Chain/EpochIdx.lean`,
    `Spqr/Specs/Chain/Chain/New.lean` and
    `Spqr/Specs/Encoding/Polynomial/PolyEncoder/IntoPb.lean`. Those are **upstream commits this
    branch does not have**, not Phase 1 edits: `git diff --name-only d47083c..HEAD -- src` is
    empty and the code freeze holds. Report what you measure, and never "repair" a Lean file to
    make a stale assertion true.

    Two branch questions ride along, and both are yours to settle - GSD will not choose a
    branch or move work between branches on its own:
    - **Is PR A's work on a branch of its own?** `branching_strategy` is `none`, so execution
      has been committing to `la/spec-catalog` directly. If PR A and PR B are to be two
      reviewable pull requests, PR A's commits need a tip you can point a PR at, and PR B's
      commits (plans 01-06 to 01-09) need to start somewhere not mixed in with them.
    - **Where should PR B branch from?** PR B edits the post-01-03 catalog and uses the
      provenance gate PR A introduces. If PR A has not merged, that means running PR A's
      tooling from PR B's branch. That is workable but it must be a choice you made.

    As of 2026-09-14, `git rev-list --left-right --count origin/la/spec-catalog...HEAD`
    reported `0	9` - the branch is **ahead by nine, zero behind**, not diverged, so a plain
    push suffices and the `--force-with-lease` note in `.planning/STATE.md:76` is stale.
    Re-measure before acting on either.

    Open the PR as a **draft** (project convention), with a body of one sentence plus the
    deliverable list, following the #537-#541 shape. There is no issue to close.
  </user-action-required>
  <resume-signal>Tell GSD the PR A number and the base branch you chose, or "skip" to continue to PR B without opening PR A yet - but in **either** case name **both** the branch PR B's commits are to be made on (its head) and the ref PR B will be opened against (its base), because plan 01-06 will not start without the head and plan 01-09 cannot measure the PR B range without the base</resume-signal>
  <action>
    Run the record commands, assemble the report, present it, and STOP. Do not run
    `gh pr create`, `gh pr ready`, `gh pr edit` or `git push`.

    **"skip" defers the pull request, not the branch decision.** `branching_strategy` is
    `none`, so whatever branch is checked out when plan 01-06 runs is where PR B's work
    lands - and if that is PR A's head, the two PRs are one branch and cannot be reviewed
    separately. So a bare `"skip"` is **not** a complete resume signal: ask again for the PR B
    branch, and do not hand control to 01-06 until the user has named it. Record the answers
    under literal `PR B head:` and `PR B base:` lines - the head is 01-06's precondition, the
    base is 01-09's range base. If the answer is
    "same branch, I'll split it later", record it verbatim as an explicit user ruling so it
    reads as a decision rather than an accident.
  </action>
  <acceptance_criteria>
    - The SUMMARY lists all four deliverables against their requirements
    - It carries the literal output of the five record commands
    - It states all five gate colours, red ones included, with reasons
    - It carries a literal `PR A implementation tip: <sha>` line and notes the later SUMMARY commit
    - It carries a literal `PR B head: <branch>` line — the branch PR B's commits are made on,
      which is plan 01-06's precondition — **and** a literal `PR B base: <ref>` line — the ref
      PR B is opened against, which is plan 01-09's range base. These are two different refs;
      recording one value for both makes `<base>...HEAD` empty and would vacuously pass 01-09's
      change-set and code-freeze checks
    - `git log --oneline -1` shows no merge or PR-creation commit; no `gh pr` command appears
      in the transcript
    - `git status --porcelain -- src SrcTranslated '*.lean'` is empty
  </acceptance_criteria>
  <done>The PR A boundary report is complete and presented, every gate colour is stated, GSD created no pull request, and the user has named both the PR A base and the PR B branch.</done>
</task>

</tasks>

<verification>
1. The SUMMARY exists with the deliverable list, the five gate colours and the record-command output
2. `PR A implementation tip:`, `PR B head:` and `PR B base:` lines are all three present, and no `PR B branch:` line remains
3. No `gh pr` command and no `git push` anywhere in the transcript
4. `git status --porcelain -- src SrcTranslated '*.lean'` is empty
</verification>

<success_criteria>
- The infrastructure half of Phase 1 is reviewable as one pull request, opened by the user
- PR B cannot begin on an unnamed branch
</success_criteria>
