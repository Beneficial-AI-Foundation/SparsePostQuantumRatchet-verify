---
phase: 01-gates-provenance-and-target-list
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/ISSUE_TEMPLATE.md
  - docs/rubrics/spqr-plan-review.md
  - CLAUDE.md
autonomous: true
requirements: [INFRA-01, CAT-01]
allowed_sorries: 0
pr_boundary: A
lean_files_touched: 0
src_touched: false
srctranslated_touched: false

must_haves:
  truths:
    - "The phantom gate token is gone from the template, the rubric and CLAUDE.md, each keeping its warning reworded"
    - "docs/ISSUE_TEMPLATE.md is a per-property PR checklist, not an issue template: no Labels section, no issue vocabulary"
    - "A contributor reading it sees five gate commands they can copy and run"
    - "The checklist names the §12 index, the property status row and the Source citation as same-PR obligations"
    - "The checklist names the ACCEPT statement review as a precondition, not a step"
    - "No operative document cites a gate that does not exist in this repository"
  artifacts:
    - path: "docs/ISSUE_TEMPLATE.md"
      provides: "Per-property PR checklist naming lake build, runLinter Spqr, Audit.lean, #print axioms, check-provenance, the Spqr.lean re-export, the catalog + §12 line, and the REV-01 precondition"
      contains: "scripts/check-gates.sh"
    - path: "docs/rubrics/spqr-plan-review.md"
      provides: "Item 7 warning reworded without naming a token; provenance and statement-review checks added"
      contains: "a gate that does not exist in this repository"
  key_links:
    - from: "docs/ISSUE_TEMPLATE.md"
      to: "scripts/check-gates.sh"
      via: "the one-command line at the end of the checklist"
      pattern: "check-gates\\.sh"
    - from: "docs/ISSUE_TEMPLATE.md"
      to: "docs/spec-review-log.md"
      via: "the REV-01 precondition line naming where the ACCEPT is recorded"
      pattern: "spec-review-log"
---

<objective>
Make the operative documents honest. `docs/ISSUE_TEMPLATE.md` currently tells a contributor
to run `#check_no_sorry`, which does not exist in this repository, and frames the unit of
work as a GitHub issue, which the 2026-09-15 re-scope dropped. Rewrite it as a per-property
**PR** checklist naming the five real gates, and strip the token from the two other
documents that cite it.

Purpose: INFRA-01 and CAT-01. Every later phase's PR is checked against this document; a
checklist naming a nonexistent gate produces false confidence at every PR boundary.
Output: three edited documents, zero Lean, zero `src/`.
</objective>

<phase_type>
Documentation phase; zero `*.lean` files, zero `src/` and `SrcTranslated/` hunks,
`allowed_sorries: 0`. No `lake` invocation is needed and none should be run.
</phase_type>

<token_hygiene>
**PHANTOM-TOKEN** is the token at `docs/ISSUE_TEMPLATE.md:98`, matched by the ERE
`check[_]no[_]sorry`. Do not write it literally into any artifact you create, including the
SUMMARY — the completion check is `git grep -nE 'check[_]no[_]sorry' -- ':!.planning'`
returning nothing, and writing it into a tracked file outside `.planning/` would defeat that.
</token_hygiene>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@docs/ISSUE_TEMPLATE.md
@docs/rubrics/spqr-plan-review.md

The five gates, in the order the checklist should name them:
1. `lake build` — no warning other than `declaration uses 'sorry'`
2. `lake exe runLinter Spqr` — no `error:`
3. `lake env lean scripts/Audit.lean` — no new line in `sorry-manifest.txt`
4. `#print axioms <thm>` — only builtins and `scripts/axiom-allowlist.txt` entries
5. `scripts/check-provenance.py` — the row's `Source:` citation resolves

Gates 1–4 exist today as commands; gate 5 and the one-command runner
`scripts/check-gates.sh` are built by plan 01-02. Reference them by their final paths.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite docs/ISSUE_TEMPLATE.md as a per-property PR checklist</name>
  <files>docs/ISSUE_TEMPLATE.md</files>
  <read_first>
    - `docs/ISSUE_TEMPLATE.md` in full (127 lines): the `{PLACEHOLDER}` set at lines 8–92
      and 127, the Checklist at 94–106, and the Labels HTML comment at 111–125
    - `.planning/PROJECT.md` § Target Selection and § Constraints — the REV-01 and PROV-01
      wording the checklist must reflect
  </read_first>
  <action>
    Retitle to `# SPQR Property — PR Checklist` and rewrite the framing paragraph: this is
    the checklist for the draft PR that proves one catalog property; there are no GitHub
    issues on this project.

    **Delete the Labels section entirely** (lines 111–125). It exists only to drive issue
    labelling and nothing consumes it now. Delete the issue-specific placeholders and any
    "file this issue" vocabulary.

    **Keep** the structural sections that describe a property — `## Property: {PROP-ID}`,
    `**Type:**`, `### Description`, `### Formal Definition`, `### Lean Provability` — and
    keep `## Property: {PROP-ID}` **byte for byte**: it is the property's identity line and
    other tooling may key on it.

    Rewrite the Checklist as two blocks.

    *Preconditions (before any proof work):*
    - the property is a target in `docs/proof-targets.md` with its band recorded
    - its `Source:` citation is in `docs/spqr-properties.md` and resolves
    - an **ACCEPT** statement review for this exact statement is recorded in
      `docs/spec-review-log.md`, dated before the first proof commit (REV-01)
    - a **support lemma** proved along the way needs its own `scope: support` ACCEPT
      (`/spqr-statement-review <ID> --support`), not a target ACCEPT — support lemmas are in
      an excluded band, so the target-only route does not apply to them and they must not be
      counted as targets

    *Gates (before the PR is opened):*
    - `lake build` — no warning other than `declaration uses 'sorry'`
    - `lake exe runLinter Spqr` — no `error:`
    - `lake env lean scripts/Audit.lean` — no new line in `sorry-manifest.txt`
    - `#print axioms <theorem>` — only builtins and `scripts/axiom-allowlist.txt` entries
    - `python3 scripts/check-provenance.py` — the row's citation resolves
    - the module is re-exported from `Spqr.lean` (`Audit.lean` cannot see a module that is
      not reachable from the root; an unexported proof passes gate 3 vacuously)
    - the property's status row, its section text and the **§12 index** are updated in this
      same PR (CAT-01)
    - one command for all five gates: `./scripts/check-gates.sh <theorem-names>`

    Close with a one-line note that the PR is opened by the user as a draft, per the
    #537–#541 pattern.
  </action>
  <verify>
    <automated>! grep -nE 'check[_]no[_]sorry' docs/ISSUE_TEMPLATE.md && grep -q 'check-gates.sh' docs/ISSUE_TEMPLATE.md && grep -q 'spec-review-log' docs/ISSUE_TEMPLATE.md && grep -q 'check-provenance' docs/ISSUE_TEMPLATE.md && grep -q '§12' docs/ISSUE_TEMPLATE.md && grep -q '^## Property: {PROP-ID}$' docs/ISSUE_TEMPLATE.md && ! grep -qi '^### Labels' docs/ISSUE_TEMPLATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'check[_]no[_]sorry' docs/ISSUE_TEMPLATE.md` exits 1 with no output
    - `grep -c 'lake ' docs/ISSUE_TEMPLATE.md` is at least 3 — gates 1, 2 and 3 are named as commands
    - `grep -q 'print axioms' docs/ISSUE_TEMPLATE.md` and `grep -q 'check-provenance' docs/ISSUE_TEMPLATE.md` exit 0
    - `grep -q 'check-gates.sh' docs/ISSUE_TEMPLATE.md` exits 0 — the one-command runner is named
    - `grep -q 'spec-review-log' docs/ISSUE_TEMPLATE.md` exits 0 and the REV-01 line says the ACCEPT predates the proof
    - `grep -q 'proof-targets' docs/ISSUE_TEMPLATE.md` exits 0
    - `grep -q '§12' docs/ISSUE_TEMPLATE.md` exits 0 — CAT-01 is on the checklist
    - `grep -q 'Spqr.lean' docs/ISSUE_TEMPLATE.md` exits 0 — the re-export trap is named
    - `grep -q '^## Property: {PROP-ID}$' docs/ISSUE_TEMPLATE.md` exits 0 — identity line preserved verbatim
    - `grep -ci 'issue' docs/ISSUE_TEMPLATE.md` is at most 1, and any remaining mention says there are none
    - `grep -qi '^### Labels' docs/ISSUE_TEMPLATE.md` exits 1 — the Labels section is gone
  </acceptance_criteria>
  <done>The template is a PR checklist naming five runnable gates, the REV-01 and PROV-01 preconditions and the CAT-01 obligation; the phantom token and the Labels section are gone; `## Property: {PROP-ID}` is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the phantom token from the rubric and CLAUDE.md, and add the two new checks to the rubric</name>
  <files>docs/rubrics/spqr-plan-review.md, CLAUDE.md</files>
  <read_first>
    - `docs/rubrics/spqr-plan-review.md` item 7 (gates) and its surrounding items
    - `CLAUDE.md` § Constraints, the `**Gates**` bullet
  </read_first>
  <action>
    In `docs/rubrics/spqr-plan-review.md` item 7: replace the clause naming the token with
    `a gate that does not exist in this repository`, and extend the item so a reviewer also
    fails a plan that (a) claims a property is proved without a resolving `Source:`
    citation, or (b) schedules proof work on a property with no **ACCEPT** entry in
    `docs/spec-review-log.md` predating it. Add the fifth gate (`check-provenance.py`) to
    whatever gate list the rubric carries.

    Add a short rubric item — or extend the existing scope item — stating that a plan whose
    goal is an **evaluation-band or single-path-band** row is a scope error: those are
    support lemmas, and the reviewer should say so rather than approving them as targets.

    In `CLAUDE.md` the `**Gates**` bullet was already reworded during the re-scope; verify
    it names five gates and no token, and correct it if not. Do not restructure the
    `GSD:*-start/end` marked blocks — edit inside them only.
  </action>
  <verify>
    <automated>! git grep -nE 'check[_]no[_]sorry' -- ':!.planning' && grep -q 'a gate that does not exist in this repository' docs/rubrics/spqr-plan-review.md && grep -q 'spec-review-log' docs/rubrics/spqr-plan-review.md && for m in project stack conventions architecture skills workflow profile; do grep -q "GSD:${m}-start" CLAUDE.md && grep -q "GSD:${m}-end" CLAUDE.md || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -nE 'check[_]no[_]sorry' -- ':!.planning'` exits 1 with **no output** — this is ROADMAP criterion 6 and the reason `.planning/` is excluded is that the re-scope records the token's removal in prose
    - `grep -q 'a gate that does not exist in this repository' docs/rubrics/spqr-plan-review.md` exits 0
    - `grep -q 'check-provenance' docs/rubrics/spqr-plan-review.md` and `grep -q 'spec-review-log' docs/rubrics/spqr-plan-review.md` exit 0
    - The rubric names the band scope error: `grep -qiE 'evaluation-band|single-path' docs/rubrics/spqr-plan-review.md` exits 0
    - All **seven** `GSD:` marker pairs are intact — `project`, `stack`, `conventions`,
      `architecture`, `skills`, `workflow`, `profile` (14 `GSD:` lines in total; the file has
      never had four pairs). Check each pair by name, not by counting lines
    - `git diff --name-only` for this task lists only the two files
  </acceptance_criteria>
  <done>No tracked file outside `.planning/` names the phantom gate; the rubric checks provenance, statement review and band scope; CLAUDE.md's marked blocks are intact.</done>
</task>

</tasks>

<verification>
1. `git grep -nE 'check[_]no[_]sorry' -- ':!.planning'` → exit 1, no output
2. `./scripts/check-gates.sh` is named in the template (the script itself lands in 01-02)
3. all seven `GSD:` marker pairs present by name (`project`, `stack`, `conventions`, `architecture`, `skills`, `workflow`, `profile`; 14 lines, never 8)
4. `git status --porcelain -- src SrcTranslated '*.lean'` → empty
</verification>

<success_criteria>
- ROADMAP criterion 6 is met: no operative document cites a nonexistent gate, and the
  template is a per-property PR checklist naming the five real gates plus the
  catalog-and-index line
- The template states REV-01 and PROV-01 as preconditions, so a later phase cannot read
  them as optional steps
</success_criteria>
