---
phase: 01-gates-issues-and-deviation-decisions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/ISSUE_TEMPLATE.md
  - docs/rubrics/spqr-plan-review.md
  - CLAUDE.md
autonomous: true
requirements: [INFRA-01, CAT-01]
allowed_sorries: 0
pr_boundary: A
lean_files_touched: 0
src_touched: false
srctranslated_touched: false

must_haves:
  truths:
    - "D-17: the phantom gate token is removed from the template, the rubric and CLAUDE.md, each keeping its warning reworded"
    - "A contributor reading docs/ISSUE_TEMPLATE.md sees four gate commands they can copy and run"
    - "The template checklist names the §12 index and the property status row as same-PR obligations (CAT-01)"
    - "No operative document (template, rubric, CLAUDE.md) cites a gate that does not exist in this repository"
  artifacts:
    - path: "docs/ISSUE_TEMPLATE.md"
      provides: "Checklist naming lake build, lake exe runLinter Spqr, lake env lean scripts/Audit.lean, #print axioms, the Spqr.lean re-export, and the catalog + §12 update line"
      contains: "scripts/Audit.lean"
    - path: "docs/rubrics/spqr-plan-review.md"
      provides: "Rubric item 7 warning reworded without naming a token"
      contains: "a gate that does not exist in this repository"
    - path: "CLAUDE.md"
      provides: "Gates constraint bullet reworded without naming a token"
      contains: "scripts/check-gates.sh"
  key_links:
    - from: "docs/ISSUE_TEMPLATE.md"
      to: "scripts/check-gates.sh"
      via: "the one-command line at the end of the checklist"
      pattern: "check-gates\\.sh"
    - from: "docs/ISSUE_TEMPLATE.md"
      to: "docs/spqr-properties.md §12"
      via: "the CAT-01 checklist line"
      pattern: "§12"
---

<objective>
Correct `docs/ISSUE_TEMPLATE.md` so its checklist names the four gates this repository
actually runs plus the CAT-01 catalog-and-index obligation, and remove the non-existent
gate token from the two other operative documents that cite it.

Purpose: every later phase files its issues from this template. A template naming a gate
that does not exist means every property PR in phases 2–8 carries a checkbox nobody can
tick. INFRA-01 and CAT-01 are both discharged structurally here.
Output: an edited template, rubric and `CLAUDE.md`; no scripts, no Lean, no `src/`.
</objective>

<phase_type>
**This is a documentation-and-tooling phase. It changes zero `*.lean` files.**
`src/` and `SrcTranslated/` are untouched (code freeze, CLAUDE.md Constraints; rubric
item 8). `allowed_sorries: 0` — no `sorry`, `axiom`, `opaque` or `native_decide` is
introduced anywhere in this phase. Both review gates (`/spqr-plan-review 1`, `spqr-eval`)
are tuned for Lean theorem diffs; for this plan they exercise their "Gates" (item 7) and
"Roadmap and catalog coherence" (item 9) lenses only.
</phase_type>

<token_hygiene>
**PHANTOM-TOKEN** is the non-existent gate token currently sitting between backticks in
the third checklist item of `docs/ISSUE_TEMPLATE.md:98`. Read it from that file.

- It is matched by the ERE `check[_]no[_]sorry`, which is what every verification command
  below uses. That pattern matches the token without any artifact containing it literally.
- **Never write the token literally into any file this phase creates or edits** — plans,
  summaries, eval files, commit messages, issue bodies. Research pitfall P-7: criterion 1's
  grep would then find the artifact that was supposed to remove it.
- The pre-execution Codex review runs against the **pre-edit** rubric, which still names
  the token at `docs/rubrics/spqr-plan-review.md:110`. That is expected, not a finding:
  this plan is the edit.
</token_hygiene>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-gates-issues-and-deviation-decisions/01-CONTEXT.md
@.planning/phases/01-gates-issues-and-deviation-decisions/01-RESEARCH.md
@.planning/phases/01-gates-issues-and-deviation-decisions/01-VALIDATION.md
@CLAUDE.md
@docs/rubrics/spqr-plan-review.md

Locked decisions implemented here: **D-17** (remove the token from the template, the
rubric and `CLAUDE.md`, keeping the warning reworded), **D-15** (this is PR A work),
**CAT-01** (the template gains the catalog-and-index line). User ruling **Q-1**: the
`.planning/` occurrences of the token are deliberately left in place; criterion 1 is
rescoped in plan 01-02, not here.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the ISSUE_TEMPLATE checklist with the four real gates plus the CAT-01 line</name>
  <files>docs/ISSUE_TEMPLATE.md</files>
  <read_first>
    - `docs/ISSUE_TEMPLATE.md` in full (127 lines) — the checklist block is lines 94–99; the
      `<!-- Add additional checklist items -->` comment at 101–107 stays
    - `.planning/phases/01-gates-issues-and-deviation-decisions/01-RESEARCH.md` lines 750–773
      (**Recommended replacement** — the exact checklist block to use)
    - `.planning/phases/01-gates-issues-and-deviation-decisions/01-RESEARCH.md` lines 378–396
      (the four gates exactly as `.github/workflows/lean.yml` runs them, with failure conventions)
    - `.github/workflows/lean.yml` — the authoritative gate definitions; read, do not edit (D-08a)
    - `scripts/Audit.lean` header comment — the "module not re-exported from `Spqr.lean` is
      invisible to the audit" warning that motivates the re-export checklist line
    - `docs/spqr-properties.md` lines 449–491 (§12 Index) — the table the CAT-01 line points at
  </read_first>
  <action>
    Replace `docs/ISSUE_TEMPLATE.md` lines 96–99 (the four current checklist bullets) with
    eight bullets, in this order, using the exact wording of RESEARCH.md lines 754–771:
    (1) `Lean theorem statement written`; (2) `Proof completed`; (3) `` `lake build` `` clean —
    no warning other than `declaration uses 'sorry'`; (4) `` `lake exe runLinter Spqr` `` clean —
    no `error:` line; (5) `` `lake env lean scripts/Audit.lean` `` — `sorry-manifest.txt` gains
    no `Spqr.Specs.*` line; (6) `` `#print axioms <theorem>` `` — only `propext`,
    `Classical.choice`, `Quot.sound` and the documented stubs of `docs/spqr-properties.md` §1;
    (7) Module re-exported from `Spqr.lean` (otherwise `scripts/Audit.lean` cannot see it);
    (8) `docs/spqr-properties.md` updated in the same PR: the property's **status row**, its
    **section text**, and the **§12 index** entry (CAT-01).

    Bullets 3–6 must each begin with `` - [ ] `lake `` or `` - [ ] `#print axioms `` so the
    `grep -c '^- \[ \] `lake'` assertion in `01-VALIDATION.md` counts three.

    After the bullet list add one line: `All four gates in one command:
    ./scripts/check-gates.sh <theorem-name>` (backticked). That script is created by plan
    01-03; the forward reference is intentional and both land in PR A.

    Do not touch the `{PLACEHOLDER}` fields (lines 8–92), the `<!-- Add additional checklist
    items -->` comment block, or the Labels section (lines 111–127) — plan 01-04 reads both
    as the source of truth for generated issue bodies. Do not rename the file: `PC-8` in
    RESEARCH.md records that five documents reference `docs/ISSUE_TEMPLATE.md` by name.
  </action>
  <verify>
    <automated>test "$(grep -c '^- \[ \] `lake' docs/ISSUE_TEMPLATE.md)" -ge 3 && grep -q 'print axioms' docs/ISSUE_TEMPLATE.md && grep -q '§12' docs/ISSUE_TEMPLATE.md && grep -q 'Spqr.lean' docs/ISSUE_TEMPLATE.md && grep -q 'check-gates.sh' docs/ISSUE_TEMPLATE.md && ! grep -nE 'check[_]no[_]sorry' docs/ISSUE_TEMPLATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^- \[ \] `lake' docs/ISSUE_TEMPLATE.md` returns a number `>= 3`
    - `grep -q 'print axioms' docs/ISSUE_TEMPLATE.md` exits 0
    - `grep -q 'scripts/Audit.lean' docs/ISSUE_TEMPLATE.md` exits 0
    - `grep -q '§12' docs/ISSUE_TEMPLATE.md` exits 0 (CAT-01)
    - `grep -q 'Spqr.lean' docs/ISSUE_TEMPLATE.md` exits 0 (the `Audit.lean` blind-spot line)
    - `grep -q 'check-gates.sh' docs/ISSUE_TEMPLATE.md` exits 0
    - `grep -nE 'check[_]no[_]sorry' docs/ISSUE_TEMPLATE.md` exits 1 with no output
    - `grep -cE '\{[A-Z_]+\}' docs/ISSUE_TEMPLATE.md` returns the same count as before the
      edit (record both numbers in the SUMMARY) — the placeholder set is unchanged
    - `git diff --name-only` for this task lists only `docs/ISSUE_TEMPLATE.md`
  </acceptance_criteria>
  <done>The template checklist names four runnable gate commands, the `Spqr.lean` re-export, and the catalog status row plus §12 index; the phantom token is gone from the file; the placeholder set is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Reword the token out of the rubric and CLAUDE.md, keeping both warnings</name>
  <files>docs/rubrics/spqr-plan-review.md, CLAUDE.md</files>
  <read_first>
    - `docs/rubrics/spqr-plan-review.md` lines 105–112 (attack-surface item 7, "Gates")
    - `CLAUDE.md` lines 1–28 — note the `<!-- GSD:project-start source:PROJECT.md -->` /
      `<!-- GSD:project-end -->` markers; the Gates bullet at line 24 is inside that
      generated block
    - `.planning/PROJECT.md` lines 60–70 — the identical Gates bullet at line 67, which the
      user's Q-1 ruling deliberately leaves unchanged
    - `.planning/phases/01-gates-issues-and-deviation-decisions/01-RESEARCH.md` lines 826–854
      (the nine-file PHANTOM-TOKEN inventory and the D-17 action column)
  </read_first>
  <action>
    Two edits, both preserving the warning and dropping the token.

    `docs/rubrics/spqr-plan-review.md` line 110–111: replace the sentence that names the
    token with `A plan citing a gate that does not exist in this repository has a broken
    gate.` Leave the four gate descriptions in lines 106–110 exactly as they are — they
    already match the real gates. Add, after that sentence, one clause naming the local
    runner: `The repeatable local runner for all four is ./scripts/check-gates.sh.`

    `CLAUDE.md` line 24 (the `**Gates**:` bullet inside the GSD project block): replace the
    leading clause that names the token and `docs/ISSUE_TEMPLATE.md` with `**Gates**: the
    real checks are` followed by the existing four-gate list, and replace the trailing
    sentence `The template should be corrected in the first phase` with `docs/ISSUE_TEMPLATE.md
    lists them as its checklist; ./scripts/check-gates.sh runs all four locally.`

    **Known residual to record, do not fix:** `CLAUDE.md` lines 1–28 are generated from
    `.planning/PROJECT.md` (the `source:PROJECT.md` marker). `.planning/PROJECT.md:67` still
    carries the original sentence because the user's Q-1 ruling left it alone, so a future
    regeneration of the project block would re-introduce the token into `CLAUDE.md`. Write
    this residual into the plan SUMMARY as a named risk with the two file:line references.
    Do not edit `.planning/PROJECT.md` — `.planning/` is out of criterion 1's scope by the
    same ruling, and editing it was explicitly declined.
  </action>
  <verify>
    <automated>git grep -nE 'check[_]no[_]sorry' -- ':!.planning'; test $? -eq 1 && grep -q 'does not exist in this repository' docs/rubrics/spqr-plan-review.md && grep -q 'check-gates.sh' CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -nE 'check[_]no[_]sorry' -- ':!.planning'` exits 1 and prints nothing
      (this is ROADMAP criterion 1 as rescoped by Q-1)
    - `git grep -lnE 'check[_]no[_]sorry'` lists only paths under `.planning/`
    - `grep -q 'does not exist in this repository' docs/rubrics/spqr-plan-review.md` exits 0
    - `grep -c 'lake build' docs/rubrics/spqr-plan-review.md` is unchanged from before the
      edit — the rubric still names all four gates
    - `grep -q 'check-gates.sh' CLAUDE.md` exits 0
    - `grep -c '^<!-- GSD:' CLAUDE.md` returns 14 — no GSD marker was disturbed
    - `git diff --name-only` for this task lists exactly `CLAUDE.md` and
      `docs/rubrics/spqr-plan-review.md`
    - The SUMMARY contains a "Known residual" entry naming `.planning/PROJECT.md:67` and
      `CLAUDE.md:24` and the regeneration risk
  </acceptance_criteria>
  <done>The scoped repo grep is clean, both documents keep a warning about citing non-existent gates, and the PROJECT.md regeneration residual is recorded rather than silently fixed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository documents → future executors | The template and rubric are read as instructions by every later phase's executor and by the Codex reviewer. A wrong gate command here propagates to phases 2–8. |
| generated block → source of truth | `CLAUDE.md` lines 1–28 are machine-generated from `.planning/PROJECT.md`; an edit to the generated copy can be silently reverted. |

No network, no credentials, no shell interpolation, no package install in this plan.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Spoofing | `docs/ISSUE_TEMPLATE.md` checklist | mitigate | A checklist item that cannot be run is a gate that always reports green. Every bullet 3–6 is a literal command copied from `.github/workflows/lean.yml` (RESEARCH.md 378–396), and the acceptance criteria grep for each by name. |
| T-1-08 | Tampering | `CLAUDE.md` generated project block | accept | The edit can be reverted by a future `PROJECT.md` regeneration. Disposition is *accept with disclosure*: the residual is written into the SUMMARY with file:line, per the user's Q-1 ruling not to edit `.planning/PROJECT.md`. |
| T-1-09 | Repudiation | phase artifacts re-introducing the token | mitigate | All artifacts use `PHANTOM-TOKEN` / the ERE `check[_]no[_]sorry`; the scoped `git grep` in task 2 is the check (research P-7). |
| T-1-SC | Tampering | npm/pip/cargo installs | n/a | This plan installs no packages. RESEARCH.md § Package Legitimacy Audit records "not applicable — this phase installs no external packages". No legitimacy checkpoint is required. |
</threat_model>

<verification>
Run from the repository root after both tasks:

1. `git grep -nE 'check[_]no[_]sorry' -- ':!.planning'` → exit 1, no output
2. `grep -c '^- \[ \] `lake' docs/ISSUE_TEMPLATE.md` → `>= 3`
3. `grep -q '§12' docs/ISSUE_TEMPLATE.md && grep -q 'Spqr.lean' docs/ISSUE_TEMPLATE.md` → exit 0
4. `git status --porcelain -- src SrcTranslated` → empty (code freeze, rubric item 8)
5. `git diff --name-only origin/main..HEAD -- '*.lean'` → empty (no Lean file touched)
</verification>

<success_criteria>
- `docs/ISSUE_TEMPLATE.md` checklist names `lake build`, `lake exe runLinter Spqr`,
  `lake env lean scripts/Audit.lean`, `#print axioms`, the `Spqr.lean` re-export, and the
  catalog status row plus §12 index (INFRA-01 + CAT-01)
- `git grep -nE 'check[_]no[_]sorry' -- ':!.planning'` returns nothing (ROADMAP criterion 1
  as rescoped by Q-1; the rescoping edit itself is plan 01-02's task 1)
- The rubric and `CLAUDE.md` still warn about non-existent gates, without naming one
- Zero hunks under `src/`, `SrcTranslated/` or any `*.lean` file
</success_criteria>

<output>
Create `.planning/phases/01-gates-issues-and-deviation-decisions/01-01-SUMMARY.md` when done.
Record: the before/after placeholder count from task 1, the `git grep -l` output showing
only `.planning/` paths remain, and the `CLAUDE.md` / `.planning/PROJECT.md:67` regeneration
residual.
</output>
