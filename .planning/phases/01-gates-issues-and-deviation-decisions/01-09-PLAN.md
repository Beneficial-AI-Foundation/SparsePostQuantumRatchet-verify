---
phase: 01-gates-issues-and-deviation-decisions
plan: 09
type: execute
wave: 5
depends_on: ["01-08"]
files_modified:
  - docs/signal-deviation-questions.md
autonomous: false
requirements: [DEV-01, DEV-02, DEV-03, DEV-04]
allowed_sorries: 0
pr_boundary: B
pr_boundary_terminal: true
lean_files_touched: 0
src_touched: false
srctranslated_touched: false

must_haves:
  truths:
    - "A sendable note exists with one question per deviation, each carrying the spec text, the code lines, the impact and the two candidate resolutions"
    - "Each question states what the project is doing absent a ruling, so silence is not mistaken for agreement"
    - "Each DEV requirement has a filed, correctly labelled issue"
    - "GSD stopped at the PR B boundary without creating a pull request"
  artifacts:
    - path: "docs/signal-deviation-questions.md"
      provides: "Five ready-to-send questions covering D1 to D5"
      contains: "Absent a ruling"
    - path: ".planning/phases/01-gates-issues-and-deviation-decisions/01-09-SUMMARY.md"
      provides: "The PR B boundary report with the DEV issue numbers and Closes lines"
      contains: "Closes #"
  key_links:
    - from: "docs/signal-deviation-questions.md"
      to: "docs/spqr-properties.md section 11"
      via: "each question naming its D-row and the provisional decision recorded there"
      pattern: "Provisional"
    - from: "scripts/create-property-issues.sh --execute DEV-01 DEV-02 DEV-03 DEV-04"
      to: "the PR B issue numbers"
      via: "the FILED lines the script prints"
      pattern: "FILED "
---

<objective>
Draft `docs/signal-deviation-questions.md`, file the four DEV issues, and close the PR B
boundary by stopping and reporting.

Purpose: D-11 — the note is the outgoing ask that makes every `Provisional` status in
section 11 honest. Without it, "pending a ruling from Signal" describes a ruling nobody
asked for. D-09 and D-16 then file PR B's issues with the script PR A introduced.
Output: one new Markdown file, four GitHub issues, and a boundary report.
</objective>

<pr_boundary_contract>
**This plan ends the PR B work and the phase. It must not run `gh pr create`, `gh pr
ready`, or any other pull-request command, and must not push a branch.** GSD stops and
reports; the user creates the PR. See `CLAUDE.md` Constraints (Process bullet) and
RESEARCH.md PC-6. A `gh pr` invocation is a BLOCKER for the plan reviewer (rubric item 8)
and `spqr-eval`.
</pr_boundary_contract>

<phase_type>
Documentation-and-tooling phase; zero `*.lean` files, zero `src/` and `SrcTranslated/`
hunks, `allowed_sorries: 0`. `src/` line references in the note are citations, never edits.
</phase_type>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-gates-issues-and-deviation-decisions/01-CONTEXT.md
@.planning/phases/01-gates-issues-and-deviation-decisions/01-RESEARCH.md
@.planning/phases/01-gates-issues-and-deviation-decisions/01-VALIDATION.md
@docs/spqr-properties.md
@CLAUDE.md

Locked decisions closed here: **D-11** (GSD drafts the note; the user sends it through
their own channel; answers are pasted back into section 11), **D-10** (each question ends
by stating the provisional position), **D-09** and **D-16** (PR B's issues are filed at the
PR B boundary with the script PR A introduced; running it from the PR A branch is
acceptable if PR A has not merged), **D-15** (PR B's scope).

Filename `docs/signal-deviation-questions.md` is kebab-case per the project convention
(PC-8).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft docs/signal-deviation-questions.md</name>
  <files>docs/signal-deviation-questions.md</files>
  <read_first>
    - `docs/spqr-properties.md` section 11 as left by plan 01-08 — both tables; the five
      Where / Spec / Code / Impact cells are the raw material, and the Decision cells are
      what each question's closing line must state
    - RESEARCH.md lines 968–979 — the content contract for this file: spec section per
      deviation (2.2 for D1, 2.5 for D2/D3/D4, 2.4 for D5), the `src/` lines from the Where
      column, the Impact sentence, and the two candidate resolutions already named in
      section 11's closing paragraph
    - RESEARCH.md lines 856–913 — the five section 11 rows quoted verbatim, including the
      `src/` line references (`authenticator.rs:43-54`, `states.rs:182`,
      `states.rs:464-467`, `states.rs:484-492`)
    - `docs/spqr-properties.md` PROP-43 as left by plan 01-08 — the D5 caller contract the
      D5 question must reference
    - `01-VALIDATION.md` Manual-Only Verifications — the row "The Signal note reads as
      sendable prose", verified by the user in task 3
    - `src/authenticator.rs` lines 43–54 and `src/v1/states.rs` lines 182, 464–467, 484–492
      — read-only, to confirm every line reference before quoting it
  </read_first>
  <action>
    Create `docs/signal-deviation-questions.md`. Open with a two-or-three sentence preamble
    naming the artefact under review (the SPQR Rust crate pinned at `d47083c`), the spec
    (R. Schmidt, *The ML-KEM Braid Protocol* Rev. 1), and the purpose: five points where the
    implementation and the specification differ, on each of which the project needs to know
    which side is authoritative. State that the differences are catalogued in
    `docs/spqr-properties.md` section 11 and that the formal proofs are currently written
    against the code.

    Then five sections, one per deviation, headed `## D1` … `## D5` with a short title, each
    containing exactly five parts in this order:
    1. **Where** — the `src/` path and line range from section 11's Where column.
    2. **What the spec says** — the spec's behaviour with its section number: 2.2 for D1,
       2.5 for D2, D3 and D4, 2.4 for D5.
    3. **What the code does** — the Code cell's behaviour, quoted from section 11.
    4. **Why it matters** — the Impact cell, expanded to a sentence or two of plain prose.
    5. **The two candidate resolutions** — exactly the pair section 11's closing paragraph
       already names: for D1, a code fix or a spec erratum; for D2, the spec mandates
       strictness or the code adopts the no-op; for D3 and D4, the spec documents the extra
       message handling or the code drops it; for D5, the caller's contract is written down
       (and point at the contract now written under PROP-43 in section 7 as the proposed
       text).

    Close every section with the identical sentence: `Absent a ruling, we are proceeding
    with: the code as implemented is authoritative (recorded as Provisional in
    docs/spqr-properties.md section 11).` This is the sentence that stops silence from being
    read as agreement.

    Finish the document with a short "How to reply" paragraph: a per-deviation choice of
    resolution, or a note that the current behaviour stands. State that answers are pasted
    back into section 11's Decision and Status columns and that a ruling disagreeing with
    the code opens a restatement issue for the affected property rather than a silent edit
    (D-10).

    The note must be sendable as-is: no GSD vocabulary, no phase numbers, no plan IDs, no
    references to `.planning/`. Re-read every `src/` line reference before writing it and
    record any mismatch in the SUMMARY rather than adjusting the claim.
  </action>
  <verify>
    <automated>bash -c "test -f docs/signal-deviation-questions.md && test \$(grep -c '^## D[1-5]' docs/signal-deviation-questions.md) -eq 5 && test \$(grep -c 'Absent a ruling' docs/signal-deviation-questions.md) -eq 5"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## D[1-5]' docs/signal-deviation-questions.md` returns `5`
    - `grep -c 'Absent a ruling' docs/signal-deviation-questions.md` returns `5`
    - Each of the five sections contains the words `spec`, `code` and `resolution` (script
      the per-section check with an awk range and paste it in the SUMMARY)
    - `grep -c 'authenticator.rs\|states.rs' docs/signal-deviation-questions.md` returns a
      number `>= 4` — every deviation with a code site cites it
    - `grep -qE '§?2\.2' docs/signal-deviation-questions.md` and the equivalent for `2.4`
      and `2.5` all exit 0
    - `grep -q 'PROP-43' docs/signal-deviation-questions.md` exits 0 — D5 points at the
      written caller contract
    - `grep -qiE 'phase [0-9]|\.planning|PLAN\.md|GSD' docs/signal-deviation-questions.md`
      exits 1 — the note contains no internal process vocabulary
    - `grep -nE 'check[_]no[_]sorry' docs/signal-deviation-questions.md` exits 1
    - Every `src/` line reference in the note was re-read in the tree and matches; any
      mismatch is recorded in the SUMMARY
    - `git status --porcelain -- src SrcTranslated` is empty
  </acceptance_criteria>
  <done>The note exists with five self-contained questions, each giving the location, the spec's behaviour with its section number, the code's behaviour, the impact, the two candidate resolutions, and the identical "absent a ruling" statement; it reads as external correspondence with no internal process vocabulary.</done>
</task>

<task type="auto">
  <name>Task 2: File the four DEV issues with the PR A script</name>
  <files>(no repository file — GitHub issues only)</files>
  <read_first>
    - `scripts/property-issues.tsv` — the `DEV-01` through `DEV-04` rows seeded by plan
      01-04, including their `description_anchor` values pointing at PROP-42, PROP-30,
      PROP-47 and PROP-43
    - `scripts/create-property-issues.sh` — the `--execute` behaviour and the exact-title
      duplicate detection
    - `01-07-SUMMARY.md` — the five PR A issue numbers and the base-branch answer the user
      gave, which decides whether the script is being run from a merged `main` or from the
      PR A branch (D-16 permits either)
    - RESEARCH.md lines 1069–1080 and 1093–1100 — pitfalls P-5 and P-8
    - RESEARCH.md lines 1193–1210 — the requirement that filed numbers be written into the
      SUMMARY, since nothing in git records them
  </read_first>
  <action>
    Confirm first that the DEV rows now render the **post-01-08** catalog text: the
    `description_anchor` for each pulls the section from `docs/spqr-properties.md`, which
    plan 01-08 has just rewritten, so each body should already contain the provisional
    decision and its D-row citation. Run the script in dry-run for the four IDs and check
    this before filing.

    Then run `./scripts/create-property-issues.sh --execute DEV-01 DEV-02 DEV-03 DEV-04`
    and capture the four `FILED` lines. Run the identical command again and confirm four
    skip lines against the same numbers, with the open-issue count up by exactly four
    across both runs combined (P-5).

    Record the four ID-to-number pairs. D-16 notes that running the script from the PR A
    branch is acceptable when PR A has not merged; state which branch it was run from.

    Do not close, retitle or relabel any existing issue, including the five PR A issues and
    #44 (D-02). Do not run any `gh pr` command.
  </action>
  <verify>
    <automated>bash -c "./scripts/create-property-issues.sh --execute DEV-01 DEV-02 DEV-03 DEV-04 | grep -c '^SKIP' | grep -qx 4"</automated>
  </verify>
  <acceptance_criteria>
    - The first `--execute` run printed four lines beginning `FILED ` and the SUMMARY lists
      all four ID-to-number pairs
    - The second run printed four lines beginning `SKIP ` and zero beginning `FILED `
    - `gh issue list --state open --limit 500 --json number --jq length` grew by exactly `4`
      from the count recorded at the start of the task
    - For each filed number N, `gh issue view N --json labels --jq '[.labels[].name]'`
      returns exactly the two labels from that TSV row
    - For each filed number N, `gh issue view N --json body --jq .body` contains the string
      `Provisional` — the body carries the post-01-08 catalog text, not the pre-decision text
    - The branch the script was run from is recorded in the SUMMARY (D-16)
    - No `gh pr` command appears in the transcript
  </acceptance_criteria>
  <done>DEV-01 through DEV-04 each have one correctly labelled issue whose body carries the newly recorded provisional decision, a repeat run files nothing, and the numbers are recorded.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: STOP at the PR B boundary and report — GSD does not open the PR</name>
  <files>(no repository file — boundary report in the SUMMARY)</files>
  <read_first>
    - `01-08-SUMMARY.md` and this plan's task 1 and task 2 output — the PR B deliverables
    - `01-07-SUMMARY.md` — the PR A boundary report, including the base-branch answer
    - `01-VALIDATION.md` Manual-Only Verifications — the row "The Signal note reads as
      sendable prose"
    - RESEARCH.md lines 1081–1092 — pitfall P-6, why a catalog-decision PR ahead of the
      theorems is correct rather than a PC-5 violation
    - `.planning/ROADMAP.md` Phase 1 success criteria 4 and 5 — the two this PR discharges
  </read_first>
  <what-was-done>
    Report, with evidence, every PR B deliverable:
    - section 11 gained a `| ID | Decision | Status |` table with five rows, all
      `Provisional (2026-09-14)`, beneath an untouched five-column table (plan 01-08) —
      DEV-01..04, ROADMAP criterion 4
    - the section 11 closing paragraph now defines the Status values and points at the
      Signal note (plan 01-08)
    - PROP-42, PROP-30, PROP-47 and PROP-50 each state the provisional behaviour and cite
      their D-row (plan 01-08) — ROADMAP criteria 4 and 5
    - PROP-43 carries the written D5 caller contract with `src/` citations, including the
      dropped-authenticator asymmetry (plan 01-08) — DEV-04, ROADMAP criterion 5
    - the section 12 D1–D5 row changed in the same PR, demonstrating CAT-01 on itself
    - `docs/signal-deviation-questions.md` exists with five sendable questions (task 1)
    - four DEV issues filed (task 2)

    Give the user a ready-to-paste `Closes #N` block for DEV-01, DEV-02, DEV-03 and DEV-04.

    State the sequencing note explicitly, because a reviewer will otherwise raise it: this
    PR changes catalog wording for theorems that do not exist yet, which is correct because
    DEV-01..04 are decisions rather than theorem statements and recording them early is the
    phase's purpose (research P-6). Put the same sentence in the PR body.

    State the section 11 layout reading as a deliberate interpretation of D-12 (research
    Q-4, option b): the existing five-column table is untouched and a second narrow table
    was added beneath it, because seven columns of prose is unreadable and D-12's actual
    prohibition is per-deviation subsections, of which none were created.

    Paste for the record: the section 11 awk parse check, the per-property D-row grep loop,
    the PROP-43 citation greps, `git diff --stat` for the PR B change set,
    `git status --porcelain -- src SrcTranslated` (expect empty), and
    `git grep -nE 'check[_]no[_]sorry' -- ':!.planning'` (expect exit 1).
  </what-was-done>
  <user-action-required>
    Two things, in this order.

    **1. Read `docs/signal-deviation-questions.md` end to end** and confirm each of the five
    questions could be sent to Signal unedited. This is the manual-only verification
    recorded in `01-VALIDATION.md`; communication quality is not machine-checkable. Send the
    note through your own channel when you are satisfied — GSD does not send it (D-11).

    **2. Open PR B yourself.** GSD does not create pull requests in this repository. Open it
    as a draft, body of one sentence plus the `Closes #N` block, following the #537–#541
    shape. Per D-16 and the base-branch answer you gave at the PR A boundary, PR B stacks on
    PR A's branch unless you decided otherwise; if PR A has since merged, base PR B on
    whatever PR A merged into.
  </user-action-required>
  <resume-signal>Tell GSD the PR B number, or "skip" to close the phase without it</resume-signal>
  <action>
    Run the record commands named below, assemble the boundary report including the P-6
    sequencing note and the Q-4 layout reading in PR-body form, present it together with the
    request that the user read `docs/signal-deviation-questions.md` end to end, and STOP. Do
    not run `gh pr create`, `gh pr ready`, `gh pr edit` or `git push`.
  </action>
  <done>The PR B boundary report has been delivered with the DEV issue numbers, the Closes block, the P-6 and Q-4 notes and the five-criterion evidence table; the user has judged the Signal note; no pull request was created.</done>
  <acceptance_criteria>
    - No `gh pr create`, `gh pr ready`, `gh pr edit` or `git push` was run in this plan
      (state this explicitly in the SUMMARY)
    - The SUMMARY contains a deliverable-to-requirement table covering DEV-01, DEV-02,
      DEV-03 and DEV-04, each with the plan number and the verification command that proved it
    - The SUMMARY contains the four ID-to-number pairs and a ready-to-paste `Closes #N` block
    - The SUMMARY contains the P-6 sequencing note and the Q-4 layout reading, both in the
      form intended for the PR body
    - The user confirmed the Signal note is sendable, or listed the edits wanted; the
      response is recorded verbatim
    - `git status --porcelain -- src SrcTranslated` is empty
    - `git grep -nE 'check[_]no[_]sorry' -- ':!.planning'` exits 1
    - All five ROADMAP Phase 1 success criteria are addressed in the SUMMARY, each with the
      command or artifact that satisfies it and a pointer to the plan that produced it
    - The user's answer (PR number or "skip") is recorded verbatim
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| outgoing note → an external organisation | `docs/signal-deviation-questions.md` is written to be sent to Signal. A wrong `src/` citation or a misstated spec claim is a correctness problem in correspondence the project does not control once sent. |
| local script with a `repo`-scoped token → live org repository | Four more `gh issue create` calls; the same token could also open a PR. |
| catalog decisions → external reader | Silence from Signal must not read as endorsement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-07 | Spoofing | silence read as a Signal ruling | mitigate | Every one of the five sections closes with the identical "Absent a ruling, we are proceeding with: the code as implemented is authoritative (recorded as Provisional …)" sentence; asserted to appear exactly five times. The note itself is the evidence the ask was made. |
| T-1-22 | Elevation of privilege | the token also permits `gh pr create` | mitigate | The PR boundary contract makes any `gh pr` invocation a BLOCKER; task 3's acceptance criteria require an explicit statement that none ran; the terminal task is a `checkpoint:human-action` handing both the note and the PR to the user. |
| T-1-24 | Tampering | a wrong `src/` citation leaving the project | mitigate | Task 1 requires re-reading every referenced line in the tree before quoting it, and recording a mismatch rather than adjusting the claim. |
| T-1-16 | Spoofing | duplicate DEV issues | mitigate | Exact-title detection; the repeat run must print four skips and zero files; the open-issue delta is asserted to be exactly four (P-5). |
| T-1-18 | Tampering | modifying existing issues | mitigate | Only `gh issue create` runs; D-02 forbids editing, closing or relabelling anything, including the five PR A issues and #44. |
| T-1-28 | Information disclosure | internal process vocabulary leaking into external correspondence | mitigate | The note is asserted free of `phase N`, `.planning`, `PLAN.md` and `GSD`; the user reads it end to end before sending. |
| T-1-SC | Tampering | npm/pip/cargo installs | n/a | No packages installed. RESEARCH.md Package Legitimacy Audit: not applicable to this phase. |
</threat_model>

<verification>
1. `grep -c '^## D[1-5]' docs/signal-deviation-questions.md` → `5`
2. `grep -c 'Absent a ruling' docs/signal-deviation-questions.md` → `5`
3. Re-running `--execute DEV-01 DEV-02 DEV-03 DEV-04` prints four skips and zero files
4. `gh issue list --state open --limit 500 --json number --jq length` grew by exactly 4
5. `git status --porcelain -- src SrcTranslated` → empty
6. `git grep -nE 'check[_]no[_]sorry' -- ':!.planning'` → exit 1
7. No `gh pr` command and no `git push` anywhere in the transcript
</verification>

<success_criteria>
- `docs/signal-deviation-questions.md` carries five sendable questions, each with the spec
  text, the code lines, the impact, the two candidate resolutions and the provisional
  position (D-11, DEV-01..04)
- DEV-01 through DEV-04 each have a filed, correctly labelled issue whose body contains the
  newly recorded provisional decision
- GSD stopped at the PR B boundary, created no pull request, and reported the issue
  numbers, the `Closes #N` block, the P-6 sequencing note and the Q-4 layout reading (PC-6)
- All five ROADMAP Phase 1 success criteria are addressed with evidence in the report
- Zero hunks under `src/`, `SrcTranslated/` or any `*.lean` file
</success_criteria>

<output>
Create `.planning/phases/01-gates-issues-and-deviation-decisions/01-09-SUMMARY.md` when done.
It is the **PR B boundary report** and the phase's closing record. It must contain: the
deliverable-to-requirement table, the four ID-to-number pairs, the `Closes #N` block, the
P-6 and Q-4 notes in PR-body form, the user's verdict on the Signal note, a five-row table
mapping each ROADMAP Phase 1 success criterion to its evidence, and an explicit statement
that no pull request was created.
</output>
