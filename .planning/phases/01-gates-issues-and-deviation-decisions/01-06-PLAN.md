---
phase: 01-gates-issues-and-deviation-decisions
plan: 06
type: execute
wave: 2
depends_on: ["01-03"]
files_modified:
  - scripts/check-gates.sh
  - scripts/axiom-allowlist.txt
  - scripts/README.md
autonomous: true
requirements: [INFRA-03]
allowed_sorries: 0
pr_boundary: A
lean_files_touched: 0
src_touched: false
srctranslated_touched: false

must_haves:
  truths:
    - "D-07: the origin/main baseline worktree is built once and its manifest cached"
    - "D-08a: the gate check is local only; .github/workflows/lean.yml is not modified, and criterion 2 is met by comparing the script's verdict on main with CI's"
    - "The gate script has been run end to end on a built tree and reported a verdict"
    - "The gate-4 matcher was written against the real #print axioms output of this Lean version, not an assumption"
    - "Each of the four gates has been observed failing when deliberately broken"
    - "The script's verdict on origin/main agrees with CI's latest main run, gate by gate"
    - "A reader knows what a cold baseline run costs before starting one"
  artifacts:
    - path: "scripts/check-gates.sh"
      provides: "Gate-4 matcher corrected against observed output"
      contains: "axiom-allowlist.txt"
    - path: "scripts/README.md"
      provides: "Measured cold-run cost and the observed #print axioms output shapes"
      contains: "Measured cold-run cost:"
  key_links:
    - from: "scripts/check-gates.sh gate 4"
      to: "the real Lean 4.31.0 #print axioms output"
      via: "patterns verified by an elaboration probe, not by assumption"
      pattern: "depends on axioms"
    - from: "scripts/check-gates.sh verdict on origin/main"
      to: "the latest lean.yml run on main"
      via: "per-gate comparison recorded in the SUMMARY"
      pattern: "gh run list --workflow lean.yml"
---

<objective>
Run the gate script for the first time: build the tree, settle the two unverified
assumptions about `#print axioms` output empirically, build the `origin/main` baseline
worktree, execute all four gates, cross-check the verdict against CI, and prove each gate
can fail.

Purpose: INFRA-03 and ROADMAP criterion 2. Plan 01-03 wrote a script nobody has run. A gate
that has never failed is not known to work, and the two `#print axioms` output patterns in
it are currently training knowledge rather than observed fact (RESEARCH.md assumptions
A1 and A2 — the tree was unbuilt during research).
Output: a corrected gate-4 matcher, a filled-in cost line in `scripts/README.md`, and a
negative-control matrix with observed output pasted into the SUMMARY.
</objective>

<runtime_warning>
**This plan is build-bound, not context-bound.** Research B-5 (verified): `.lake/build` does
not exist — the project is currently **unbuilt** — and mathlib is a transitive dependency via
aeneas, so both the working tree and the baseline worktree need `lake exe cache get` before
`lake build` or mathlib compiles from source. Expect a GB-scale olean download and tens of
minutes of compilation on a warm cache, hours without. Budget for it; do not treat any
`lake` invocation below as instantaneous. Disk is not a constraint (415G free at research time).
</runtime_warning>

<phase_type>
Documentation-and-tooling phase; the **committed** diff touches zero `*.lean` files and
zero `src/` / `SrcTranslated/` paths, `allowed_sorries: 0`. Task 3 deliberately introduces
broken Lean declarations, but only inside a throwaway `git worktree` that is removed before
the plan completes — nothing from it may reach the working tree or the index.
</phase_type>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-gates-issues-and-deviation-decisions/01-CONTEXT.md
@.planning/phases/01-gates-issues-and-deviation-decisions/01-RESEARCH.md
@.planning/phases/01-gates-issues-and-deviation-decisions/01-VALIDATION.md
@scripts/check-gates.sh
@scripts/axiom-allowlist.txt
@.github/workflows/lean.yml

Locked decisions exercised here: **D-07** (SHA-keyed worktree baseline), **D-08**
(`#print axioms` targets as arguments, scratch file, allowlist grep, `Audit.lean`
unchanged), **D-08a** (local only; criterion 2 is met by running the script on `main` and
comparing with CI's result, not by editing `lean.yml`).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the working tree and settle the #print axioms output format empirically</name>
  <files>scripts/check-gates.sh, scripts/axiom-allowlist.txt</files>
  <read_first>
    - `.planning/phases/01-gates-issues-and-deviation-decisions/01-RESEARCH.md` lines
      517–598 — the gate-4 recipe, the allowlist and the three naming traps
    - `.planning/phases/01-gates-issues-and-deviation-decisions/01-RESEARCH.md` lines
      1408–1424 — the Assumptions Log, specifically A1 (output shapes and the unknown-name
      behaviour) and A2 (`opaque` invisibility), both flagged LOW confidence and explicitly
      requiring this probe
    - `.planning/phases/01-gates-issues-and-deviation-decisions/01-RESEARCH.md` lines
      1038–1046 — pitfall P-1, the gate that passes because the name was misspelled
    - `scripts/check-gates.sh` gate-4 block as left by plan 01-03, including the `#` comment
      marking the two patterns as "verified in plan 01-06"
    - `Spqr/Specs/Kdf/HkdfToSlice.lean` — `spqr.kdf.hkdf_to_slice_spec` (the `axiom`) beside
      `spqr.kdf.hkdf_to_slice` (the `opaque`), the probe's two subjects
    - `lean-toolchain` — `leanprover/lean4:v4.31.0`, the version whose output format matters
  </read_first>
  <action>
    Run `lake exe cache get` then `lake build --no-ansi` in the working tree and record the
    wall-clock duration of each.

    Then run the probe by hand, before touching the script. Write a scratch file under a
    `mktemp -d` directory containing `import Spqr` followed by four `#print axioms` lines,
    in this order: (1) `spqr.kdf.hkdf_to_slice_spec` — the single hand-written axiom, whose
    report should list itself; (2) `spqr.kdf.hkdf_to_slice` — the `opaque`, which tests A2;
    (3) a theorem from `Spqr/Specs/` that the current `sorry-manifest.txt` marks as
    sorry-tainted, which tests the `sorryAx` path; (4) a deliberately misspelled name, which
    tests P-1. Elaborate with `lake env lean <scratch>` and capture stdout and stderr.

    **Paste the literal output of all four probes into the SUMMARY verbatim.** Then compare
    it against the two shapes plan 01-03 assumed — `'<name>' depends on axioms: [...]` and
    `'<name>' does not depend on any axioms` — and against the assumed unknown-name text
    `unknown identifier` / `unknown constant`.

    If the observed strings differ in any way, correct the gate-4 matcher in
    `scripts/check-gates.sh` to the observed strings and replace the `#` comment marking
    them provisional with one citing this probe and the Lean version. If they match, still
    replace the comment with the confirmation. Either way the comment must no longer say
    "verified in plan 01-06" in the future tense.

    If probe (2) shows that `opaque` declarations **do** appear in `#print axioms` output —
    contradicting A2 — add `spqr.kdf.hkdf_to_slice` and `Spqr.Crypto.Hkdf.HMAC_SHA256` to
    `scripts/axiom-allowlist.txt`, adjust its expected count, and say so in the SUMMARY. Do
    not widen the allowlist for any other reason (PC-4: the allowlist documents the existing
    trusted base, it must not silently grow it).

    Then run `./scripts/check-gates.sh --no-delta spqr.kdf.hkdf_to_slice_spec` and record the
    per-gate verdicts. `--no-delta` is deliberate here: the baseline does not exist yet and
    building it is task 2.
  </action>
  <verify>
    <automated>./scripts/check-gates.sh --no-delta spqr.kdf.hkdf_to_slice_spec; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `lake build --no-ansi` completes and `find .lake/build -name '*.olean' | wc -l` returns
      a number greater than `0`
    - The verbatim output of all four `#print axioms` probes is in the SUMMARY
    - `grep -q 'depends on axioms' scripts/check-gates.sh` matches the **observed** string,
      and the SUMMARY states explicitly whether A1 held or was corrected
    - The SUMMARY states explicitly whether A2 held (`opaque` invisible) or was corrected,
      and `grep -vc '^\s*#\|^\s*$' scripts/axiom-allowlist.txt` returns `20` if A2 held, or
      `22` with a written justification if it did not
    - `grep -q 'verified in plan 01-06' scripts/check-gates.sh` exits 1 — the provisional
      marker was replaced
    - The misspelled-name probe produced an error string, and that exact string appears in
      the gate-4 matcher (`grep -qF '<observed string>' scripts/check-gates.sh`)
    - `./scripts/check-gates.sh --no-delta spqr.kdf.hkdf_to_slice_spec` prints a verdict line
      for gates 1, 2, 3a and 4 and `SKIPPED` for 3b; the exit code and all five lines are in
      the SUMMARY
    - `git status --porcelain -- SrcTranslated src '*.lean'` is empty
  </acceptance_criteria>
  <done>The tree builds, the four probe outputs are recorded verbatim, the gate-4 matcher uses observed strings rather than assumed ones, A1 and A2 are settled in writing, and the script has been run end to end with a recorded per-gate verdict.</done>
</task>

<task type="auto">
  <name>Task 2: Build the origin/main baseline, run the full gate set, and cross-check the verdict against CI</name>
  <files>scripts/check-gates.sh, scripts/README.md</files>
  <read_first>
    - `scripts/check-gates.sh` gate 3b block as left by task 1
    - `.planning/phases/01-gates-issues-and-deviation-decisions/01-RESEARCH.md` lines
      465–516 — the worktree recipe, the SHA-keyed cache, the `--skip-baseline` /
      `--refresh-baseline` rationale, and the cold-cost warning
    - `.planning/phases/01-gates-issues-and-deviation-decisions/01-RESEARCH.md` lines
      1053–1068 — pitfalls P-3 (toolchain drift) and P-4 (stale manifest as baseline)
    - `.planning/phases/01-gates-issues-and-deviation-decisions/01-RESEARCH.md` lines
      234–256 (blocker B-4) — `git diff --stat origin/main..HEAD` is 14 files, 2229
      insertions, **0 deletions**, and zero `*.lean` / `lakefile.toml` / `lean-toolchain`
      differences, so this branch's own sorry-delta must come out empty
    - `.github/workflows/lean.yml` lines 40–90 — the `use-mathlib-cache: true` step and the
      `sorry-manifest-main-${sha}` cache key, i.e. CI's twin of the local baseline
    - `git worktree list` — an unrelated worktree already exists at
      `/home/lacra/git_repos/baif/spqr-pr340`; do not disturb it
  </read_first>
  <action>
    Run `./scripts/check-gates.sh --refresh-baseline spqr.kdf.hkdf_to_slice_spec` and let it
    build the `origin/main` worktree. Time the cold run and record: the `lake exe cache get`
    duration, the `lake build` duration, the resulting worktree `.lake` size, and the total.

    Confirm the mechanics rather than just the verdict:
    - the cache file created is named `sorry-manifest-<sha>.txt` where `<sha>` equals
      `git rev-parse origin/main`;
    - the head manifest used is the one regenerated in **this** run, not the pre-existing
      repo-root file dated 2026-08-07 (check the mtime before and after, P-4);
    - the toolchain-drift comparison ran and reported no drift, since `lean-toolchain` and
      the aeneas `rev` are identical between the trees today (P-3);
    - `.sorry-delta-comment.md` does not exist in the repository root after the run;
    - gate 3b reports an **empty** delta, which is the correct answer for this branch (B-4:
      zero Lean differences from `origin/main`) and is *not* evidence the delta path works —
      that is task 3's job.

    Then satisfy ROADMAP criterion 2. Run the script inside the baseline worktree itself so
    it is judging `origin/main`, and compare its four verdicts against CI's latest `main`
    run: `gh run list --workflow lean.yml --branch main --limit 1 --json
    conclusion,databaseId,headSha` for the overall conclusion, and `gh run view <id> --log`
    (or `gh run view <id> --json jobs`) for the per-step results of "Build and check for
    non-sorry warnings", "Lint hand-written code" and "Axiom audit and sorry manifest".
    Build a four-row comparison table — gate, local verdict, CI verdict, agree yes/no — and
    put it in the SUMMARY. Gate 4 has no CI counterpart; record it as `n/a (no CI step)`
    rather than inventing agreement.

    If any gate disagrees, **stop and report** with both outputs rather than adjusting the
    script to make them match — a local script tuned until it agrees is exactly the
    false-green failure this phase exists to prevent.

    Finally, fill in `scripts/README.md`: replace `Measured cold-run cost: TBD (recorded in
    plan 01-06)` with the measured numbers (cache-get time, build time, total, worktree
    size, and the date and machine class), and add the observed `#print axioms` output
    shapes from task 1 as a short note under the gate-4 row.
  </action>
  <verify>
    <automated>test -f ".gate-cache/sorry-manifest-$(git rev-parse origin/main).txt" && test ! -f .sorry-delta-comment.md && grep -q 'Measured cold-run cost:' scripts/README.md && ! grep -q 'Measured cold-run cost: TBD' scripts/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f ".gate-cache/sorry-manifest-$(git rev-parse origin/main).txt"` exits 0
    - `test ! -f .sorry-delta-comment.md` exits 0 — the stray file was cleaned up
    - The SUMMARY records the repo-root `sorry-manifest.txt` mtime before and after, showing
      it was regenerated in this run and never used as the base (P-4)
    - The SUMMARY contains the toolchain-drift comparison output showing `lean-toolchain`
      and the aeneas `rev` identical between the two trees (P-3)
    - Gate 3b reported an empty delta, and the SUMMARY states explicitly that this is
      expected from B-4 and is **not** evidence the delta path works
    - The SUMMARY contains the four-row local-vs-CI comparison table with the CI run id and
      head SHA, and every row reads `agree: yes` or `n/a (no CI step)`
    - `grep -q 'Measured cold-run cost:' scripts/README.md` exits 0 and
      `grep -q 'Measured cold-run cost: TBD' scripts/README.md` exits 1
    - `git worktree list` still contains `/home/lacra/git_repos/baif/spqr-pr340`
    - `git status --porcelain -- src SrcTranslated '*.lean'` is empty
  </acceptance_criteria>
  <done>The SHA-keyed baseline exists, the full four-gate run completed with the delta computed by `sorry-diff.py`, the local verdict on `origin/main` agrees gate-by-gate with CI's latest `main` run, and the measured cold-run cost is in `scripts/README.md`.</done>
</task>

<task type="auto">
  <name>Task 3: Run the negative-control matrix and prove every gate can fail</name>
  <files>(no repository file — scratch worktree only)</files>
  <read_first>
    - `.planning/phases/01-gates-issues-and-deviation-decisions/01-VALIDATION.md`
      § Negative Controls — the six-row matrix this task executes, including the
      silent-failure control
    - `.planning/phases/01-gates-issues-and-deviation-decisions/01-RESEARCH.md` lines
      1275–1300 — the same matrix with its rationale
    - `scripts/Audit.lean` header comment — "Any `Spqr.*` module not imported by the root
      `Spqr` module will not be scanned", the documented blind spot row 4 demonstrates
    - `Spqr.lean` — the root module whose imports decide what `Audit.lean` can see
    - `scripts/check-gates.sh` as left by tasks 1 and 2
  </read_first>
  <action>
    Create a throwaway worktree for the breakages — `git worktree add --detach
    <path-outside-the-repo> HEAD` — so no broken declaration can reach the working tree or
    the index. Run every control inside it, one at a time, reverting between runs.

    Execute all six rows of the `01-VALIDATION.md` negative-control matrix and capture the
    script's literal output for each:
    1. **Gate 1** — add a declaration in a `Spqr/` module that triggers a non-sorry warning.
       Expect gate 1 FAIL with the warning printed.
    2. **Gate 2** — introduce a declaration the standard linter set rejects. Expect gate 2
       FAIL with an `error:` line.
    3. **Gate 3b** — add a one-line theorem closed by `sorry` in a **new** `Spqr/Specs/`
       module, re-exported from `Spqr.lean`. Expect gate 3b FAIL with the new declaration
       listed as `direct`. This is the only run in the whole phase that exercises the delta
       path, because Phase 1 changes no `*.lean` file (B-4).
    4. **Gate 3b silent-failure control** — the same theorem in a module **not** re-exported
       from `Spqr.lean`. Expect gate 3b to **PASS**. Record this: it is `Audit.lean`'s
       documented blind spot and the reason plan 01-01 added the re-export line to the
       template checklist. A control that passes is the finding here, not a failure.
    5. **Gate 4** — request `#print axioms` on a misspelled name. Expect gate 4 FAIL naming
       the unknown identifier, **not** a silent pass (P-1).
    6. **Gate 4** — request a theorem whose closure contains `sorryAx`. Expect gate 4 FAIL
       unless `--allow-sorry` names it; run it both ways and record both outcomes.

    Paste every observed output into the SUMMARY as a six-row table: control, what was
    changed, expected, observed, verdict.

    Then tear the worktree down with `git worktree remove --force <path>` — never `rm -rf` —
    and confirm `git worktree list` is back to the baseline plus the unrelated
    `spqr-pr340` entry. Confirm `git status --porcelain` in the main tree shows no `*.lean`
    change and no new file.
  </action>
  <verify>
    <automated>git status --porcelain -- '*.lean' Spqr SrcTranslated src | wc -l | grep -qx 0 && git worktree list | grep -cv 'spqr-gate-baseline\|spqr-pr340' | grep -qx 1</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY contains a six-row table with the literal script output for every control
    - Controls 1, 2, 3 and 5 each show the gate reporting FAIL with a non-zero script exit
    - Control 4 shows gate 3b reporting PASS, and the SUMMARY names it as `Audit.lean`'s
      documented blind spot with a pointer to the `Spqr.lean` re-export checklist line added
      by plan 01-01
    - Control 6 shows FAIL without `--allow-sorry` and PASS with it, both outputs recorded
    - `git status --porcelain -- '*.lean' Spqr SrcTranslated src` is empty in the main tree
    - `git worktree list` contains no scratch worktree beyond the gate baseline and the
      pre-existing `spqr-pr340`
    - `git stash list` is empty — no breakage was parked in a stash
    - `01-VALIDATION.md`'s `nyquist_compliant` and `wave_0_complete` flags can be flipped to
      `true` on the evidence in this SUMMARY (state so explicitly)
  </acceptance_criteria>
  <done>All six negative controls have been executed with their literal output recorded, four gates are proved capable of failing, the `Audit.lean` blind spot is documented with evidence, and the scratch worktree is gone with the main tree untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate script verdict → merge decision | This plan is where the verdict stops being a claim and becomes evidence. |
| scratch worktree with broken Lean → working tree | Task 3 deliberately introduces warnings, lint errors and `sorry`s; none may reach the index. |
| CI run log → local comparison | The "matches CI" criterion is judged against a live GitHub Actions run whose content this plan reads but does not control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Spoofing | a gate reporting PASS without running | mitigate | **This plan is the mitigation.** The six-row negative-control matrix in task 3 is the only evidence any gate can fail; Phase 1 changes no `*.lean` file, so gate 3b is otherwise never exercised (B-4). Controls are mandatory, not optional, and their literal output goes into the SUMMARY. |
| T-1-20 | Spoofing | tuning the local script until it agrees with CI | mitigate | Task 2 forbids adjusting the script on disagreement: it stops and reports with both outputs. Agreement is an observation, not a target. |
| T-1-05 | Spoofing | stale baseline manifest | mitigate | The cache filename is asserted to contain `git rev-parse origin/main`; the repo-root manifest mtime is recorded before and after to prove it was regenerated, not reused (P-4). |
| T-1-21 | Tampering | broken declarations leaking from the scratch worktree | mitigate | All breakages happen in a detached `git worktree` outside the repo; teardown uses `git worktree remove --force`; `git status`, `git worktree list` and `git stash list` are all asserted clean afterwards. |
| T-1-13 | Denial of service | destructive teardown | mitigate | `git worktree remove` only; `rm -rf` is not used in this plan. |
| T-1-04 | Tampering | widening the trusted base under cover of a probe | mitigate | The allowlist may grow **only** if probe (2) disproves A2, and only by the two named `opaque` declarations, with written justification (PC-4). |
| T-1-SC | Tampering | npm/pip/cargo installs | n/a | No packages installed. `lake exe cache get` downloads pre-built mathlib oleans from the toolchain's own cache, pinned by `lake-manifest.json`; it is not a package-manager install and adds no new dependency. RESEARCH.md § Package Legitimacy Audit: not applicable to this phase. |
</threat_model>

<verification>
1. `./scripts/check-gates.sh spqr.kdf.hkdf_to_slice_spec; echo $?` → `0`, with a verdict line for all four gates
2. `test -f ".gate-cache/sorry-manifest-$(git rev-parse origin/main).txt"` → exit 0
3. `test ! -f .sorry-delta-comment.md` → exit 0
4. `git status --porcelain -- '*.lean' Spqr SrcTranslated src` → empty
5. `git worktree list` → baseline plus the pre-existing `spqr-pr340`, nothing else
6. `grep -q 'Measured cold-run cost: TBD' scripts/README.md` → exit 1
</verification>

<success_criteria>
- The gate script runs end to end on a built tree and reports a per-gate verdict (INFRA-03)
- RESEARCH.md assumptions A1 and A2 are settled with pasted evidence, and the gate-4
  matcher uses observed strings
- The local verdict on `origin/main` agrees gate-by-gate with CI's latest `main` run
  (ROADMAP criterion 2)
- All six negative controls have run, with the silent-failure control recorded as a
  documented blind spot rather than a pass
- `scripts/README.md` carries the measured cold-run cost
- The committed diff touches zero `*.lean` files and zero `src/` / `SrcTranslated/` paths
</success_criteria>

<output>
Create `.planning/phases/01-gates-issues-and-deviation-decisions/01-06-SUMMARY.md` when done.
Record: the four verbatim `#print axioms` probe outputs, the A1/A2 verdicts, the cold-run
timings, the local-vs-CI comparison table with the run id, and the six-row negative-control
matrix with literal output.
</output>
