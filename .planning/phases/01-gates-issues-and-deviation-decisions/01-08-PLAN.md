---
phase: 01-gates-issues-and-deviation-decisions
plan: 08
type: execute
wave: 4
depends_on: ["01-07"]
files_modified:
  - docs/spqr-properties.md
autonomous: true
requirements: [DEV-01, DEV-02, DEV-03, DEV-04, CAT-01]
allowed_sorries: 0
pr_boundary: B
lean_files_touched: 0
src_touched: false
srctranslated_touched: false

must_haves:
  truths:
    - "Section 11 records a Decision and a dated Status for each of D1 to D5"
    - "PROP-42, PROP-30, PROP-47, PROP-50 and PROP-43 each state the provisional behaviour and cite their D-row"
    - "PROP-43 in section 7 carries a written caller contract, including what happens to the already-updated authenticator on a verify_ct failure"
    - "A Phase 5 or Phase 6 planner can quote the catalog text verbatim as a theorem statement without re-deriving it from the code"
    - "The section 12 index changed in the same PR as the section text, demonstrating CAT-01 on itself"
  artifacts:
    - path: "docs/spqr-properties.md"
      provides: "Section 11 decision table, five reworked property statements, the D5 caller contract, and the updated section 12 row"
      contains: "Provisional (2026-09-14)"
  key_links:
    - from: "docs/spqr-properties.md section 11 row D5"
      to: "docs/spqr-properties.md section 7 PROP-43"
      via: "the Decision cell pointing at the written caller contract"
      pattern: "PROP-43"
    - from: "docs/spqr-properties.md PROP-43"
      to: "src/lib.rs:356, src/v1/unchunked/send_ct.rs:107, src/v1/unchunked/send_ek.rs:160"
      via: "file:line citations backing the contract"
      pattern: "lib\\.rs:3"
---

<objective>
Record a decision for each of D1–D5 in `docs/spqr-properties.md` section 11, write the D5
caller contract into section 7 under PROP-43, and restate PROP-42, PROP-30, PROP-47 and
PROP-50 so each names the provisionally-decided behaviour and cites its D-row.

Purpose: DEV-01 through DEV-04, and the ROADMAP phase goal — "so no later theorem has to be
restated". Phases 5 and 6 quote this text verbatim as their theorem statements, and every
one of those statements is currently ambiguous between the spec's behaviour and the code's.
Output: edits to one file, `docs/spqr-properties.md`. No `src/`, no Lean.
</objective>

<phase_type>
Documentation-and-tooling phase; zero `*.lean` files, `allowed_sorries: 0`.

**`src/` is read-only in this plan.** D-10 records provisional decisions only; the code
freeze in `CLAUDE.md` limits `src/` changes to D1–D5 resolutions agreed with Signal, and no
such agreement exists. Every `src/` reference below is a citation to read, never to edit.
Any hunk under `src/` is a BLOCKER for the plan reviewer (rubric item 8) and `spqr-eval`.
</phase_type>

<sequencing_note>
**Research pitfall P-6, stated up front so the reviewer does not read it as a PC-5
violation.** PC-5 requires a catalog statement change to land in the same PR as the theorem
it describes. This PR changes wording for theorems that do not exist yet — they arrive in
phases 5 and 6. That is correct and deliberate: DEV-01..04 are decisions, not theorem
statements, and recording them before the proofs is the entire point of Phase 1. Repeat
this in PR B's body.
</sequencing_note>

<layout_decision>
**Reading of D-12, decided by the user (research Q-4, option b). State this reasoning in
the PR B body; it is a deliberate interpretation, not drift.**

D-12 says section 11 "keeps its single table and gains two columns: Decision and Status
(Provisional / Decided, with date). No per-deviation subsections."

Adding two columns to the existing five (ID, Where, Spec, Code, Impact) produces seven
columns of long prose and roughly 600-character Markdown lines; the D3 Impact cell alone is
three sentences. D-12's actual prohibition is per-deviation subsections, which would
scatter the deviations across the document. A second narrow table directly beneath the
existing one violates neither the letter of "no subsections" nor its purpose, keeps section
11 readable, and is the table Phases 5 and 6 will quote.

So: the existing five-column table and its five rows stay verbatim, byte for byte, and a
second table with header `| ID | Decision | Status |` is added immediately beneath it. Both
tables live under the same `## 11.` heading. No new heading, no subsection.
</layout_decision>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-gates-issues-and-deviation-decisions/01-CONTEXT.md
@.planning/phases/01-gates-issues-and-deviation-decisions/01-RESEARCH.md
@.planning/phases/01-gates-issues-and-deviation-decisions/01-VALIDATION.md
@docs/spqr-properties.md

Locked decisions implemented here: **D-10** (every decision is "code as implemented is
authoritative", every status is `Provisional (2026-09-14)`; Phase 1 does not wait on
Signal), **D-12** as read in the layout decision above, **D-13** (the D5 caller contract is
written in section 7 under PROP-43 and section 11's D5 row points at it), **D-14**
(PROP-30, PROP-47, PROP-50 and PROP-43 state the provisional behaviour and cite their
D-row; PROP-42 section 5 is aligned with D1), **D-15** (this is PR B), **CAT-01** (the
section 12 index changes in the same PR).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the section 11 decision table and update the section 12 index row</name>
  <files>docs/spqr-properties.md</files>
  <read_first>
    - `docs/spqr-properties.md` lines 431–447 — the `## 11. Deviations from the spec`
      heading, the five-column table header, the five D-rows, and the closing paragraph
      ending "Until decided, the properties above describe the code as it is."
    - `docs/spqr-properties.md` lines 449–491 — the `## 12. Index` table; its last row is
      `| D1–D5 | 11 | Deviations |`
    - RESEARCH.md lines 856–913 — section 11's current shape with the five rows quoted
      verbatim, and the two layout options with the recommendation
    - RESEARCH.md lines 924–946 — the section 12 index format and the CAT-01 recommendation
      for the D1–D5 row
    - `01-CONTEXT.md` Specific Ideas — "section 11's existing sentence 'Until decided, the
      properties above describe the code as it is' becomes the definition of Status =
      Provisional"
    - `01-VALIDATION.md` "The section 11 table parse check" — the two awk assertions this
      task must satisfy
  </read_first>
  <action>
    Leave the existing five-column table — header, separator and all five D-rows — byte for
    byte unchanged. Insert a blank line after it, then a second Markdown table with header
    `| ID | Decision | Status |` and five rows in the order D1, D2, D3, D4, D5.

    Every Status cell is exactly `Provisional (2026-09-14)` (D-10). Every Decision cell
    states that the code as implemented is authoritative and names the concrete behaviour,
    so the cell stands alone when quoted:
    D1 — KDF_AUTH uses salt `[0u8; 32]` and IKM `root_key ‖ update_key`; PROP-42 is stated
    against that. D2 — a message with `msg.epoch > epoch` is `Err(EpochOutOfRange)`, not a
    no-op, except `Ct2Sampled` at `epoch + 1`. D3 — `EkSentCt1Received.send` emits
    `Ct1Ack(true)` and `EkReceivedCt1Sampled.recv` accepts `Ct1Ack(true)`. D4 —
    `Ct1Acknowledged.recv` also accepts `Ek` chunks. D5 — the caller contract is written
    under PROP-43 in section 7; the cell must name PROP-43 explicitly (D-13).

    Then rewrite the closing paragraph at lines 443–447. Keep its first sentence listing the
    four resolution shapes. Replace the final sentence with a definition of the new Status
    column: Provisional means the properties above describe the code as it is, pending a
    ruling from Signal; Decided will carry the ruling's date; and a ruling that disagrees
    with the code opens a restatement issue for the affected property rather than a silent
    edit (D-10). Add one sentence pointing at `docs/signal-deviation-questions.md` as the
    outgoing ask — that file is created by plan 01-09, and the forward reference is
    intentional because both land in PR B.

    Finally, satisfy CAT-01 in the very PR that introduces the rule: change the section 12
    index row `| D1–D5 | 11 | Deviations |` to
    `| D1–D5 | 11 | Deviations (D1–D5 provisional) |`. Change no other section 12 row; no
    property status changes in this phase.
  </action>
  <verify>
    <automated>bash -c "awk '/^## 11\./{f=1} /^## 12\./{f=0} f \&\& /^\| D[1-5] \| .* \| Provisional /' docs/spqr-properties.md | wc -l" | grep -qx 5</automated>
  </verify>
  <acceptance_criteria>
    - The awk scan of section 11 for rows matching `^| D[1-5] | ... | Provisional ` returns
      exactly `5` lines
    - `grep -c 'Provisional (2026-09-14)' docs/spqr-properties.md` returns `5`
    - The field-count scan `awk '/^## 11\./{f=1} /^## 12\./{f=0} f && /^\| D[1-5] \|/{n=split($0,c,"|"); print n}'` yields ten rows: five at the wide table's field count and five at the narrow table's (paste the output in the SUMMARY)
    - `git diff -U0 docs/spqr-properties.md` contains no deleted line matching `^-\| D[1-5] \|`
      — the original five-column rows are untouched
    - `grep -c '^## 11\.' docs/spqr-properties.md` returns `1`, and `grep -c '^### '
      docs/spqr-properties.md` is unchanged from before the edit — no per-deviation
      subsection was introduced (D-12)
    - `grep -q 'signal-deviation-questions.md' docs/spqr-properties.md` exits 0
    - `grep -q 'Deviations (D1–D5 provisional)' docs/spqr-properties.md` exits 0 (CAT-01)
    - `grep -c '^| PROP-' docs/spqr-properties.md` is unchanged from before the edit
    - `git status --porcelain -- src SrcTranslated` is empty
  </acceptance_criteria>
  <done>Section 11 has a five-row Decision/Status table beneath an untouched five-column table, every status reads `Provisional (2026-09-14)`, the closing paragraph defines the status values, and the section 12 D1–D5 row records the provisional state.</done>
</task>

<task type="auto">
  <name>Task 2: Restate PROP-42, PROP-30, PROP-47 and PROP-50 against their D-row decisions</name>
  <files>docs/spqr-properties.md</files>
  <read_first>
    - `docs/spqr-properties.md` lines 204–212 — PROP-42, closing with "Info and length match
      §2.2. Salt and IKM do not: see deviation D1. **Proved** as `update_spec`"
    - `docs/spqr-properties.md` lines 305–320 — PROP-30, the six-row dispatch table (rows 4
      and 6 read `Err(EpochOutOfRange)` / "no-op (D2)") and its status line
    - `docs/spqr-properties.md` lines 321–348 — PROP-47, the 13-row transition table where
      row 11 reads "also accepts Ek (D4)" and row 12 "also accepts Ct1Ack(true) (D3)", and
      the trailing paragraph about `EkSentCt1Received.send`
    - `docs/spqr-properties.md` lines 349–360 — PROP-50, whose closing sentences mention D2
      and "holds only for in-order delivery across epochs"
    - `docs/spqr-properties.md` section 11 as left by task 1 — the Decision cells these
      statements must agree with word for word
    - RESEARCH.md lines 914–923 — the per-property table saying exactly what must change
    - `.planning/ROADMAP.md` lines 99–121 — the Phase 5 and Phase 6 success criteria whose
      theorems will quote this text
    - `src/v1/states.rs` around lines 182, 464–467 and 484–492 — read-only confirmation of
      the D3 and D4 behaviour before writing it down
  </read_first>
  <action>
    Four edits, each adding an explicit provisional-behaviour sentence and a D-row citation.
    Do not delete the existing spec-versus-code contrast in any of them: the catalog must
    keep showing both, with the decision naming which one the theorems target.

    PROP-42 (section 5, D1, DEV-01): after the existing "Salt and IKM do not: see deviation
    D1" sentence, add that the provisional D1 decision makes the code authoritative, so the
    property to prove is the code's salt `[0u8; 32]` and IKM `root_key ‖ update_key`, which
    `update_spec` already proves; and that if Signal rules for the spec, D1 flips to Decided
    and PROP-42 is restated under a new issue. Keep the `**Proved** as update_spec` sentence
    and its file path.

    PROP-30 (section 8, D2, DEV-02): leave the six-row dispatch table intact. After it, add
    that under the provisional D2 decision the code's strict behaviour is the one to prove —
    `msg.epoch > epoch` yields `Err(EpochOutOfRange)` for every variant other than
    `Ct2Sampled` at `epoch + 1`, rather than the spec's no-op — so the Greater rows are
    proved as errors, not as no-ops. Leave the existing status line unchanged; no proof
    status changes in this phase.

    PROP-47 (section 8, D3 and D4, DEV-03): leave the 13-row transition table intact. Extend
    the trailing paragraph to state that under the provisional D3 and D4 decisions the
    code's behaviour is what transitions 1–13 must be proved against:
    `EkSentCt1Received.send` emits `Ct1Ack(true)` where the spec emits `None` (D3, and row
    12 accepts it), and `Ct1Acknowledged.recv` also accepts `Ek` chunks (D4, row 11) with
    integrity still checked by PROP-25. Name D3 and D4 by ID. Leave "Status: **Open** for
    all 13" unchanged.

    PROP-50 (section 8, D2, DEV-02): replace the existing D2 sentence with a statement of
    the provisional decision — because a future-epoch message is an error rather than a
    no-op, the liveness property is stated for in-order delivery: from
    `(KeysUnsampled_A(e), NoHeaderReceived_B(e))`, in-order delivery of every message
    reaches `(NoHeaderReceived_A(e+1), KeysUnsampled_B(e+1))` and every reachable pair has a
    next step. That is the form Phase 6's TRACE-04 quotes (ROADMAP Phase 6 criterion 4).
    Keep the §2.6 and Fig. 2 citations and leave "Status: **Open**" unchanged.

    Every added sentence must cite its D-row by ID so the validation grep finds it, and must
    agree word for word with the matching Decision cell from task 1. If the two disagree,
    Phases 5 and 6 will quote the wrong one.
  </action>
  <verify>
    <automated>bash -c 'for p in 42 30 47 50; do grep -A30 "^### PROP-$p " docs/spqr-properties.md | grep -qE "D[1-5]" || exit 1; done'</automated>
  </verify>
  <acceptance_criteria>
    - For each of `42`, `30`, `47`, `50`: `grep -A30 "^### PROP-$p " docs/spqr-properties.md | grep -qE 'D[1-5]'` exits 0
    - `grep -A25 '^### PROP-42 ' docs/spqr-properties.md | grep -qi 'provisional'` exits 0
    - `grep -A25 '^### PROP-30 ' docs/spqr-properties.md | grep -q 'EpochOutOfRange'` exits 0
    - `grep -A45 '^### PROP-47 ' docs/spqr-properties.md | grep -q 'Ct1Ack(true)'` and the
      same range matched against `Ek` chunks acceptance both exit 0
    - `grep -A20 '^### PROP-50 ' docs/spqr-properties.md | grep -q 'in-order'` exits 0
    - `grep -A25 '^### PROP-30 ' docs/spqr-properties.md | grep -q 'Proved (branch)'` exits 0
      and `grep -A45 '^### PROP-47 ' docs/spqr-properties.md | grep -q 'Status: \*\*Open\*\* for all 13'`
      exits 0 — no proof status was changed
    - `grep -c '^### PROP-' docs/spqr-properties.md` is unchanged from before the edit
    - For each of D1, D2, D3, D4: the behaviour phrase used in the property text is byte-identical
      to the phrase in that row's section 11 Decision cell (paste both, side by side, in the SUMMARY)
    - `git status --porcelain -- src SrcTranslated` is empty
  </acceptance_criteria>
  <done>PROP-42, PROP-30, PROP-47 and PROP-50 each name their D-row, state the provisional behaviour in the same words as the section 11 Decision cell, and keep their existing proof status and spec-versus-code contrast.</done>
</task>

<task type="auto">
  <name>Task 3: Write the D5 caller contract into section 7 under PROP-43</name>
  <files>docs/spqr-properties.md</files>
  <read_first>
    - `docs/spqr-properties.md` lines 285–294 — the whole current PROP-43 paragraph,
      including "Status: **Open**. See deviation D5 for the spec's stronger requirement."
    - RESEARCH.md lines 947–967 — the D5 caller contract with its three-row `src/` evidence
      table, and the paragraph naming the non-obvious asymmetry
    - `src/v1/unchunked/send_ct.rs` around line 107 — `self.auth.verify_hdr(self.epoch,
      &hdr, mac)?;`, the header-MAC failure propagated before `HeaderReceived` is constructed
    - `src/authenticator.rs` lines 44–90 — `verify_ct` raising `Err(Error::InvalidCtMac)` at
      :59 and `verify_hdr` raising `Err(Error::InvalidHdrMac)` at :82/:84
    - `src/v1/unchunked/send_ek.rs` lines 145–175 — `decaps` at :150, `hkdf_to_vec` at :156,
      `auth.update(epoch, &ss)` at :158, then `auth.verify_ct(epoch, &ct1, &mac)?;` at :160
    - `src/lib.rs` lines 356–455 — `pub fn recv(state: &SerializedState, msg:
      &SerializedMessage) -> Result<Recv, Error>`, the propagation at :425, and the sole
      state write inside `Ok(...)` at :437–451
    - `.planning/ROADMAP.md` lines 99–109 — Phase 5 criterion 5 and requirement AUTH-01, the
      theorem that will quote this contract
    - `01-VALIDATION.md` — the assertion that `grep -A30 '^### PROP-43'` must match `caller`
  </read_first>
  <action>
    Extend the PROP-43 paragraph in section 7 with the written caller contract required by
    D-13. Keep the existing text — the two failure sites, the `lib.rs` no-write claim, the
    transition-5 ordering note, "Status: **Open**", and the pointer to D5.

    Add a clearly delimited **Caller contract (D5, provisional 2026-09-14)** paragraph
    stating, in this order:
    1. `spqr::recv` returns `Err(InvalidHdrMac)` or `Err(InvalidCtMac)` on a MAC failure.
    2. It mutates nothing: the library takes state by reference
       (`src/lib.rs:356`) and the only state write is inside the `Ok` arm
       (`src/lib.rs:437-451`), so the caller's previous `SerializedState` remains valid.
    3. The caller may reuse that state for a retry.
    4. The library does not terminate the session: there is no abort, no zeroization and no
       teardown. Deciding whether to negotiate a new session after repeated MAC failures is
       the caller's responsibility — which is where this deviates from section 2.4's
       "should not proceed with the session and should negotiate a new session".
    5. **The asymmetry, stated explicitly because it is the half a theorem is most likely to
       miss:** on a `verify_ct` failure the epoch secret has *already* been derived and
       `auth.update` has *already* run on the local authenticator copy
       (`src/v1/unchunked/send_ek.rs:150,156,158` before the check at `:160`), but because
       `recv_ct2` consumed `self` and returns `Err`, that updated authenticator is dropped
       and the caller's stored state still holds the pre-`update` authenticator. On a
       `verify_hdr` failure the check precedes any state construction
       (`src/v1/unchunked/send_ct.rs:107`), so no such window exists.

    Cite each claim with its `path:line`. These are the citations the Phase 5 AUTH-01
    theorem and the plan reviewer will both re-verify against the pinned commit
    (`d47083c`), so they must be exact — re-read each line before writing it, and if any
    citation does not match the tree, record the discrepancy in the SUMMARY rather than
    adjusting the claim to fit.

    Then make section 11's D5 Decision cell (written in task 1) point at this paragraph by
    name, if it does not already.

    Do not change PROP-43's status: it stays **Open** until Phase 5 proves AUTH-01. Do not
    edit any file under `src/`.
  </action>
  <verify>
    <automated>bash -c "grep -A40 '^### PROP-43' docs/spqr-properties.md | grep -q 'aller' && grep -A40 '^### PROP-43' docs/spqr-properties.md | grep -q 'send_ek.rs:1'"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A40 '^### PROP-43' docs/spqr-properties.md | grep -q 'aller contract'` exits 0
    - The PROP-43 range contains all five citations: `lib.rs:356`, `lib.rs:437`,
      `send_ct.rs:107`, `send_ek.rs:160` and at least one of `send_ek.rs:150|156|158`
      (grep for each; paste the results in the SUMMARY)
    - The PROP-43 range mentions both `InvalidHdrMac` and `InvalidCtMac`
    - The PROP-43 range states the asymmetry: it contains `auth.update` and a phrase saying
      the updated authenticator is dropped
    - `grep -A40 '^### PROP-43' docs/spqr-properties.md | grep -q 'Status: \*\*Open\*\*'` exits 0
    - Section 11's D5 Decision cell names PROP-43
    - Every `path:line` citation was re-read in the tree at the pinned commit and matches;
      any mismatch is recorded in the SUMMARY instead of being papered over
    - `git status --porcelain -- src SrcTranslated` is empty
    - `git diff --name-only` for this whole plan lists only `docs/spqr-properties.md`
  </acceptance_criteria>
  <done>PROP-43 carries a written, citation-backed caller contract covering the error values, the no-mutation guarantee, retry, the absence of session teardown, and the dropped-authenticator asymmetry; section 11's D5 row points at it; the status is still Open.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalog text → future theorem statements | Phases 5 and 6 quote this text verbatim. A statement written here that does not match the code produces a theorem proving the wrong thing. |
| catalog text → Signal reviewers | Section 11 is what an external reviewer reads to learn what was decided and on whose authority. |
| `src/` → citations | The contract's authority rests on `path:line` references into a frozen tree at commit `d47083c`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-07 | Spoofing | a provisional decision reading as a Signal ruling | mitigate | Every Status cell is literally `Provisional (2026-09-14)`; the closing paragraph defines Provisional as "pending a ruling from Signal"; plan 01-09's `docs/signal-deviation-questions.md` is the record that the ask is outstanding. Asserted by `grep -c 'Provisional (2026-09-14)'` returning 5. |
| T-1-24 | Tampering | catalog statement drifting from the code it cites | mitigate | Every behaviour claim carries a `path:line` citation that task 3 requires re-reading in the tree; a mismatch is recorded, not silently corrected. Rubric item 1 (catalog fidelity) is the reviewer-side check. |
| T-1-25 | Tampering | editing `src/` to resolve a deviation | mitigate | `src/` is read-only this phase (D-10: provisional only; code freeze needs Signal's agreement). `git status --porcelain -- src SrcTranslated` is asserted empty in every task. |
| T-1-26 | Repudiation | the section 11 Decision cell and the property text disagreeing | mitigate | Task 2 requires byte-identical behaviour phrases between the Decision cell and the property paragraph, pasted side by side in the SUMMARY. |
| T-1-27 | Tampering | silently changing a proof status while editing prose | mitigate | PROP-30's "Proved (branch)", PROP-47's "Open for all 13" and PROP-43's "Open" are each asserted unchanged; `grep -c '^| PROP-'` in section 12 is asserted unchanged. |
| T-1-SC | Tampering | npm/pip/cargo installs | n/a | No packages installed. RESEARCH.md Package Legitimacy Audit: not applicable to this phase. |
</threat_model>

<verification>
1. The section 11 awk scan returns five `Provisional` decision rows
2. `grep -c 'Provisional (2026-09-14)' docs/spqr-properties.md` → `5`
3. Each of PROP-42, PROP-30, PROP-47, PROP-50 cites a `D[1-5]` within 30 lines of its heading
4. `grep -A40 '^### PROP-43' docs/spqr-properties.md | grep -q 'aller contract'` → exit 0
5. `grep -q 'Deviations (D1–D5 provisional)' docs/spqr-properties.md` → exit 0
6. `git status --porcelain -- src SrcTranslated` → empty
7. `git diff --name-only` for the plan → only `docs/spqr-properties.md`
</verification>

<success_criteria>
- Section 11 carries a recorded Decision and a dated Provisional Status for each of D1–D5
  (DEV-01..DEV-04, ROADMAP criterion 4)
- PROP-42's section 5 statement matches the D1 decision (ROADMAP criterion 4)
- PROP-30, PROP-50, PROP-47 and PROP-43 state the decided behaviour for D2, D3/D4 and D5 so
  Phases 5–6 can quote them verbatim (ROADMAP criterion 5)
- PROP-43 carries the written caller contract with exact `src/` citations, including the
  dropped-authenticator asymmetry (DEV-04, D-13)
- The section 12 index changed in the same PR (CAT-01)
- Zero hunks under `src/`, `SrcTranslated/` or any `*.lean` file
</success_criteria>

<output>
Create `.planning/phases/01-gates-issues-and-deviation-decisions/01-08-SUMMARY.md` when done.
Record: the section 11 field-count scan output, the side-by-side Decision-cell versus
property-text comparison for D1–D4, the five re-verified `src/` citations for the D5
contract, and the before/after counts for `^### PROP-` and `^| PROP-`.
</output>
