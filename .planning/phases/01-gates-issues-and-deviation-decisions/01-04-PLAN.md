---
phase: 01-gates-issues-and-deviation-decisions
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - scripts/create-property-issues.sh
  - scripts/property-issues.tsv
  - scripts/property-issue-labels.tsv
autonomous: true
requirements: [INFRA-02]
allowed_sorries: 0
pr_boundary: A
lean_files_touched: 0
src_touched: false
srctranslated_touched: false

must_haves:
  truths:
    - "D-02: each generated body cross-links #510-#533 and #44 as related, editing no existing issue"
    - "D-03: the label scheme is the template's type:* and status:* set, one-to-one with catalog status"
    - "D-05: one idempotent dry-run-by-default script is the single source of truth for issue bodies"
    - "Running the script with no flags prints a title-and-label listing and files nothing"
    - "Running it with --execute and an explicit ID files exactly that issue; a second run skips it"
    - "Every generated body is rendered from docs/ISSUE_TEMPLATE.md with no placeholder left unsubstituted"
    - "Property text in a body comes from docs/spqr-properties.md, not from a copy inside the script"
    - "No property text ever reaches a shell command line"
  artifacts:
    - path: "scripts/create-property-issues.sh"
      provides: "Idempotent, dry-run-by-default issue filer and label creator"
      contains: "--body-file"
      min_lines: 90
    - path: "scripts/property-issues.tsv"
      provides: "One row per Phase 1 deliverable, 11 tab-separated columns"
      contains: "INFRA-01"
    - path: "scripts/property-issue-labels.tsv"
      provides: "The 15 labels of the ISSUE_TEMPLATE Labels section, with colours"
      contains: "status:proved-branch"
  key_links:
    - from: "scripts/create-property-issues.sh"
      to: "docs/ISSUE_TEMPLATE.md"
      via: "placeholder substitution over the template file"
      pattern: "ISSUE_TEMPLATE\\.md"
    - from: "scripts/create-property-issues.sh"
      to: "docs/spqr-properties.md"
      via: "sed range on the description_anchor heading"
      pattern: "spqr-properties\\.md"
    - from: "scripts/create-property-issues.sh"
      to: "gh issue create"
      via: "--body-file with a mktemp file, never --body with an interpolated string"
      pattern: "gh issue create .*--body-file"
---

<objective>
Author the replacement issue tooling: `scripts/create-property-issues.sh` plus its two TSV
data files. The script renders `docs/ISSUE_TEMPLATE.md`, pulls property text from
`docs/spqr-properties.md`, creates the template's 15 labels idempotently, and files issues
through `gh issue create --body-file` — dry-run by default, `--execute` opt-in, exact-title
duplicate detection.

Purpose: INFRA-02 as reworded by D-09. The old `issues/create_issues.sh` duplicates 45
issue bodies as heredocs, `eval`s an interpolated command string, and uses a label scheme
that matches neither the template nor the catalog's status values.
Output: three files under `scripts/`. **This plan writes the tooling and runs it only in
dry-run mode; it creates no GitHub state.** Label creation and issue filing happen in plan
01-07, behind the D-01 user checkpoint.
</objective>

<phase_type>
Documentation-and-tooling phase; zero `*.lean` files, zero `src/` and `SrcTranslated/`
hunks, `allowed_sorries: 0`.
</phase_type>

<do_not_hand_roll>
RESEARCH.md lines 1016–1034 is binding:
- **No `eval`, no interpolated command strings.** Write each body to a `mktemp` file and
  pass `--body-file`. Property text contains backticks, `$`, `‖`, `⦃ ⦄` and quotes; the old
  script's `eval` shape (`issues/create_issues.sh:28-34,79-88`) is a correctness and
  injection hazard.
- **No `gh label create … || true`.** Use `--force`, which updates an existing label. `|| true`
  also swallows auth failures, rate limits and typos, reporting success having created nothing.
- **No local "already filed" state file.** GitHub is the state: query it with
  `gh issue list --state all --search "\"$title\" in:title"` and an exact `--jq` match.
- **No second copy of the issue body text.** The template is the shape, the catalog and
  `.planning/REQUIREMENTS.md` are the prose (D-05: one source of truth).
</do_not_hand_roll>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-gates-issues-and-deviation-decisions/01-CONTEXT.md
@.planning/phases/01-gates-issues-and-deviation-decisions/01-RESEARCH.md
@.planning/phases/01-gates-issues-and-deviation-decisions/01-VALIDATION.md
@docs/ISSUE_TEMPLATE.md

Locked decisions implemented here: **D-01** (dry-run listing exists for the user to review
before anything is filed), **D-02** (fresh per-deliverable issues, cross-linking #44 and
#510–#533 as "related", never editing them), **D-03** (the template's `type:*`/`status:*`
label scheme; the repo's existing `Proof`/`Specification`/`Infrastructure`/`Sorry` labels are
not used), **D-04** (non-property deliverables get issues too, with `type:proof-infra` or
`spec-deviation`), **D-05** (one idempotent script, one source of truth), **D-09**
(just-in-time filing by explicit ID at each PR boundary).

Depends on plan 01-01 because the script renders the **corrected** template; rendering the
pre-correction template would put a non-existent gate into every issue body.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the label table and the issue script</name>
  <files>scripts/create-property-issues.sh, scripts/property-issue-labels.tsv</files>
  <read_first>
    - `docs/ISSUE_TEMPLATE.md` in full, as left by plan 01-01 — the `{PLACEHOLDER}` set
      (lines 8–92 and 127) and the Labels HTML comment (lines 111–125) that defines the
      15-label scheme
    - `.planning/phases/01-gates-issues-and-deviation-decisions/01-RESEARCH.md` lines
      621–722 — environment facts (repo slug, `gh` 2.100.0 authenticated as `astefano` with
      `repo` scope), the six labels that exist today (none of them `type:*`/`status:*`), and
      the exact idempotent `gh` invocations
    - `.planning/phases/01-gates-issues-and-deviation-decisions/01-RESEARCH.md` lines
      723–797 — the placeholder→source table and the TSV layout recommendation
    - `.planning/phases/01-gates-issues-and-deviation-decisions/01-RESEARCH.md` lines
      1158–1192 — the idempotent label-creation and exact-title issue-filing skeletons
    - `.planning/phases/01-gates-issues-and-deviation-decisions/01-RESEARCH.md` lines
      1069–1080 and 1093–1100 — pitfalls P-5 (duplicate filing) and P-8 (`gh issue create`
      prints a URL, not a number)
    - `.planning/phases/01-gates-issues-and-deviation-decisions/01-RESEARCH.md` lines
      1325–1360 — the Security Domain table this script's controls come from
    - `issues/create_issues.sh` lines 1–90 — read it for the colour values of the nine
      `type:*` labels and the two special labels, and as the counter-example whose `eval`
      and `|| true` shapes must not be reproduced
  </read_first>
  <action>
    Create `scripts/property-issue-labels.tsv`: three tab-separated columns
    `name`, `color`, `description`; `#` comment lines allowed. **15 rows**: the nine
    `type:algebraic-spec`, `type:state-invariant`, `type:behavioral-spec`,
    `type:correspondence`, `type:serialization`, `type:deviation-flag`,
    `type:history-erasure`, `type:progress`, `type:proof-infra`; the four
    `status:open`, `status:axiom`, `status:proved-branch`, `status:proved`; and the two
    special `modelling-assumption`, `spec-deviation`. Reuse the existing colours from
    `issues/create_issues.sh` for the nine type labels and the two special labels; for the
    status axis use `status:open`=`d93f0b`, `status:axiom`=`fbca04`,
    `status:proved-branch`=`bfd4f2`, `status:proved`=`2ea44f`. Each `status:*` description
    must name the `docs/spqr-properties.md` status value it mirrors one-to-one (D-03). Do
    **not** include `status:provable-now`, `status:feasible`, `status:axiom-required`,
    `status:blocked` or any `tier:*` label — those are the old script's divergent scheme.

    Create `scripts/create-property-issues.sh`, `set -euo pipefail`, usage
    `./scripts/create-property-issues.sh [--execute] [--labels-only] [--repo SLUG] [ID ...]`.
    Default repo `Beneficial-AI-Foundation/SparsePostQuantumRatchet-verify`.

    Behaviour:
    - **Dry-run is the default.** With no `--execute`, print one line per selected row
      giving the ID, the title and the two labels, plus the path of the rendered body file,
      and call no mutating `gh` subcommand at all (D-01).
    - **`--execute` requires at least one explicit ID.** With `--execute` and no IDs, exit
      non-zero with the message that D-09 files issues just in time and a bulk file is not
      supported. This is the blast-radius control for a `repo`-scoped token (threat T-1-02).
    - With no IDs and no `--execute`, process every row — that is the batch listing D-01
      asks the user to review.
    - `--labels-only` runs just the label step and exits.
    - Validate `--repo` against the ERE `^[A-Za-z0-9._-]+/[A-Za-z0-9._-]+$` and exit
      non-zero otherwise (threat T-1-15).
    - The only mutating `gh` subcommands the script may contain are `gh label create` and
      `gh issue create`. It must not contain `gh pr create` (PC-6: GSD never opens a PR),
      nor `gh issue close`, `gh issue edit`, `gh issue comment` or `gh label delete` (D-02).

    Label step: iterate `scripts/property-issue-labels.tsv` with
    `while IFS=$'\t' read -r name color desc`, skipping `#` and blank lines; in dry-run
    print the intended `gh label create`; with `--execute` run
    `gh label create "$name" --color "$color" --description "$desc" --force --repo "$REPO"`.

    Body rendering, for each selected row of `scripts/property-issues.tsv`:
    - resolve `{DESCRIPTION}` from the row's `description_anchor` column — if it begins with
      `###`, extract the catalog section with a `sed -n` range from that heading in
      `docs/spqr-properties.md` up to the next line beginning `### ` or `## `; if it is the
      literal `REQUIREMENTS`, extract the matching `- [ ] **<ID>**:` line from
      `.planning/REQUIREMENTS.md`; otherwise fail loudly naming the row;
    - substitute `{PROP-ID}`, `{TYPE}`, `{SOURCES}`, `{DESCRIPTION}`, `{LEAN_STATEMENT}`,
      `{DIFFICULTY}`, `{STATUS}`, `{APPROACH}`, `{DEPENDENCIES}`, `{TYPE_LABEL}`,
      `{STATUS_LABEL}` into a copy of `docs/ISSUE_TEMPLATE.md` written to a file created
      under a `mktemp -d` directory with a `trap … EXIT` cleanup. Substitute by reading the
      template and the values as shell variables into a `while read` / parameter-expansion
      loop or a `python3` one-liner reading values from the environment — **never** by
      building a command string and `eval`ing it, and never by passing property text as a
      command-line argument;
    - append a `### Related` block from the row's `related` column: `Umbrella: #44.
      Overlapping per-function issues: #NNN, #NNN. This issue does not supersede them; it
      tracks <ID>.` Omit the sentence for whichever of the two parts the row leaves empty;
    - assert the rendered body has no unsubstituted placeholder: the ERE `\{[A-Z_]+\}` must
      not match, and the script must fail the row if it does.

    Filing step (only with `--execute`): detect an existing issue with
    `gh issue list --repo "$REPO" --state all --limit 200 --search "\"$title\" in:title"
    --json number,title --jq --arg t "$title" 'map(select(.title == $t)) | .[0].number //
    empty'`; if found, print `SKIP  <ID> → already filed as #<n>` and continue (P-5).
    Otherwise `gh issue create --repo "$REPO" --title "$title" --label
    "$type_label,$status_label" --body-file "$body"`, capture stdout as a URL, derive the
    number with `num="${url##*/}"` (P-8: the command prints a URL, not a number), and print
    `FILED <ID> → #<num>`. At the end, print a summary block listing every `FILED`/`SKIP`
    pairing in the form `<ID> #<num>` so the PR-boundary report in plan 01-07 can copy the
    numbers verbatim.

    No `set -x`. Never echo `gh auth token`. Do not create a local state file.
  </action>
  <verify>
    <automated>bash -n scripts/create-property-issues.sh && test "$(grep -vc '^\s*#\|^\s*$' scripts/property-issue-labels.tsv)" -eq 15 && grep -q 'gh issue create' scripts/create-property-issues.sh && grep -q -- '--body-file' scripts/create-property-issues.sh && grep -q -- '--force' scripts/create-property-issues.sh && ! grep -qE '\beval\b|gh pr create|gh issue (close|edit|comment)|gh label delete|set -x' scripts/create-property-issues.sh && ./scripts/create-property-issues.sh --help >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/create-property-issues.sh` exits 0 and the file is executable
    - `grep -vc '^\s*#\|^\s*$' scripts/property-issue-labels.tsv` returns `15`
    - `cut -f1 scripts/property-issue-labels.tsv | grep -c '^type:'` returns `9`;
      `... | grep -c '^status:'` returns `4`
    - `grep -cE 'status:(provable-now|feasible|axiom-required|blocked)|tier:' scripts/property-issue-labels.tsv` returns `0`
    - `awk -F'\t' 'NF!=3 && $0 !~ /^#/ && NF>0 {exit 1}' scripts/property-issue-labels.tsv` exits 0 — every row has exactly 3 fields
    - `grep -qE '\beval\b' scripts/create-property-issues.sh` exits 1
    - `grep -qE 'gh pr create|gh issue close|gh issue edit|gh issue comment|gh label delete' scripts/create-property-issues.sh` exits 1
    - `grep -c 'set -x' scripts/create-property-issues.sh` returns `0`
    - `grep -q -- '--body-file' scripts/create-property-issues.sh` exits 0 and
      `grep -qE "gh issue create[^|]*--body [\"']" scripts/create-property-issues.sh` exits 1
    - `grep -q 'gh label create' scripts/create-property-issues.sh` and
      `grep -q -- '--force' scripts/create-property-issues.sh` exit 0
    - `grep -q 'mktemp -d' scripts/create-property-issues.sh` and
      `grep -q "trap .* EXIT" scripts/create-property-issues.sh` exit 0
    - `grep -q 'in:title' scripts/create-property-issues.sh` and
      `grep -q 'select(.title == \$t)' scripts/create-property-issues.sh` exit 0
    - `grep -q '\${url##\*/}' scripts/create-property-issues.sh` exits 0
    - `./scripts/create-property-issues.sh --help` exits 0 and names `--execute`,
      `--labels-only`, `--repo`
    - `./scripts/create-property-issues.sh --execute` (no IDs) exits non-zero and its message
      names D-09
    - `./scripts/create-property-issues.sh --repo 'not a slug'` exits non-zero
  </acceptance_criteria>
  <done>The script exists, is dry-run by default, refuses a bulk `--execute`, validates its repo slug, creates labels with `--force`, files with `--body-file`, detects duplicates by exact title, derives the issue number from the returned URL, and contains no `eval`, no `set -x` and no mutating `gh` subcommand beyond `label create` and `issue create`.</done>
</task>

<task type="auto">
  <name>Task 2: Seed property-issues.tsv with the nine Phase 1 deliverables and dry-run the whole table</name>
  <files>scripts/property-issues.tsv</files>
  <read_first>
    - `scripts/create-property-issues.sh` as left by task 1 — the column order it reads
    - `.planning/REQUIREMENTS.md` lines 13–17 — the INFRA-01/02/03 bullets the
      `REQUIREMENTS` anchor resolves against, as reworded by plan 01-02
    - `.planning/ROADMAP.md` lines 163–174 — the CAT-01 note
    - `docs/spqr-properties.md` lines 431–447 (§11) — the D1–D5 rows the DEV issues describe
    - `docs/ISSUE_TEMPLATE.md` lines 14–40 and 69–92 — the legal values for `{TYPE}`,
      `{SOURCES}`, `{DIFFICULTY}`, `{STATUS}`
    - `.planning/phases/01-gates-issues-and-deviation-decisions/01-RESEARCH.md` lines
      798–813 — the cross-linking block and the note that the #510–#533 mapping is hand-written
    - `.planning/phases/01-gates-issues-and-deviation-decisions/01-RESEARCH.md` lines
      1483–1490 — open question Q-5, which becomes the ninth row
  </read_first>
  <action>
    Create `scripts/property-issues.tsv` with a `#` header comment naming the eleven
    tab-separated columns in this order: `id`, `title`, `type`, `sources`, `difficulty`,
    `status`, `type_label`, `status_label`, `dependencies`, `related`, `description_anchor`.

    Seed **nine rows**, all Phase 1 deliverables — D-09 files just in time, so later phases
    append their own rows rather than this table pre-declaring the whole milestone:

    PR A rows, all `type` = `Proof Infrastructure`, `type_label` = `type:proof-infra`,
    `status_label` = `status:open`, `status` = `Open`, `description_anchor` = `REQUIREMENTS`
    (except CAT-01, see below): `INFRA-01`, `INFRA-02`, `INFRA-03`, plus `CAT-01` whose
    `description_anchor` is `REQUIREMENTS` as well. Titles follow the catalog-style shape
    `INFRA-01: issue template names the four real gates` — short, deterministic, and
    unique, because exact-title matching is the idempotency key.

    PR B rows, `type` = `Deviation Flag`, `type_label` = `type:deviation-flag`,
    `status_label` = `status:open`, `sources` includes `spec-code deviation`,
    `description_anchor` = `### PROP-42 KDF_AUTH info and length` for DEV-01,
    `### PROP-30 Epoch and type dispatch` for DEV-02, `### PROP-47 Transition refinement`
    for DEV-03 and `### PROP-43 MAC failure` for DEV-04: `DEV-01` (D1), `DEV-02` (D2),
    `DEV-03` (D3/D4), `DEV-04` (D5).

    Ninth row: the deferred CI question from research Q-5 — id `INFRA-CI-GATE4`, title
    `INFRA: run gate 4 (#print axioms allowlist) in CI`, `type:proof-infra`,
    `status:open`, `description_anchor` = `REQUIREMENTS` with a `dependencies` value of
    `INFRA-03`. Filing this row is how the deferred idea gets tracked instead of lost
    (research Q-5; D-04 already says INFRA deliverables get issues). If no matching
    `- [ ] **INFRA-CI-GATE4**` bullet exists in `.planning/REQUIREMENTS.md`, set this row's
    anchor to the literal `-` and extend the script's resolver with a third case that emits
    a one-paragraph description taken from the `title` plus a pointer to
    `.planning/phases/01-gates-issues-and-deviation-decisions/01-RESEARCH.md` Q-5 — record
    which branch you took in the SUMMARY.

    `related` column, hand-written per D-02 and research lines 798–813: umbrella `#44` for
    `DEV-02` (it covers PROP-30); the per-function `states.rs` issues in the `#510–#533`
    range for `DEV-03` and `DEV-04` — look them up with
    `gh issue list --state all --limit 200 --search 'states.rs in:title' --json number,title`
    and put the matching numbers in, or leave the column empty if none match. Never edit,
    close or relabel any existing issue (D-02).

    Then run the script in dry-run over the whole table and confirm it renders every row.
  </action>
  <verify>
    <automated>test "$(grep -vc '^\s*#\|^\s*$' scripts/property-issues.tsv)" -eq 9 && awk -F'\t' '!/^#/ && NF>0 && NF!=11 {exit 1}' scripts/property-issues.tsv && ./scripts/create-property-issues.sh > /tmp/issues-dryrun.txt && grep -c 'INFRA-01\|INFRA-02\|INFRA-03\|CAT-01\|DEV-01\|DEV-02\|DEV-03\|DEV-04\|INFRA-CI-GATE4' /tmp/issues-dryrun.txt | grep -qv '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -vc '^\s*#\|^\s*$' scripts/property-issues.tsv` returns `9`
    - `awk -F'\t' '!/^#/ && NF>0 && NF!=11 {exit 1}' scripts/property-issues.tsv` exits 0 —
      every row has exactly 11 tab-separated fields
    - `cut -f1 scripts/property-issues.tsv | grep -c 'INFRA-01\|INFRA-02\|INFRA-03\|CAT-01'` returns `4`
    - `cut -f1 scripts/property-issues.tsv | grep -c '^DEV-0[1-4]$'` returns `4`
    - `cut -f7 scripts/property-issues.tsv | grep -v '^#' | sort -u` yields only label names
      present in `scripts/property-issue-labels.tsv` (script the cross-check; paste it in the SUMMARY)
    - `cut -f8 scripts/property-issues.tsv | grep -v '^#' | sort -u` likewise
    - `cut -f2 scripts/property-issues.tsv | grep -v '^#' | sort | uniq -d` prints nothing —
      titles are unique, which the exact-title idempotency key requires
    - `./scripts/create-property-issues.sh > /tmp/issues-dryrun.txt` exits 0 and the output
      contains one listing line per ID with its two labels
    - The open-issue count is unchanged across the dry run:
      `gh issue list --state open --limit 500 --json number --jq length` before equals after
      (record both numbers in the SUMMARY)
    - For one rendered body, `! grep -qE '\{[A-Z_]+\}' <body>` exits 0 and
      `grep -c '^- \[ \] `lake' <body>` returns `>= 3` — the corrected template's gate lines
      survive rendering
    - `git status --porcelain` shows no new file outside `scripts/`
  </acceptance_criteria>
  <done>The TSV holds nine well-formed rows whose labels all exist in the label table and whose titles are unique; a full dry run renders every row with no unsubstituted placeholder and files nothing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository text → shell command | Property text from `docs/spqr-properties.md` and requirement text from `.planning/REQUIREMENTS.md` flows into the issue body. It contains backticks, `$`, `‖`, `⦃ ⦄` and quotes. |
| local script → GitHub org repo | `gh` is authenticated as `astefano` with a keyring token carrying `gist, read:org, repo, workflow`. `repo` can write issues, labels, branches and pull requests on a real organisation repository. |
| TSV data file → `gh` arguments | Title, labels and related-issue numbers are read from a tab-separated file and passed as command arguments. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-02 | Elevation of privilege | `gh` token with `repo` scope driving `--execute` | mitigate | Blast radius of a bug is unintended writes to a live org repo. Four controls, all asserted: dry-run is the default; `--execute` **requires explicit IDs** so a bulk file is impossible; the script's mutating `gh` surface is exactly `label create` and `issue create` (`gh pr create`, `issue close/edit/comment` and `label delete` are grep-asserted absent, enforcing PC-6 and D-02); and plan 01-07 gates the first `--execute` behind the D-01 user review of the dry-run listing. |
| T-1-03 | Elevation of privilege | property text interpolated into a `gh` command | mitigate | `--body-file` with a `mktemp` file; no `eval` (grep-asserted absent); no property text on any command line; every expansion quoted; `set -euo pipefail`. This is the exact failure shape of `issues/create_issues.sh:28-34,79-88`. |
| T-1-15 | Tampering | `--repo` argument pointing at another repository | mitigate | `--repo` validated against `^[A-Za-z0-9._-]+/[A-Za-z0-9._-]+$`; the default is the project slug. |
| T-1-16 | Spoofing | duplicate issues from a re-run under D-09's repeated filing | mitigate | Exact-title detection via `gh issue list --search "\"$title\" in:title"` plus a `--jq` equality match; titles asserted unique in the TSV; `SKIP … already filed as #N` printed so the skip is visible (P-5). Residual: GitHub search indexing lag within seconds of a file (RESEARCH.md A4) — low risk at D-09's cadence. |
| T-1-06 | Information disclosure | secrets in script output | mitigate | No `set -x` (grep-asserted absent); the script never reads or echoes `gh auth token`; the token stays in the keyring. |
| T-1-14 | Elevation of privilege | issue bodies written to a predictable temp path | mitigate | `mktemp -d` (mode 0700) with `trap … EXIT` cleanup (ASVS V12). |
| T-1-SC | Tampering | npm/pip/cargo installs | n/a | No packages installed. RESEARCH.md § Package Legitimacy Audit: not applicable to this phase. |
</threat_model>

<verification>
1. `bash -n scripts/create-property-issues.sh` → exit 0
2. `./scripts/create-property-issues.sh` → exit 0, prints nine listing lines, files nothing
3. `gh issue list --state open --limit 500 --json number --jq length` → unchanged across the dry run
4. `grep -qE '\beval\b|gh pr create|set -x' scripts/create-property-issues.sh` → exit 1
5. `git status --porcelain -- src SrcTranslated .github` → empty
</verification>

<success_criteria>
- `scripts/create-property-issues.sh` is dry-run by default, refuses a bulk `--execute`, and
  its only mutating `gh` calls are `label create --force` and `issue create --body-file` (INFRA-02)
- `scripts/property-issue-labels.tsv` lists exactly the template's 15 labels
- `scripts/property-issues.tsv` holds nine 11-column rows with unique titles and valid labels
- A dry run over the whole table renders every body with zero unsubstituted placeholders and
  creates no GitHub state
- Zero hunks under `src/`, `SrcTranslated/`, `.github/` or any `*.lean` file
</success_criteria>

<output>
Create `.planning/phases/01-gates-issues-and-deviation-decisions/01-04-SUMMARY.md` when done.
Record: the full dry-run listing, the before/after open-issue count, the label cross-check
output, and which resolver branch the `INFRA-CI-GATE4` row took.
</output>
