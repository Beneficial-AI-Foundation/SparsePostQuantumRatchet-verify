---
phase: 01-gates-issues-and-deviation-decisions
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [INFRA-01, INFRA-02]
allowed_sorries: 0
pr_boundary: A
lean_files_touched: 0
src_touched: false
srctranslated_touched: false

must_haves:
  truths:
    - "D-09: INFRA-02 and Phase 1 criterion 3 are reworded for just-in-time, per-PR issue filing"
    - "Phase 1 success criterion 1 states a check that can actually return empty, and says why .planning/ is excluded"
    - "Phase 1 success criterion 3 asks for a demonstrated issue-filing script, not a per-property issue count"
    - "INFRA-01 and INFRA-02 in REQUIREMENTS.md describe what this phase actually delivers"
    - "gsd-sdk can still parse the edited ROADMAP phase"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "INFRA-01 reworded away from naming the token; INFRA-02 reworded per D-09"
      contains: "- [ ] **INFRA-02**"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 1 criteria 1 and 3 reworded; Plans line stating two PR boundaries"
      contains: "':!.planning'"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: "gsd-sdk query roadmap.get-phase 1"
      via: "the numbered success-criteria list indentation the parser depends on"
      pattern: "^  [0-9]\\. "
    - from: ".planning/REQUIREMENTS.md"
      to: "gsd-tools requirements mark-complete"
      via: "the `- [ ] **REQ-ID**:` bullet shape"
      pattern: "^- \\[ \\] \\*\\*INFRA-0[12]\\*\\*"
---

<objective>
Apply the two planning-document rewordings this phase is required to make: D-09's
INFRA-02 and criterion-3 rewording, and the user's Q-1 rescoping of criterion 1 and
INFRA-01.

Purpose: rubric item 9 (roadmap and catalog coherence) fails a plan whose endpoint does
not match the ROADMAP's stated criteria. Criterion 1 as written today demands an unscoped
repo grep return empty, which research B-3 proves impossible while `.planning/` records
the decision to remove the token — including the criterion itself. Criterion 3 demands one
open issue per v1 property, which D-09's just-in-time filing deliberately does not produce.
Output: edited `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md`, still parseable by
the GSD tooling.
</objective>

<phase_type>
Documentation-and-tooling phase; zero `*.lean` files, zero `src/` and `SrcTranslated/`
hunks, `allowed_sorries: 0`. See plan 01-01 `<phase_type>` for the full statement.
</phase_type>

<token_hygiene>
**PHANTOM-TOKEN** is the token at `docs/ISSUE_TEMPLATE.md:98`. Matched by the ERE
`check[_]no[_]sorry`. Do not write it literally into any artifact. New criterion and
requirement text must use the ERE form, not the token.
</token_hygiene>

<blocker_note>
**Research B-2 (verified this session): there is no `gsd-sdk` handler that edits
requirement or success-criterion text.** `gsd-tools requirements` exposes only
`mark-complete`; `gsd-tools roadmap` exposes only `analyze, get-phase,
update-plan-progress, annotate-dependencies`. D-09's clause "REQUIREMENTS.md and ROADMAP.md
changes go through `gsd-sdk` handlers, not direct edits" is **not executable as written**.

Both edits below are therefore **manual `Edit` operations**, with
`gsd-sdk query roadmap.get-phase 1` used as the post-edit parse check. This deviation from
D-09's letter is deliberate, is the only executable route, and must be stated in the
SUMMARY so the post-execution eval does not read it as drift.
</blocker_note>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-gates-issues-and-deviation-decisions/01-CONTEXT.md
@.planning/phases/01-gates-issues-and-deviation-decisions/01-RESEARCH.md
@.planning/phases/01-gates-issues-and-deviation-decisions/01-VALIDATION.md

Locked decisions implemented here: **D-09** (INFRA-02 rewording, criterion-3 rewording,
roadmap references property IDs not issue numbers), **D-15** (two PR boundaries, so the
Plans line says two), user ruling **Q-1** (criterion 1 scoped to `-- ':!.planning'` with a
stated exclusion clause; INFRA-01 stops naming the token; `.planning/PROJECT.md:67` and
`.planning/STATE.md:66` are left alone).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reword INFRA-01 and INFRA-02 in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 1–25 — the Infrastructure block; INFRA-01 is line 15,
      INFRA-02 line 16, INFRA-03 line 17
    - `.planning/phases/01-gates-issues-and-deviation-decisions/01-CONTEXT.md` § D-09 — the
      verbatim INFRA-02 replacement text
    - `.planning/phases/01-gates-issues-and-deviation-decisions/01-RESEARCH.md` lines 826–854
      (PHANTOM-TOKEN inventory; entry 6 is `.planning/REQUIREMENTS.md:15`, the requirement
      that names the token it mandates removing)
  </read_first>
  <action>
    Edit `.planning/REQUIREMENTS.md`, preserving the exact bullet shape
    `- [ ] **INFRA-0N**: …` on one line each (the `gsd-tools requirements mark-complete`
    handler parses that shape — see `get-shit-done/bin/lib/milestone.cjs`).

    INFRA-01 (line 15): keep the leading clause naming `docs/ISSUE_TEMPLATE.md` and the four
    gate commands verbatim, and replace the trailing clause that names the token with
    `instead of a gate that does not exist in the repository` (the user's Q-1 wording).

    INFRA-02 (line 16): replace the whole description with D-09's verbatim text: `A
    repeatable script files one issue per property from the corrected template; each PR's
    issue is created at its PR boundary, before the user opens the PR.`

    Do not touch INFRA-03 or any requirement outside the INFRA block. Do not tick any
    checkbox — completion is recorded by the phase's verification step, not here.
  </action>
  <verify>
    <automated>grep -nE '^- \[ \] \*\*INFRA-0[123]\*\*: ' .planning/REQUIREMENTS.md | wc -l | grep -qx 3 && ! grep -nE 'check[_]no[_]sorry' .planning/REQUIREMENTS.md && grep -q 'at its PR boundary' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^- \[ \] \*\*INFRA-0[123]\*\*: ' .planning/REQUIREMENTS.md` returns `3`
    - `grep -nE 'check[_]no[_]sorry' .planning/REQUIREMENTS.md` exits 1 with no output
    - `grep -q 'a gate that does not exist in the repository' .planning/REQUIREMENTS.md` exits 0
    - `grep -q 'at its PR boundary, before the user opens the PR' .planning/REQUIREMENTS.md` exits 0
    - `grep -c '^- \[ \] \*\*' .planning/REQUIREMENTS.md` is unchanged from before the edit
      (record both numbers in the SUMMARY) — no requirement was added or lost
    - `git diff --name-only` for this task lists only `.planning/REQUIREMENTS.md`
  </acceptance_criteria>
  <done>INFRA-01 no longer names the token, INFRA-02 states D-09's just-in-time filing, the bullet shape the SDK parses is intact, and the requirement count is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Rescope Phase 1 criterion 1, reword criterion 3, and state two PR boundaries in ROADMAP.md</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - `.planning/ROADMAP.md` lines 51–61 — the Phase 1 detail block; criterion 1 is line 56,
      criterion 3 line 58, the `**Plans**:` line is 61
    - `.planning/ROADMAP.md` lines 145–160 — the Progress table, whose Phase 1 row carries
      the plan count
    - `.planning/phases/01-gates-issues-and-deviation-decisions/01-RESEARCH.md` lines
      1441–1470 (Open Question Q-1, with the proposed criterion-1 wording)
    - `.planning/phases/01-gates-issues-and-deviation-decisions/01-CONTEXT.md` § D-09 and
      § D-15 — the criterion-3 wording and the two-PR-boundary grouping
  </read_first>
  <action>
    Three edits inside `.planning/ROADMAP.md`. Preserve the two-space-indented numbered list
    shape `  1. ` … `  5. ` exactly — `gsd-sdk query roadmap.get-phase 1` parses it.

    Criterion 1 (line 56): keep the first clause (`docs/ISSUE_TEMPLATE.md` names the four
    real gates … plus a catalog-and-index update line) verbatim, and replace the trailing
    grep clause with: `and ` then, backticked, `git grep -nE 'check[_]no[_]sorry' --
    ':!.planning'` ` returns nothing, so no operative document (template, rubric, CLAUDE.md,
    scripts) cites a gate that does not exist. ` then the exclusion clause `.planning/ is
    excluded because it is the append-only record of the decision to remove the token.`
    Use the ERE form, not the token itself.

    Criterion 3 (line 58): replace the whole criterion with D-09's wording: `The issue script
    files a correctly labelled issue from the corrected template, demonstrated on the Phase 1
    issues; later phases reference property IDs, not issue numbers.`

    `**Plans**:` line (line 61): if it still reads
    `TBD (expect one per requirement, 8 PR boundaries)`, replace it with
    `9 plans in 6 dependency waves across 2 PR boundaries (PR A: infra; PR B: deviations)`
    per D-15. The review of 2026-09-14 already applied this line and resynced the wave
    listing beneath the block to the `depends_on` DAG (1/1/1, 2, 3/3, 4, 5, 6) — so if the
    line already says that, **leave it and the checklist beneath it alone**. Do not renumber
    the wave headings; they are derived from plan frontmatter, not chosen here. Update the Phase 1 row of the Progress table
    (line ~152) from `0/TBD` to `0/9`.

    Do not change criteria 2, 4 or 5. Do not change any other phase's block. Do not edit
    `.planning/PROJECT.md` or `.planning/STATE.md` — the user's Q-1 ruling leaves both alone.
  </action>
  <verify>
    <automated>gsd-sdk query roadmap.get-phase 1 >/tmp/gsd-phase1.json && grep -q "':!.planning'" .planning/ROADMAP.md && grep -q 'demonstrated on the Phase 1 issues' .planning/ROADMAP.md && ! grep -nE 'check[_]no[_]sorry' .planning/ROADMAP.md && grep -cE '^  [1-5]\. ' .planning/ROADMAP.md | grep -qv '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `gsd-sdk query roadmap.get-phase 1` exits 0 and its JSON still reports five success
      criteria for Phase 1 (paste the criteria array into the SUMMARY)
    - `grep -q "git grep -nE 'check\[_\]no\[_\]sorry' -- ':!.planning'" .planning/ROADMAP.md` exits 0
    - `grep -nE 'check[_]no[_]sorry' .planning/ROADMAP.md` exits 1 with no output
    - `grep -q 'append-only record' .planning/ROADMAP.md` exits 0 — the exclusion is justified
      in the criterion, not silently applied
    - `grep -q 'demonstrated on the Phase 1 issues' .planning/ROADMAP.md` exits 0
    - `grep -q '2 PR boundaries' .planning/ROADMAP.md` exits 0
    - `grep -c '^### Phase ' .planning/ROADMAP.md` returns `8` — no phase block was damaged
    - `git diff --name-only` for this task lists only `.planning/ROADMAP.md`
  </acceptance_criteria>
  <done>Criterion 1 states a runnable, satisfiable check with its exclusion justified; criterion 3 matches D-09's just-in-time filing; the Plans line and Progress row say 9 plans / 6 waves / 2 PR boundaries; `roadmap.get-phase 1` still parses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning documents → GSD tooling | `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md` are parsed by `gsd-sdk`; a formatting break silently degrades every later phase operation. |
| success criteria → verification | A criterion nobody can satisfy makes phase verification either permanently red or quietly waived. |

No network, no credentials, no shell interpolation, no package install in this plan.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-10 | Repudiation | ROADMAP success criterion 1 | mitigate | Rescoping a criterion so the phase can pass it is exactly how a verification gate becomes theatre. Mitigation: the exclusion is written **into the criterion text** with its reason ("append-only record of the decision to remove the token"), and the scope reduction is confined to `.planning/`; every operative document is still covered. The narrowed check is stricter in form (`git grep` on tracked files) than the original `grep -rn`. |
| T-1-11 | Tampering | `gsd-sdk` ROADMAP/REQUIREMENTS parsers | mitigate | Manual edits can break the `  N. ` indentation and the `- [ ] **REQ**:` bullet shape. Mitigation: `gsd-sdk query roadmap.get-phase 1` is run as an `<automated>` verify, and the requirement/phase counts are asserted unchanged. |
| T-1-09 | Repudiation | phase artifacts re-introducing the token | mitigate | New criterion text uses the ERE `check[_]no[_]sorry`, never the token. |
| T-1-SC | Tampering | npm/pip/cargo installs | n/a | No packages installed. RESEARCH.md § Package Legitimacy Audit: not applicable to this phase. |
</threat_model>

<verification>
1. `gsd-sdk query roadmap.get-phase 1` → exit 0, five criteria returned
2. `grep -cE '^- \[ \] \*\*' .planning/REQUIREMENTS.md` → unchanged from the pre-edit count
3. `grep -c '^### Phase ' .planning/ROADMAP.md` → `8`
4. `git grep -nE 'check[_]no[_]sorry' -- ':!.planning'` → exit 1 (with plan 01-01 applied)
5. `git status --porcelain -- src SrcTranslated` → empty
</verification>

<success_criteria>
- INFRA-01 no longer names a token; INFRA-02 carries D-09's wording (INFRA-01, INFRA-02)
- Phase 1 criterion 1 is scoped, justified and runnable; criterion 3 matches D-09
- The ROADMAP says 9 plans in 6 dependency waves across 2 PR boundaries
- `gsd-sdk query roadmap.get-phase 1` still parses the phase
- Zero hunks under `src/`, `SrcTranslated/` or any `*.lean` file
</success_criteria>

<output>
Create `.planning/phases/01-gates-issues-and-deviation-decisions/01-02-SUMMARY.md` when done.
Record: the `roadmap.get-phase 1` criteria array after the edit, the before/after requirement
counts, and an explicit note that D-09's "go through `gsd-sdk` handlers" clause was
discharged as manual edits plus a parser check because research B-2 proved no such handler
exists.
</output>
