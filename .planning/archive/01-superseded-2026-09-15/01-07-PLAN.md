---
phase: 01-gates-issues-and-deviation-decisions
plan: 07
type: execute
wave: 4
depends_on: ["01-01", "01-02", "01-03", "01-04", "01-05", "01-06"]
files_modified: []
autonomous: false
requirements: [INFRA-01, INFRA-02, INFRA-03, CAT-01]
allowed_sorries: 0
pr_boundary: A
pr_boundary_terminal: true
lean_files_touched: 0
src_touched: false
srctranslated_touched: false

must_haves:
  truths:
    - "D-01: GSD files the issues itself with gh issue create, after the user reviews the dry-run title and label listing"
    - "D-03: the type:* and status:* labels are created once by an idempotent step"
    - "D-04: every PR deliverable gets an issue, including the INFRA and CAT requirements"
    - "D-15: PR A carries the template fix, CAT-01 line, gate script, issue script and issues/ retirement"
    - "D-16: PR A comes first; GSD stops at the boundary and the user opens the PR"
    - "The user saw the title-and-label listing before any issue was filed"
    - "The 15 template labels exist in the repository"
    - "Each PR A deliverable has exactly one open issue, filed from the corrected template"
    - "Re-running the filing step files nothing"
    - "GSD stopped at the PR boundary without creating a pull request, and reported the issue numbers and the base-branch question"
    - "The SUMMARY carries a literal `PR B branch:` line recording the user's answer; a bare \"skip\" did not waive it"
  artifacts:
    - path: ".planning/phases/01-gates-issues-and-deviation-decisions/01-07-SUMMARY.md"
      provides: "The PR A boundary report: deliverables, issue numbers, Closes lines, the PR A implementation tip SHA, and the answered base-branch / PR B branch question"
      contains: "Closes #"
    - path: ".planning/phases/01-gates-issues-and-deviation-decisions/01-07-SUMMARY.md"
      provides: "The recorded PR B branch answer, which is plan 01-08's precondition"
      contains: "PR B branch:"
  key_links:
    - from: "scripts/create-property-issues.sh --execute"
      to: "the PR A issue numbers"
      via: "the FILED <ID> arrow #<num> summary block the script prints"
      pattern: "FILED "
    - from: "the boundary report"
      to: "the user opening PR A"
      via: "Closes #N lines the user pastes into the PR body"
      pattern: "Closes #"
---

<objective>
Close the PR A boundary: show the user the dry-run listing, create the 15 labels, file one
issue per PR A deliverable, prove the filing is idempotent, and then **stop and report**.

Purpose: D-09 files issues just in time, at the PR boundary, so the user can open the PR
with `Closes #N`. D-01 requires the user to review the listing first. PC-6 and the project
constraint in `CLAUDE.md` are absolute: **GSD never creates a pull request.**
Output: GitHub labels and issues, plus a boundary report. No changes to source, docs,
scripts or catalog files — but the boundary report itself,
`.planning/phases/01-gates-issues-and-deviation-decisions/01-07-SUMMARY.md`, **is** a
tracked file (`commit_docs: true`), so this plan does add one commit and one file to PR A's
diff. "No repository file changes" means no change outside `.planning/`.
</objective>

<pr_boundary_contract>
**This plan ends the PR A work. It must not run `gh pr create`, `gh pr ready`, or any other
pull-request command, and must not push a branch.** When the work is verified, GSD stops
and reports; the user creates the PR. This is a project constraint recorded in
`CLAUDE.md` Constraints (Process bullet), `.planning/PROJECT.md`, the ROADMAP process
paragraph, and RESEARCH.md PC-6. A `gh pr` invocation anywhere in this plan's execution is
a BLOCKER for both the plan reviewer (rubric item 8) and `spqr-eval`.

Issues are not PRs. D-01 explicitly permits GSD to run `gh issue create`.
</pr_boundary_contract>

<phase_type>
Documentation-and-tooling phase; this plan changes **no repository file at all** — its
output is GitHub state plus a SUMMARY. Zero `*.lean`, zero `src/`, zero `SrcTranslated/`,
`allowed_sorries: 0`.
</phase_type>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-gates-issues-and-deviation-decisions/01-CONTEXT.md
@.planning/phases/01-gates-issues-and-deviation-decisions/01-RESEARCH.md
@.planning/phases/01-gates-issues-and-deviation-decisions/01-VALIDATION.md
@CLAUDE.md

Locked decisions closed here: **D-01** (user reviews the dry-run listing first),
**D-03** (the template's 15 labels, created once idempotently), **D-04** (INFRA and CAT
deliverables get issues too), **D-09** (just-in-time filing, then stop and report the
numbers), **D-15** (PR A's scope), **D-16** (PR A before PR B).

Depends on every other PR A plan: 01-01 (corrected template, the body source), 01-02
(reworded requirement text, which the `REQUIREMENTS` anchor renders into the bodies),
01-03 and 01-06 (the gate script exists **and has been proved to fail when broken** —
INFRA-03 cannot honestly be declared done before plan 01-06's negative controls have run),
01-04 (the script and TSVs), 01-05 (the legacy tooling is retired).
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Show the user the dry-run listing before anything is filed</name>
  <files>(no repository file — dry-run output only)</files>
  <read_first>
    - `scripts/property-issues.tsv` — the nine rows, five of which this plan files
    - `scripts/property-issue-labels.tsv` — the 15 labels that will be created
    - `.planning/phases/01-gates-issues-and-deviation-decisions/01-CONTEXT.md` D-01
    - `.planning/phases/01-gates-issues-and-deviation-decisions/01-VALIDATION.md`
      Manual-Only Verifications — the row "Issue titles and labels are the ones the user
      wants filed"
  </read_first>
  <what-built>
    Plan 01-04 produced `scripts/create-property-issues.sh` plus its two TSV data files.
    Nothing has been filed: the script is dry-run by default and refuses `--execute` without
    explicit IDs.
  </what-built>
  <how-to-verify>
    1. Run `./scripts/create-property-issues.sh` with no arguments and show the user the
       full listing: one line per row with ID, title and the two labels.
    2. Run `./scripts/create-property-issues.sh --labels-only` (no `--execute`) and show the
       15 intended `gh label create` lines with their colours.
    3. Show the `INFRA-01` body in full with
       `./scripts/create-property-issues.sh --show-body INFRA-01`, so the corrected
       template's four gate lines and the `### Related` block are visible in context. Use
       `--show-body`, not the dry-run's body path: bodies are written under a `mktemp -d`
       that the script's `EXIT` trap removes, so a printed path is dead by the time you read
       it (plan 01-04 defines `--show-body` for exactly this step).
    4. State the current open-issue count from
       `gh issue list --state open --limit 500 --json number --jq length`, and the repo slug
       `Beneficial-AI-Foundation/SparsePostQuantumRatchet-verify` the script will write to.
    5. State explicitly which five IDs the next task will file: `INFRA-01`, `INFRA-02`,
       `INFRA-03`, `CAT-01` (the four PR A deliverables) and `INFRA-CI-GATE4` (the deferred
       CI question from research Q-5, filed so it is tracked, and **not** closed by PR A).
    6. Note that none of the 15 labels exists in the repository today — the repo has six
       labels, none of them `type:*` or `status:*` — so the label step creates all 15.
  </how-to-verify>
  <resume-signal>Type "approved" to file, or list the titles/labels/IDs to change first</resume-signal>
  <action>
    Record `gh issue list --state open --limit 500 --json number --jq length` as the
    baseline count. Run the read-only commands named below, show the user their output, and
    STOP. Run no `gh label create` and no `gh issue create` before the resume signal (D-01).
  </action>
  <done>The user has seen the full dry-run listing, the label plan and one rendered body, and has given a resume signal; the open-issue count is unchanged.</done>
  <acceptance_criteria>
    - The full dry-run listing was shown and no `gh issue create` or `gh label create` ran
      before the resume signal
    - `gh issue list --state open --limit 500 --json number --jq length` is identical before
      and after this task (both numbers recorded in the SUMMARY)
    - The rendered `INFRA-01` body shown to the user contains no unsubstituted placeholder
      and at least three lines matching `^- \[ \] .lake`
    - The user's resume signal is recorded verbatim in the SUMMARY
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create the labels, file the five PR A issues, and prove idempotency</name>
  <files>(no repository file — GitHub labels and issues only; scripts/property-issues.tsv only if the user asks for a change)</files>
  <read_first>
    - The resume signal and any change list from task 1
    - `scripts/create-property-issues.sh` — the `--labels-only`, `--execute` and
      ID-selection behaviour
    - `.planning/phases/01-gates-issues-and-deviation-decisions/01-RESEARCH.md` lines
      635–722 — the label inventory, the idempotent `gh` invocations and the exact-title
      duplicate detection
    - `.planning/phases/01-gates-issues-and-deviation-decisions/01-RESEARCH.md` lines
      1069–1080 and 1093–1100 — pitfalls P-5 (duplicate filing) and P-8 (`gh issue create`
      prints a URL, not a number)
    - `.planning/phases/01-gates-issues-and-deviation-decisions/01-RESEARCH.md` lines
      1193–1210 — the Runtime State Inventory requirement that filed issue numbers be
      written back into the phase SUMMARY, because no git artifact records them
  </read_first>
  <action>
    Apply any change the user asked for in task 1 to `scripts/property-issues.tsv` or
    `scripts/property-issue-labels.tsv` first, then re-show the affected listing lines.

    Run `./scripts/create-property-issues.sh --labels-only --execute` and capture the
    output. Verify with `gh label list --limit 100 --json name --jq '[.[].name]'` that all
    15 template labels are present. Re-run the label step once and confirm `--force` updates
    rather than errors, so the step is a no-op in effect; capture both runs.

    Then file exactly five issues, by explicit ID, in one invocation:
    `./scripts/create-property-issues.sh --execute INFRA-01 INFRA-02 INFRA-03 CAT-01
    INFRA-CI-GATE4`. Capture every `FILED` line.

    Then run the identical command a second time. Every line must now report a skip against
    the same number, and the open-issue count must have grown by exactly five across both
    runs combined (P-5).

    **This back-to-back re-run is only a valid idempotency test because plan 01-04 detects
    duplicates by full enumeration of `gh issue list`, not by `in:title` search.** RESEARCH
    assumption A4 warns that the search index may not yet contain an issue created seconds
    earlier; A4's "filed minutes to days apart" mitigation does not cover this test, which is
    deliberately immediate. If the script's detection has drifted back to `--search`, do not
    run this step — it would file five duplicates and only then report the failure. Check
    first: `grep -q 'in:title' scripts/create-property-issues.sh` must exit 1.

    Record the five ID-to-number pairs. They exist nowhere in git, so this SUMMARY is the
    only place the next phase can read them.

    Do **not** file `DEV-01` through `DEV-04` here — those are PR B's, filed by plan 01-09
    at the PR B boundary (D-09, D-16). Do not close, retitle or relabel any existing issue,
    including #44 and the `#510`–`#533` range (D-02). Do not run any `gh pr` command.
  </action>
  <verify>
    <automated>gh label list --limit 100 --json name --jq '[.[].name]|length' && ./scripts/create-property-issues.sh --execute INFRA-01 INFRA-02 INFRA-03 CAT-01 INFRA-CI-GATE4</automated>
  </verify>
  <acceptance_criteria>
    - `gh label list --limit 100 --json name --jq '[.[].name]'` contains all 15 names from
      `scripts/property-issue-labels.tsv` (script the cross-check as a loop and paste it in
      the SUMMARY)
    - The second `--labels-only --execute` run exits 0 and creates no new label:
      `gh label list --limit 100 --json name --jq '[.[].name]|length'` is identical before
      and after
    - The first `--execute` run printed five lines beginning `FILED ` and the SUMMARY lists
      all five ID-to-number pairs
    - The second `--execute` run printed five lines beginning `SKIP ` and zero beginning
      `FILED `
    - `gh issue list --state open --limit 500 --json number --jq length` grew by exactly `5`
      from the count recorded in task 1
    - For each filed number N, `gh issue view N --json labels --jq '[.labels[].name]'`
      returns exactly the two labels from that row of `scripts/property-issues.tsv`
    - For one filed number N, `gh issue view N --json body --jq .body` contains at least
      three lines matching `^- \[ \] .lake` and the string `§12` — the corrected template
      reached GitHub
    - `gh issue view 44 --json updatedAt` is unchanged from the value recorded at the start
      of the task (D-02: no existing issue is touched)
    - No `gh pr` command appears anywhere in this task's transcript
  </acceptance_criteria>
  <done>All 15 labels exist, five issues are filed from the corrected template with the right labels, a repeat run files nothing, and the ID-to-number pairs are recorded.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: STOP at the PR A boundary and report — GSD does not open the PR</name>
  <files>(no repository file — boundary report in the SUMMARY)</files>
  <read_first>
    - `01-01-SUMMARY.md` through `01-06-SUMMARY.md` — the deliverables and evidence to report
    - The ID-to-number pairs from task 2
    - `.planning/phases/01-gates-issues-and-deviation-decisions/01-RESEARCH.md` lines
      234–256 (blocker B-4) and lines 1471–1482 (open question Q-2) — the base-branch
      evidence and the question to put to the user
    - `.planning/STATE.md` line 76 — "Local `la/spec-catalog` has diverged from
      `origin/la/spec-catalog`; pushing needs `--force-with-lease`"
    - `.planning/phases/01-gates-issues-and-deviation-decisions/01-RESEARCH.md` lines
      1081–1092 — pitfall P-6, why a catalog-decision PR ahead of the theorems is correct
      rather than a PC-5 violation (state this in the report so PR B is not misread later)
  </read_first>
  <what-was-done>
    Report, with evidence, every PR A deliverable:
    - `docs/ISSUE_TEMPLATE.md` checklist names the four gates, the `Spqr.lean` re-export and
      the catalog-plus-§12 line (plan 01-01) — INFRA-01, CAT-01
    - the phantom gate token is gone from every operative document; the scoped
      `git grep ... -- ':!.planning'` returns nothing (plan 01-01) — INFRA-01
    - `.planning/REQUIREMENTS.md` INFRA-01/INFRA-02 and `.planning/ROADMAP.md` criteria 1
      and 3 reworded, `roadmap.get-phase 1` still parsing (plan 01-02)
    - `scripts/check-gates.sh` with gates 1–4, the `check-lint.sh` shim,
      `scripts/axiom-allowlist.txt`, the `scripts/README.md` gates section and the two
      `.gitignore` entries (plan 01-03) — INFRA-03
    - the gate script run end to end, its `origin/main` verdict agreeing gate-by-gate with
      CI, and the six-row negative-control matrix (plan 01-06) — INFRA-03, ROADMAP criterion 2
    - `scripts/create-property-issues.sh` plus the two TSVs, dry-run by default (plan 01-04)
      — INFRA-02
    - `issues/` retired and its `.gitignore` rule removed (plan 01-05) — INFRA-02
    - the 15 labels and the five filed issues (task 2) — INFRA-02, ROADMAP criterion 3

    Give the user a ready-to-paste `Closes #N` block for `INFRA-01`, `INFRA-02`, `INFRA-03`
    and `CAT-01`, and state that `INFRA-CI-GATE4` is filed for tracking and is **not**
    closed by this PR.

    State the two diff-shape facts a reviewer will otherwise query: the `issues/` deletion
    produces **no hunk** because the directory was untracked (research B-1) — the reviewable
    evidence is the `.gitignore` line removal; and PR A contains no `*.lean` change, so its
    sorry-delta is trivially empty, which is why plan 01-06's negative controls exist.

    Run the final gate for the record and paste its output:
    `./scripts/check-gates.sh spqr.kdf.hkdf_to_slice_spec`, plus
    `git grep -nE 'check[_]no[_]sorry' -- ':!.planning'` (expect exit 1),
    `git status --porcelain -- src SrcTranslated` (expect empty), and — for the whole PR A
    change set — `git diff --stat <base>...HEAD` against the base branch the user chose
    below. **Not a bare `git diff --stat`**: by this point the phase's work is committed, so
    a bare `git diff` compares the working tree to the index and reports nothing, which
    would read as "no changes" for a PR that changes fourteen files. Include the tracked
    `.planning/` artifacts in that range and say so: `.planning/` is committed on this
    project (`commit_docs: true`), so the phase SUMMARYs and this plan set are real PR
    scope, not invisible runtime state.
  </what-was-done>
  <user-action-required>
    **Open PR A yourself.** GSD does not create pull requests in this repository.

    Before you do, answer the open base-branch question. Research blocker B-4 established
    that `origin/main` contains **none** of the files this phase edits: the catalog, the
    template, the rubric, `CLAUDE.md` and all of `.planning/` are new on the unmerged
    `la/spec-catalog` branch. Re-measure the range rather than quoting a figure — B-4's
    original "14 files, 2229 insertions" is stale; as of 2026-09-14
    `git diff --stat origin/main..HEAD` reports **25 files, 7058 insertions, 0 deletions**,
    and it grows with every planning commit. Report the number you measured, with the date.
    The substantive finding is unchanged and is what matters here: `origin/main` has no
    `docs/spqr-properties.md` and no `.planning/` at all, and
    `git diff --name-only origin/main..HEAD -- '*.lean' lakefile.toml lean-toolchain
    lake-manifest.json` is **empty**, so this branch carries no Lean or toolchain change.

    Two branch questions ride along with the base question, and both are yours to settle —
    GSD will not choose a branch or move work between branches on its own:
    - **Is PR A's work on a branch of its own?** `.planning/config.json` sets
      `branching_strategy: none`, so execution has been committing to `la/spec-catalog`
      directly. If PR A and PR B are to be two reviewable pull requests, the PR A commits
      need a tip you can point a PR at, and PR B's commits (plans 01-08 and 01-09) need to
      start somewhere that is not mixed in with them.
    - **Where should PR B branch from?** Plan 01-09 runs `scripts/create-property-issues.sh`
      — which exists only in PR A's work — while editing the post-01-08 catalog. If PR A has
      not merged, that means running PR A's script from PR B's branch. That is workable
      (D-16 permits it) but it must be a choice you made, not an accident of both PRs
      sharing one branch.

    Record PR A's final commit SHA in the SUMMARY before answering, so the boundary between
    the two PRs is pinned to something concrete. Record the **implementation tip** — the SHA
    of the last task commit — and note separately that the tracked `01-07-SUMMARY.md` commit
    lands after it, so the two SHAs are not the same and the PR A range must include both. The question, verbatim from RESEARCH.md Q-2:

    > **Q-2 — What is PR A's base branch?**
    > *What we know:* `main` contains none of the files this phase edits; `la/spec-catalog`
    > is unmerged and is **ahead of** its remote, not diverged
    > (`git rev-list --left-right --count origin/la/spec-catalog...HEAD` → `0	9` on
    > 2026-09-14, i.e. zero commits behind, nine ahead — so a plain push suffices and the
    > `--force-with-lease` note in `.planning/STATE.md:76` is stale; re-measure before
    > acting on either). `branching_strategy` is `none`.
    > *What's unclear:* whether you intend to merge `la/spec-catalog` to `main` first, or
    > stack PR A and PR B on it.
    > *Recommendation:* default to stacking on `la/spec-catalog` (PR A to `la/spec-catalog`,
    > PR B to PR A's branch) and raise it at the first PR-boundary report rather than
    > deciding silently.

    Consequence either way: if PR A is based on `main`, its diff includes the entire catalog
    and all of `.planning/` — thousands of lines of unrelated review surface. Report the
    figure you measured above rather than any number quoted in this plan; every planning
    commit changes it.

    Open the PR as a **draft** (project convention), with a body of one sentence plus the
    `Closes #N` block, following the #537–#541 shape.
  </user-action-required>
  <resume-signal>Tell GSD the PR A number and the base branch you chose, or "skip" to continue to PR B without opening PR A yet — but in **either** case name the branch PR B's commits are to be made on, because plan 01-08 will not start without it</resume-signal>
  <action>
    Run the record commands named below, assemble the boundary report, present it, and
    STOP. Do not run `gh pr create`, `gh pr ready`, `gh pr edit` or `git push`. Wait for the
    user to report the PR number and the base branch they chose, or to say "skip".

    **"skip" defers the pull request, not the branch decision.** `branching_strategy` is
    `none`, so whatever branch is checked out when plan 01-08 runs is where PR B's catalog
    edits land — and if that is PR A's head, the two PRs are one branch and cannot be
    reviewed separately (D-15/D-16 require two). So a bare `"skip"` is **not** a complete
    resume signal: ask again for the PR B branch, and do not hand control to 01-08 until the
    user has named it. Record the answer in the SUMMARY under a literal
    `PR B branch:` line — that line is 01-08's precondition. If the user's answer is "same
    branch, I'll split it later", record that verbatim as an explicit user ruling, so it
    reads as a decision rather than an accident.
  </action>
  <done>The PR A boundary report has been delivered with the issue numbers, the Closes block and the Q-2 base-branch question; no pull request was created; the user's answer is recorded.</done>
  <acceptance_criteria>
    - No `gh pr create`, `gh pr ready`, `gh pr edit` or `git push` was run in this plan
      (state this explicitly in the SUMMARY; the transcript is the evidence)
    - The SUMMARY contains a deliverable-to-requirement table covering INFRA-01, INFRA-02,
      INFRA-03 and CAT-01, each with the plan number and the verification command that
      proved it
    - The SUMMARY contains the five ID-to-number pairs and a ready-to-paste `Closes #N`
      block naming exactly the four PR A deliverable issues
    - The SUMMARY contains the Q-2 base-branch question verbatim and marks it OPEN pending
      the user's answer
    - The SUMMARY records the B-1 no-hunk explanation for the `issues/` deletion and the
      B-4 diff-shape consequence
    - `./scripts/check-gates.sh spqr.kdf.hkdf_to_slice_spec` output is pasted with its exit code
    - `git grep -nE 'check[_]no[_]sorry' -- ':!.planning'` exits 1 with no output
    - `git status --porcelain -- src SrcTranslated` is empty
    - The user's answer (PR number and base branch, or "skip") is recorded verbatim
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local script with a `repo`-scoped token → live org repository | This is the plan where `--execute` actually runs. `gh` is authenticated as `astefano` with `gist, read:org, repo, workflow`; `repo` can create issues, labels **and pull requests**. |
| GSD → the PR boundary | The project constraint forbidding GSD from opening PRs is enforced by discipline, not by the token scope. The token would permit it. |
| existing GitHub issues → this phase | #44 and the `#510`–`#533` range are cross-linked and must not be modified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-02 | Elevation of privilege | `gh` token driving the first real `--execute` | mitigate | Task 1 is a blocking `checkpoint:human-verify` showing the exact titles, labels and rendered body before any write (D-01). Filing is by five explicit IDs in one invocation; the script refuses a bulk `--execute`. The open-issue delta is asserted to be exactly 5. |
| T-1-22 | Elevation of privilege | the token also permits `gh pr create` | mitigate | `<pr_boundary_contract>` makes any `gh pr` invocation a BLOCKER; task 3's acceptance criteria require an explicit statement that no `gh pr` or `git push` ran; the terminal task is a `checkpoint:human-action` whose whole content is handing the PR to the user. |
| T-1-18 | Tampering | modifying existing issues | mitigate | `gh issue view 44 --json updatedAt` is compared before and after; the script contains no `issue close/edit/comment` (asserted in plan 01-04); D-02 permits cross-linking only. |
| T-1-16 | Spoofing | duplicate issues | mitigate | The repeat `--execute` run must print five skips and zero files; the open-issue count is asserted to have grown by exactly five in total (P-5). |
| T-1-23 | Repudiation | filed issue numbers existing only on GitHub | mitigate | Nothing in git records them, so the SUMMARY must carry the five ID-to-number pairs and the `Closes #N` block (RESEARCH.md Runtime State Inventory). |
| T-1-06 | Information disclosure | token or secret in transcript | mitigate | No `set -x`; the script never reads `gh auth token`; only label and issue names are echoed. |
| T-1-SC | Tampering | npm/pip/cargo installs | n/a | No packages installed. RESEARCH.md Package Legitimacy Audit: not applicable to this phase. |
</threat_model>

<verification>
1. `gh label list --limit 100 --json name --jq '[.[].name]'` contains all 15 template labels
2. Re-running `--execute` with the same five IDs prints five skips and zero files
3. `gh issue list --state open --limit 500 --json number --jq length` grew by exactly 5
4. `./scripts/check-gates.sh spqr.kdf.hkdf_to_slice_spec` → exit 0, four gate verdicts
5. `git grep -nE 'check[_]no[_]sorry' -- ':!.planning'` → exit 1
6. `git status --porcelain -- src SrcTranslated` → empty
7. No `gh pr` command and no `git push` anywhere in the transcript
</verification>

<success_criteria>
- INFRA-01, INFRA-02, INFRA-03 and CAT-01 each have a filed, correctly labelled issue
  created from the corrected template (ROADMAP criterion 3 as reworded by D-09)
- The 15 template labels exist and the label step is idempotent (D-03)
- The user reviewed the dry-run listing before anything was filed (D-01)
- GSD stopped at the PR boundary, created no pull request, and reported the issue numbers,
  the `Closes #N` block and the Q-2 base-branch question (PC-6, D-09)
- Zero repository file changes in this plan
</success_criteria>

<output>
Create `.planning/phases/01-gates-issues-and-deviation-decisions/01-07-SUMMARY.md` when done.
It is the **PR A boundary report**. It must contain: the deliverable-to-requirement table,
the five ID-to-number pairs, the ready-to-paste `Closes #N` block, the Q-2 question verbatim
marked OPEN, the B-1 no-hunk and B-4 diff-shape notes, the final gate output, and an
explicit statement that no pull request was created.
</output>
