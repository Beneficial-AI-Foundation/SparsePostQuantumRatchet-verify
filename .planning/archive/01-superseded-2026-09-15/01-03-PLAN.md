---
phase: 01-gates-issues-and-deviation-decisions
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-gates.sh
  - scripts/check-lint.sh
  - scripts/axiom-allowlist.txt
  - scripts/README.md
  - .gitignore
autonomous: true
requirements: [INFRA-03]
allowed_sorries: 0
pr_boundary: A
lean_files_touched: 0
src_touched: false
srctranslated_touched: false

must_haves:
  truths:
    - "D-06: scripts/check-lint.sh is extended into a four-gate runner"
    - "D-07: the sorry delta uses a cached origin/main worktree baseline under a gitignored path"
    - "D-08: gate 4 writes a scratch #print axioms file for names passed as arguments and greps against the allowlist, leaving Audit.lean unchanged"
    - "One command runs all four repository gates and reports a per-gate verdict"
    - "Gates 1 and 2 issue byte-for-byte the same commands as .github/workflows/lean.yml"
    - "The sorry delta is computed by scripts/sorry-diff.py against a SHA-keyed origin/main baseline, never against the stale repo-root manifest"
    - "A misspelled #print axioms target fails the gate instead of passing vacuously"
    - "A reader of scripts/README.md can run, configure and tear down the gate check"
  artifacts:
    - path: "scripts/check-gates.sh"
      provides: "The four-gate local runner with a fail accumulator"
      contains: "sorry-diff.py"
      min_lines: 90
    - path: "scripts/check-lint.sh"
      provides: "Backward-compatibility shim execing check-gates.sh"
      max_lines: 6
    - path: "scripts/axiom-allowlist.txt"
      provides: "The 20 names gate 4 accepts: 3 builtins plus 17 documented stubs"
      contains: "spqr.kdf.hkdf_to_slice_spec"
    - path: "scripts/README.md"
      provides: "## Verification gates section: usage, flags, env vars, caveats, teardown"
      contains: "SPQR_BASELINE_REF"
    - path: ".gitignore"
      provides: "Ignore entries for the gate cache and the sorry-diff stray comment file"
      contains: ".gate-cache/"
  key_links:
    - from: "scripts/check-gates.sh"
      to: "scripts/sorry-diff.py"
      via: "SORRY_FAIL_ON_NEW=true python3 invocation with positional base then head"
      pattern: "sorry-diff\\.py"
    - from: "scripts/check-gates.sh"
      to: "scripts/axiom-allowlist.txt"
      via: "gate 4 reads the allowlist from the sidecar file"
      pattern: "axiom-allowlist\\.txt"
    - from: "scripts/check-lint.sh"
      to: "scripts/check-gates.sh"
      via: "exec shim"
      pattern: "exec .*check-gates\\.sh"
---

<objective>
Author the repeatable local gate runner: `scripts/check-gates.sh`, its allowlist sidecar,
a compatibility shim at the old `scripts/check-lint.sh` path, the two `.gitignore` entries
it needs, and its documentation in `scripts/README.md`.

Purpose: INFRA-03 and ROADMAP criterion 2 — a property must be declarable done before the
PR is opened, by one command whose verdict matches CI's. Today only gates 1 and 2 exist
locally and only as a script that exits at the first failure.
Output: four new/edited files under `scripts/` plus `.gitignore`. **This plan writes the
script; it does not run it.** Running, measuring and negative-controlling it is plan 01-06,
which needs a built tree.
</objective>

<phase_type>
Documentation-and-tooling phase; zero `*.lean` files, zero `src/` and `SrcTranslated/`
hunks, `allowed_sorries: 0`. The gate script must never write into `SrcTranslated/` or
change `lean-toolchain`, `lakefile.toml` or `lake-manifest.json` (PC-3: no toolchain bump
inside a property PR).
</phase_type>

<rename_decision>
Claude's-discretion item from CONTEXT.md, decided per RESEARCH.md lines 599–620:
**`git mv scripts/check-lint.sh scripts/check-gates.sh`, and re-create
`scripts/check-lint.sh` as a three-line `exec` shim.** Reasons: the file stops being about
linting; `check-lint.sh` is referenced in `01-CONTEXT.md` and in contributors' habits;
`git mv` preserves history; the shim costs nothing.
</rename_decision>

<do_not_hand_roll>
RESEARCH.md lines 1016–1034 is binding here. In particular:
- **Do not reimplement the manifest delta in bash.** Call
  `python3 scripts/sorry-diff.py <base> <head>`. It already keys on the declaration column
  only, already filters to `Spqr.Specs.*`, already handles a missing baseline, and is the
  same code CI runs. A bash reimplementation drifts from CI, which is what criterion 2 forbids.
- **Do not change `scripts/Audit.lean`** (D-08 forbids it).
- **Do not copy or symlink `.lake` between trees.** Lake's build is path-sensitive; use
  `git worktree` plus `lake exe cache get`.
- **Do not edit `.github/workflows/lean.yml`** (D-08a: local only).
</do_not_hand_roll>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-gates-issues-and-deviation-decisions/01-CONTEXT.md
@.planning/phases/01-gates-issues-and-deviation-decisions/01-RESEARCH.md
@.planning/phases/01-gates-issues-and-deviation-decisions/01-VALIDATION.md
@scripts/check-lint.sh
@scripts/sorry-diff.py
@scripts/README.md
@.github/workflows/lean.yml
@.github/workflows/sorry-delta-comment.yml

Locked decisions implemented here: **D-06** (extend the existing script with gates 3a, 3b
and 4), **D-07** (SHA-keyed `git worktree` baseline at `origin/main`), **D-08**
(`#print axioms` targets as script arguments, scratch Lean file, allowlist grep, no change
to `Audit.lean`), **D-08a** (local only; `lean.yml` untouched).

`shellcheck` is **not installed** (RESEARCH.md § Environment Availability). Do not install
it and do not block on it: `bash -n` plus a careful read is the fallback, and that is what
the acceptance criteria assert.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename to check-gates.sh, add gates 3a and 3b with a SHA-keyed baseline, add the shim and the .gitignore entries</name>
  <files>scripts/check-gates.sh, scripts/check-lint.sh, .gitignore</files>
  <read_first>
    - `scripts/check-lint.sh` in full (20 lines) — gates 1 and 2 as they exist today; they
      must survive the rename byte-for-byte
    - `.github/workflows/lean.yml` lines 20–90 — the `LEAN_ABORT_ON_PANIC` env block, the
      "Build and check for non-sorry warnings" step, the "Lint hand-written code" step
      (note the `|| true`: the linter's own exit code is ignored, only the log grep decides),
      the "Axiom audit and sorry manifest" step, and the `sorry-manifest-main-${sha}` cache key
    - `.github/workflows/sorry-delta-comment.yml` lines 30–45 — how CI invokes `sorry-diff.py`
    - `scripts/sorry-diff.py` in full — positional `base head`, `SORRY_FAIL_ON_NEW`,
      `GITHUB_STEP_SUMMARY`/`GITHUB_OUTPUT`, the stray `.sorry-delta-comment.md` side effect,
      and the "missing base is not an error" behaviour
    - `scripts/Audit.lean` header plus its final `IO.FS.writeFile "sorry-manifest.txt"` —
      the manifest is written **relative to the CWD**, so a worktree run produces the
      worktree's own file
    - `.planning/phases/01-gates-issues-and-deviation-decisions/01-RESEARCH.md` lines
      378–516 (the four gates as CI runs them, `Audit.lean` output, the `sorry-diff.py` CLI,
      the concrete worktree recipe) and lines 1103–1157 (the gate runner skeleton)
    - `.planning/phases/01-gates-issues-and-deviation-decisions/01-RESEARCH.md` lines
      1036–1100 (pitfalls P-2, P-3, P-4)
    - `.gitignore` in full (37 lines) — `sorry-manifest.txt` is already ignored at line 27;
      `issues/` at line 25 is **not** this task's business (plan 01-05 removes it)
    - `lean-toolchain` and `lakefile.toml` — the pinned `leanprover/lean4:v4.31.0` and the
      aeneas `rev` the toolchain-drift check compares
  </read_first>
  <action>
    `git mv scripts/check-lint.sh scripts/check-gates.sh`, then extend the moved file.

    Preserve gates 1 and 2 verbatim — same commands, same `/tmp/lake-build.log` and
    `/tmp/lake-lint.log` paths, same greps — and change only the control flow: replace each
    `exit 1` with `fail=1` so one run reports every failing gate, and add
    `export LEAN_ABORT_ON_PANIC=1` near the top to match `lean.yml`'s workflow-level env.

    **Replacing `exit 1` with `fail=1` is not sufficient on its own.** The file inherits
    `set -euo pipefail`, so a *non-zero `lake build`* — a genuine Lean error, not a warning —
    kills the script at the pipeline itself, before any `fail=1` line is reached, and gates
    2, 3 and 4 never run. That directly contradicts the all-gates report this plan promises.
    Each gate's command must therefore be run so that its failure is captured rather than
    fatal: wrap the pipeline in a form that cannot trip `errexit` (e.g. run it inside
    `if ! <pipeline>; then fail=1; fi`, or `<pipeline> || true` with the exit status
    captured from `PIPESTATUS`), record the observed exit status in the gate's verdict line,
    and treat a non-zero build or audit exit as a FAIL of that gate — distinct from, and
    reported alongside, the log-grep verdict. Do not solve this by dropping `set -e`
    wholesale: the rest of the script relies on it.
    Any drift in the gate 1 or gate 2 commands breaks ROADMAP criterion 2; the acceptance
    criteria diff them against the pre-rename file.

    Add argument parsing for `--no-delta`, `--refresh-baseline`, `--clean`, `--allow-sorry
    <theorem>` (repeatable, consumed by gate 4 in task 2), `-h|--help`, and positional
    theorem names collected into an array. Unknown flags must exit non-zero with the usage
    text. `--clean` runs `lake clean` before gate 1 and must **not** be the default (P-2:
    `lake build` on an already-built tree emits no warnings, so gate 1 can pass without
    recompiling — the same blind spot CI avoids by always building fresh).

    Add gate 3a: `lake env lean scripts/Audit.lean 2>&1 | tee /tmp/lake-audit.log`, then
    assert the manifest was produced **by this invocation**. `test -f sorry-manifest.txt` is
    *not* enough: a stale `sorry-manifest.txt` already sits in the repo root (gitignored,
    dated 2026-08-07, 14139 bytes, 134 lines — verified 2026-09-14), so the bare `test -f`
    passes even when `Audit.lean` did not run at all. Instead: record the file's mtime (or
    delete it) **before** the `lake env lean` call and require afterwards that the file
    exists *and* its mtime is newer than that mark; combine that with the captured non-zero
    exit check above. `Audit.lean` never exits non-zero on findings, so the freshness
    assertion plus its exit status are the whole of gate 3a's evidence.

    Add gate 3b following the worktree recipe at RESEARCH.md lines 465–516:
    `BASELINE_REF` from `${SPQR_BASELINE_REF:-origin/main}`, `WT_DIR` from
    `${SPQR_BASELINE_WORKTREE:-<repo-parent>/spqr-gate-baseline}`, `CACHE_DIR` from
    `${SPQR_GATE_CACHE:-$REPO_ROOT/.gate-cache}`; `git fetch --quiet origin main`;
    `BASE_SHA="$(git rev-parse "$BASELINE_REF")"`; cache file
    `$CACHE_DIR/sorry-manifest-$BASE_SHA.txt`. On a cache miss (or `--refresh-baseline`):
    print a loud multi-line warning that a cold baseline build is starting and is expected
    to take a long time, `git worktree add --detach "$WT_DIR" "$BASE_SHA"` (or
    `git -C "$WT_DIR" checkout --detach "$BASE_SHA"` if it exists), then inside the worktree
    run `lake exe cache get`, `lake build --no-ansi`, `lake env lean scripts/Audit.lean`, and
    copy `$WT_DIR/sorry-manifest.txt` into the cache. Then run
    `SORRY_FAIL_ON_NEW=true python3 scripts/sorry-diff.py "$BASE_MANIFEST" sorry-manifest.txt`
    (positional order is base first, head second) with `|| fail=1`, and `rm -f
    .sorry-delta-comment.md` afterwards. **Resolve the cold-start rule explicitly, because the two
    sentences above would otherwise contradict each other on a cache miss:** a cache miss
    *does* build the baseline (that is the whole point of `--refresh-baseline` being merely
    a way to force a rebuild of a baseline that is already cached). Gate 3b is therefore
    `SKIPPED` in exactly two cases and no others: `--no-delta` was passed, or the baseline
    build itself failed (network down, `lake exe cache get` failure). A skip prints
    `=== Gate 3b: SKIPPED ===` with the reason and does not set `fail` — **but a run in
    which any gate was skipped must not report itself as a full pass.** Print a final line
    naming every skipped gate, and make the script's summary distinguish
    `ALL GATES PASSED` (nothing skipped) from `PASSED WITH SKIPS: 3b, 4` (partial). Only
    the former may be cited as evidence a property is ready; `01-VALIDATION.md` is amended
    in the same spirit (see task 4).

    Three safety requirements in this block, each from a recorded pitfall or threat:
    (a) **never** use the repo-root `sorry-manifest.txt` as the base — it is a stale,
    gitignored build artifact dated 2026-08-07 (P-4); the base always comes from the
    SHA-keyed cache. (b) Before using an existing worktree, (i) assert it is **clean and at the
    expected SHA** — `git -C "$WT_DIR" status --porcelain` empty and
    `git -C "$WT_DIR" rev-parse HEAD` equal to `$BASE_SHA` — and refuse to use it
    otherwise, because `checkout --detach` leaves local modifications in place and a cache
    file named after `$BASE_SHA` would then not actually describe that SHA; and (ii) compare
    `lean-toolchain` and the aeneas `rev` line of `lakefile.toml` between `$REPO_ROOT` and
    `$WT_DIR` and print a loud `WARNING: toolchain drift` block naming both values when they
    differ (P-3) — do not auto-bump anything (PC-3). (c) Guard every path variable with `${VAR:?...}` before any
    destructive operation, and tear down the baseline worktree and cache with
    `git worktree remove` rather than `rm -rf` (threat T-1-13). The one permitted `rm -rf`
    in the whole script is task 2's `trap 'rm -rf "$SCRATCH_DIR"' EXIT` over a `mktemp -d`
    path, which is created by this script and contains nothing else.

    Do **not** set `GITHUB_STEP_SUMMARY` or `GITHUB_OUTPUT`; do **not** use `set -x`.
    End the script with `exit $fail`.

    Then create a new `scripts/check-lint.sh` containing only a shebang, a one-line
    deprecation comment saying it was renamed to `check-gates.sh` because it now runs gates
    1–4, and `exec "$(dirname "$0")/check-gates.sh" "$@"`. `chmod +x` both files.

    Finally add two lines to `.gitignore` under the `# Local / generated` block:
    `.gate-cache/` and `.sorry-delta-comment.md`. Do not touch the `issues/` line at 25 —
    plan 01-05 removes it together with the directory, so that the untracked directory never
    sits un-ignored in `git status`.
  </action>
  <verify>
    <automated>bash -n scripts/check-gates.sh && bash -n scripts/check-lint.sh && test -x scripts/check-gates.sh && grep -q 'sorry-diff.py' scripts/check-gates.sh && grep -q 'SORRY_FAIL_ON_NEW=true' scripts/check-gates.sh && grep -q 'LEAN_ABORT_ON_PANIC=1' scripts/check-gates.sh && grep -q '.gate-cache/' .gitignore && grep -q '.sorry-delta-comment.md' .gitignore && ./scripts/check-gates.sh --help >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/check-gates.sh` exits 0
    - `bash -n scripts/check-lint.sh` exits 0
    - `test -x scripts/check-gates.sh && test -x scripts/check-lint.sh` exits 0
    - `wc -l < scripts/check-lint.sh` returns a number `<= 6`
    - `grep -q 'exec .*check-gates.sh' scripts/check-lint.sh` exits 0
    - `git show HEAD:scripts/check-lint.sh | grep -c 'lake build --no-ansi 2>&1 | tee /tmp/lake-build.log'` and the same grep against `scripts/check-gates.sh` both return `1` — gate 1's command is byte-identical across the rename
    - `grep -c '/tmp/lake-build.log\|/tmp/lake-lint.log' scripts/check-gates.sh` returns a number `>= 4` — the CI log paths are preserved
    - `grep -q 'runLinter Spqr 2>&1 | tee /tmp/lake-lint.log || true' scripts/check-gates.sh` exits 0
    - `grep -q 'lake env lean scripts/Audit.lean' scripts/check-gates.sh` exits 0
    - `grep -q 'SORRY_FAIL_ON_NEW=true python3 scripts/sorry-diff.py' scripts/check-gates.sh` exits 0
    - `grep -q 'rm -f .sorry-delta-comment.md' scripts/check-gates.sh` exits 0
    - `grep -qE 'SPQR_BASELINE_REF|SPQR_BASELINE_WORKTREE|SPQR_GATE_CACHE' scripts/check-gates.sh` exits 0 and all three appear
    - `grep -q 'git worktree remove' scripts/check-gates.sh` exits 0, and the **only**
      `rm -rf` in the script is the `mktemp -d` scratch cleanup from task 2:
      `grep -n 'rm -rf' scripts/check-gates.sh` shows at most one hit and that hit is
      inside `trap 'rm -rf "$SCRATCH_DIR"' EXIT`. No `rm -rf` may take a baseline-worktree,
      cache or repository path (T-1-13): `grep 'rm -rf' scripts/check-gates.sh | grep -qE 'WT_DIR|CACHE_DIR|REPO_ROOT|BASELINE'` exits 1
    - `grep -cE '\$\{[A-Z_]+:\?' scripts/check-gates.sh` returns a number `>= 1`
    - `grep -c 'set -x' scripts/check-gates.sh` returns `0`
    - `grep -q 'lean-toolchain' scripts/check-gates.sh` exits 0 — the drift check exists
    - `./scripts/check-gates.sh --help` exits 0 and its output names `--no-delta`,
      `--refresh-baseline`, `--clean` and `--allow-sorry`
    - `./scripts/check-gates.sh --nonsense-flag` exits non-zero
    - `grep -q '^.gate-cache/$' .gitignore && grep -q '^.sorry-delta-comment.md$' .gitignore` exits 0
    - `grep -c '^issues/$' .gitignore` returns `1` — untouched by this task
    - `git status --porcelain -- .github` is empty — `lean.yml` was not edited (D-08a)
    - `git status --porcelain -- scripts/Audit.lean scripts/sorry-diff.py` is empty (D-08)
  </acceptance_criteria>
  <done>`scripts/check-gates.sh` parses its flags, reproduces gates 1 and 2 byte-for-byte, runs `Audit.lean`, and computes the delta by calling `sorry-diff.py` against a SHA-keyed `origin/main` baseline; `check-lint.sh` is a shim; the two gate-cache paths are gitignored; `lean.yml`, `Audit.lean` and `sorry-diff.py` are unmodified.</done>
</task>

<task type="auto">
  <name>Task 2: Add gate 4 with the allowlist sidecar and a per-target report-line assertion</name>
  <files>scripts/axiom-allowlist.txt, scripts/check-gates.sh</files>
  <read_first>
    - `scripts/check-gates.sh` as left by task 1
    - `.planning/phases/01-gates-issues-and-deviation-decisions/01-RESEARCH.md` lines
      517–598 — the gate-4 recipe, the full allowlist table with a `file:line` source for
      every entry, and the three naming traps
    - `.planning/phases/01-gates-issues-and-deviation-decisions/01-RESEARCH.md` lines
      1038–1046 (pitfall P-1: the gate that passes because the theorem name was misspelled)
    - `SrcTranslated/FunsExternal.lean` — confirm each allowlist entry exists at its cited
      line; note `open spqr` at line 16 (**not** `namespace spqr`), so these axioms are
      root-level `libcrux_ml_kem.*`, `encoding.*`, `incremental_mlkem768.*`
    - `SrcTranslated/Funs.lean` line 26 (`namespace spqr`) and the `def initial_state` /
      `def send` / `def recv` declarations — the real functions, versus the dead bare
      `axiom initial_state` / `send` / `recv` at `FunsExternal.lean:3694,3700,3708`
    - `Spqr/Specs/Kdf/HkdfToSlice.lean` lines 17–29 — `namespace spqr.kdf` at `:18` and the
      single hand-written axiom, declared bare as `axiom hkdf_to_slice_spec` at `:25`, so
      its Lean name is `spqr.kdf.hkdf_to_slice_spec`. The `opaque hkdf_to_slice` it specifies
      is **not** in this file — it is at `SrcTranslated/FunsExternal.lean:3669`, inside
      `namespace spqr.kdf` (`:3661`)
    - `docs/spqr-properties.md` §1 (lines 32–72) — the prose source of truth for the stub list
  </read_first>
  <action>
    Create `scripts/axiom-allowlist.txt`: one fully-qualified Lean name per line, `#`
    comments and blank lines allowed, **exactly 20 names** — the three builtins `propext`,
    `Classical.choice`, `Quot.sound`, plus the 17 stub entries in the RESEARCH.md table at
    lines 526–546. Group them with `#` section comments (`# Lean builtins`,
    `# Hand-written axiom (docs/spqr-properties.md §1)`, `# libcrux KEM stubs`,
    `# libcrux HMAC stubs`, `# encoding / incremental_mlkem768 stubs`), and give each stub
    line a trailing `#` comment with its `SrcTranslated/FunsExternal.lean:<line>` source.

    Honour the three traps, and add a `#` comment in the file for each:
    (1) `FunsExternal.lean` names are **not** `spqr.`-prefixed (the file has `open spqr`,
    not `namespace spqr`) — the sole exception is `spqr.kdf.hkdf_to_slice_spec`;
    (2) `opaque` declarations never appear in `#print axioms` output, so
    `spqr.kdf.hkdf_to_slice` (`SrcTranslated/FunsExternal.lean:3669`) and
    `crypto.HMAC_SHA256` (`Spqr/Crypto/Hkdf.lean:30`, inside `namespace crypto` at `:27` —
    **not** `Spqr.Crypto.Hkdf.HMAC_SHA256`, which is the module path, not the declaration's
    Lean name) are deliberately **absent** from the list;
    (3) the bare root-level `axiom initial_state`, `axiom send`, `axiom recv` in
    `FunsExternal.lean` are dead template leftovers and must **not** be allowlisted — a
    theorem depending on them depends on the wrong constant.
    Before writing each entry, confirm it exists at its cited line with
    `grep -n '<name>' SrcTranslated/FunsExternal.lean`; record any mismatch in the SUMMARY
    rather than silently adjusting the list.

    Then add gate 4 to `scripts/check-gates.sh`. It runs only when at least one positional
    theorem name was given; with none, print `=== Gate 4: SKIPPED (no theorem targets) ===`.
    Create a scratch directory with `mktemp -d` and a `trap 'rm -rf "$SCRATCH_DIR"' EXIT`
    cleanup; write `import Spqr` followed by one `#print axioms <name>` per target; run
    `lake env lean "$SCRATCH_DIR/PrintAxioms.lean" 2>&1 | tee /tmp/lake-axioms.log`.

    The matcher must satisfy P-1 — **validate, do not merely grep for bad names**:
    - count reports matching `'<name>' depends on axioms:` or
      `'<name>' does not depend on any axioms` and require **exactly one report per
      requested target**; a short count is a FAIL naming the missing targets;
    - any `unknown identifier` or `unknown constant` in the log is a FAIL naming the target;
    - **the axiom list is not guaranteed to be one line.** Lean formats it with
      `MessageData.ofList`, which joins entries with `"," ++ Format.line`
      (`Lean/Message.lean:414-417`, pinned toolchain v4.31.0), and `Format.line` is a *soft*
      break: it renders as a space when the group fits the print width and as a **newline**
      when it does not. `#print axioms` builds the list at
      `Lean/Elab/Print.lean:244`. A long closure — and the prost/libcrux-dependent theorems
      have long closures — will therefore wrap. A line-oriented matcher that reads only the
      text on the `depends on axioms:` line silently drops every axiom on a continuation
      line, which is a **false green**, the exact failure gate 4 exists to prevent.
      So: parse each report as a region from `depends on axioms: [` through its matching
      closing `]`, accumulating continuation lines; normalise interior whitespace and
      newlines to single spaces; then split on `,` and trim. If a report's closing `]` is
      not found before the next report header or EOF, that is a FAIL (`malformed
      #print axioms report`), never a pass;
    - fail on any resulting name not present in `scripts/axiom-allowlist.txt` (after
      stripping `#` comments), printing the offending name and the theorem that pulled it in;
    - `sorryAx` is rejected by default and accepted only for targets named by
      `--allow-sorry`, so the two known hand-written sorries
      (`Spqr/Specs/Aeneas/MapCollectBridge.lean:70`, `Spqr/Specs/Lib/DecodeState.lean:54`)
      and the prost-dependent rows stay opt-in until Phase 4's AXIOM-04/AXIOM-05 formalise
      the set.

    **Assumption to settle empirically, not now:** RESEARCH.md A1 and A2 record that the
    two `#print axioms` output shapes and the invisibility of `opaque` declarations are
    training knowledge, not verified against this tree (it is unbuilt). Write the matcher
    against both documented shapes **and against the wrapped multi-line shape above**, and
    add a `#` comment in the script marking the patterns as provisional pending plan 01-06's
    probe. Plan 01-06 runs the probe — including a target with a long, wrapping closure —
    and corrects them. A1 may not be marked verified on the strength of a short,
    single-line report alone.
  </action>
  <verify>
    <automated>bash -n scripts/check-gates.sh && test "$(grep -vc '^\s*#\|^\s*$' scripts/axiom-allowlist.txt)" -eq 20 && grep -q 'spqr.kdf.hkdf_to_slice_spec' scripts/axiom-allowlist.txt && ! grep -qE '^(initial_state|send|recv)$' scripts/axiom-allowlist.txt && grep -q 'axiom-allowlist.txt' scripts/check-gates.sh && grep -q 'unknown identifier' scripts/check-gates.sh && grep -q 'sorryAx' scripts/check-gates.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -vc '^\s*#\|^\s*$' scripts/axiom-allowlist.txt` returns `20`
    - `grep -c '^propext$\|^Classical.choice$\|^Quot.sound$' scripts/axiom-allowlist.txt` returns `3`
    - `grep -q '^spqr.kdf.hkdf_to_slice_spec' scripts/axiom-allowlist.txt` exits 0
    - `grep -qE '^(initial_state|send|recv)\s*$' scripts/axiom-allowlist.txt` exits 1 — trap 3
    - `grep -qE '^spqr\.kdf\.hkdf_to_slice\s*$' scripts/axiom-allowlist.txt` exits 1 — trap 2,
      the `opaque` is not listed
    - `grep -c 'FunsExternal.lean:' scripts/axiom-allowlist.txt` returns a number `>= 15` —
      every stub entry carries its source citation
    - Every allowlist entry other than the three builtins resolves to a real declaration.
      The check must be **namespace-aware and comment-stripping**, because neither holds
      naively: the entries carry trailing `# file:line` annotations, and
      `spqr.kdf.hkdf_to_slice_spec` is spelled in the tree as `namespace spqr.kdf` plus a
      bare `axiom hkdf_to_slice_spec` — a literal `grep -F` on the full name returns
      **exit 1** (verified 2026-09-14). Script the loop as: strip `#` comments and
      surrounding whitespace to get the bare name `N`; then accept `N` if the declaration
      keyword `axiom`/`opaque` is followed by the name `N` **across any intervening
      whitespace, including a newline**, in the file whose enclosing `namespace` (or root
      scope, for `FunsExternal.lean`, which has `open spqr` not `namespace spqr`) prefixes
      to exactly `N`. Concretely: for an `N` with no `spqr.kdf.` prefix, match the whole `N`
      at root scope in `SrcTranslated/FunsExternal.lean`; for
      `spqr.kdf.hkdf_to_slice_spec`, strip the `spqr.kdf.` namespace prefix and match
      `hkdf_to_slice_spec` in `Spqr/Specs/Kdf/HkdfToSlice.lean`.
      Paste the loop and its output into the SUMMARY.
      A namespace-blind literal check is not acceptable: it would fail the one
      hand-written axiom and pass silently on a typo'd root-level name.

      **A single-line predicate is also not acceptable, and this is verified, not
      hypothetical.** Aeneas wraps a long declaration name onto the line after the
      keyword: `SrcTranslated/FunsExternal.lean:3687-3688` is `axiom` alone on one line
      followed by
      `incremental_mlkem768.potentially_fix_state_incorrectly_encoded_by_libcrux_issue_1275`
      indented on the next, and 119 of the file's axioms have this shape. So

      ```bash
      grep -qE '^axiom +incremental_mlkem768\.potentially_fix_state_incorrectly_encoded_by_libcrux_issue_1275\b' SrcTranslated/FunsExternal.lean
      ```

      returns **exit 1** (verified 2026-09-14) on an entry that is mandatory and correct.
      Use a whitespace-insensitive matcher — e.g. `grep -Pzo` / `perl -0777` /
      `python3` over the whole file with `(?m)^(axiom|opaque)\s+NAME\b` — or `grep -A1` on
      a bare `^axiom$` and test the continuation line. Whichever you pick, the loop must
      accept the libcrux-1275 entry, and the SUMMARY must show it accepted **by name**.
      Never delete, shorten or rename an allowlist entry to satisfy the predicate: the
      allowlist is the trusted base, the predicate is the tooling.
    - The source-validation loop accepts
      `incremental_mlkem768.potentially_fix_state_incorrectly_encoded_by_libcrux_issue_1275`
      — the multiline-declaration case — and the SUMMARY shows that entry's accept line
    - `grep -q 'axiom-allowlist.txt' scripts/check-gates.sh` exits 0
    - `grep -q 'unknown identifier' scripts/check-gates.sh` and
      `grep -q 'unknown constant' scripts/check-gates.sh` both exit 0
    - `grep -q 'does not depend on any axioms' scripts/check-gates.sh` exits 0
    - The gate-4 matcher accumulates continuation lines rather than reading one line: the
      script contains a closing-bracket terminator for the axiom region (grep for `]` used
      as a parse terminator) and a `malformed` failure path —
      `grep -qi 'malformed' scripts/check-gates.sh` exits 0
    - `grep -q 'sorryAx' scripts/check-gates.sh` and `grep -q 'allow-sorry' scripts/check-gates.sh` exit 0
    - `grep -q 'mktemp -d' scripts/check-gates.sh` and `grep -q "trap .* EXIT" scripts/check-gates.sh` exit 0
    - `bash -n scripts/check-gates.sh` exits 0
    - `git status --porcelain -- SrcTranslated` is empty — the allowlist was read, not edited
  </acceptance_criteria>
  <done>Gate 4 exists, reads its 20-name allowlist from a sidecar file whose every entry was grep-confirmed in the tree, asserts one well-formed report line per requested target, fails loudly on an unknown name, and rejects `sorryAx` unless opted in.</done>
</task>

<task type="auto">
  <name>Task 3: Document the gate runner in scripts/README.md</name>
  <files>scripts/README.md</files>
  <read_first>
    - `scripts/README.md` in full (18 lines) — it currently documents only the three
      `npm run` extraction commands plus the aeneas pinning note; a gates section is additive
    - `scripts/check-gates.sh` as left by tasks 1 and 2 — the flags and env vars to document
    - `.planning/phases/01-gates-issues-and-deviation-decisions/01-RESEARCH.md` lines
      1193–1210 (Runtime State Inventory: the four script-level env vars, the `.gate-cache/`
      and worktree residue, and the teardown obligation)
    - `.planning/phases/01-gates-issues-and-deviation-decisions/01-RESEARCH.md` lines
      1038–1060 (P-1, P-2, P-3, P-4 — the caveats a reader must be warned about)
  </read_first>
  <action>
    Add a `## Verification gates` section to `scripts/README.md`, after `## Commands` and
    before `## Configuration`. It must contain:

    - the one-line usage: `./scripts/check-gates.sh [--no-delta] [--refresh-baseline]
      [--clean] [--allow-sorry THM] [THEOREM ...]`, and a note that `scripts/check-lint.sh`
      is a deprecated shim for the same script;
    - a four-row table listing gate 1 `lake build` (no warning other than
      `declaration uses 'sorry'`), gate 2 `lake exe runLinter Spqr` (no `error:` line),
      gate 3 `lake env lean scripts/Audit.lean` plus the `scripts/sorry-diff.py` delta, and
      gate 4 `#print axioms` against `scripts/axiom-allowlist.txt`, each naming the
      `.github/workflows/lean.yml` step it mirrors (gate 4 has none — say so);
    - the four env vars `SPQR_BASELINE_REF` (default `origin/main`),
      `SPQR_BASELINE_WORKTREE` (default a sibling `spqr-gate-baseline` directory outside the
      repo), `SPQR_GATE_CACHE` (default `.gate-cache/`), and `SORRY_FAIL_ON_NEW` (consumed by
      `sorry-diff.py`, set to `true` by the script);
    - a **first run** warning: the baseline worktree needs `lake exe cache get` before
      `lake build` because mathlib is a transitive dependency via aeneas, and a cold run
      downloads GB-scale oleans. Add a line `Measured cold-run cost: TBD (recorded in plan
      01-06)` for plan 01-06 to fill in;
    - four caveats, each named: gate 1 can pass on an already-built tree because `lake build`
      emits no warnings for modules it did not recompile — use `--clean` before a PR (P-2);
      a module not re-exported from `Spqr.lean` is invisible to `scripts/Audit.lean`, so
      gate 3 cannot see its sorries (`Audit.lean` header);
      the repo-root `sorry-manifest.txt` is a stale gitignored artifact and is never used as
      a baseline (P-4); a toolchain difference between the worktree and the working tree
      produces a phantom delta and the script warns about it rather than bumping anything (P-3);
    - teardown: `git worktree remove <path>` for the baseline worktree and `rm -rf
      .gate-cache` for the cache, with a note that neither is tracked by git.

    Do not remove or reword the existing `## Commands`, `## Configuration` or
    `## Updating the aeneas version` sections.
  </action>
  <verify>
    <automated>grep -q '^## Verification gates' scripts/README.md && grep -q 'SPQR_BASELINE_REF' scripts/README.md && grep -q 'SPQR_GATE_CACHE' scripts/README.md && grep -q 'lake exe cache get' scripts/README.md && grep -q 'git worktree remove' scripts/README.md && grep -q 'Spqr.lean' scripts/README.md && grep -q '^## Updating the aeneas version' scripts/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '^## Verification gates' scripts/README.md` exits 0
    - `grep -c 'SPQR_BASELINE_REF\|SPQR_BASELINE_WORKTREE\|SPQR_GATE_CACHE\|SORRY_FAIL_ON_NEW' scripts/README.md` returns a number `>= 4`
    - `grep -q 'lake exe cache get' scripts/README.md` exits 0
    - `grep -q 'git worktree remove' scripts/README.md` exits 0
    - `grep -q 'Spqr.lean' scripts/README.md` exits 0 — the `Audit.lean` blind spot is documented
    - `grep -q 'Measured cold-run cost: TBD' scripts/README.md` exits 0 — the slot plan 01-06 fills
    - `grep -c '^## ' scripts/README.md` returns `4` — the three original sections plus the new one
    - `grep -nE 'check[_]no[_]sorry' scripts/README.md` exits 1
  </acceptance_criteria>
  <done>`scripts/README.md` has a `## Verification gates` section covering usage, the four gates and their CI counterparts, the four env vars, the cold-run warning with a slot for the measurement, four named caveats and the teardown commands; the three original sections are intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate script verdict → merge decision | `check-gates.sh`'s PASS is the evidence a property is done before the PR is opened. A false green here propagates an unproved property into the catalog. |
| `origin/main` worktree → local filesystem | The script creates and checks out a sibling worktree and deletes cached files under paths taken from environment variables. |
| stale build artifacts → gate input | `sorry-manifest.txt` at the repo root is a gitignored artifact from 2026-08-07; treating it as a baseline makes the delta meaningless. |

Gate 4 is invented by this phase; it has no CI counterpart, so no CI run can catch a bug in it.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Spoofing | `check-gates.sh` reporting PASS without running a gate | mitigate | **Highest-severity threat in this phase.** Four mitigations, all asserted: gates 1 and 2 are byte-identical to `lean.yml` (diffed in task 1's acceptance criteria); gate 3a asserts the manifest file was produced; gate 3b prints `SKIPPED` with a reason rather than PASS when no baseline exists; gate 4 counts one well-formed report line **per requested target** instead of grepping for bad names (P-1). Residual risk is closed by the negative-control matrix in plan 01-06 — a gate that has never failed is not known to work. |
| T-1-05 | Spoofing | stale or poisoned baseline manifest | mitigate | Cache key is the resolved `origin/main` SHA; `git fetch` runs before `rev-parse`; the repo-root `sorry-manifest.txt` is never read as a base (P-4); a toolchain difference between trees raises a loud warning (P-3). |
| T-1-12 | Tampering | the delta reimplemented in bash and drifting from CI | mitigate | The script **calls** `scripts/sorry-diff.py` with positional `base head` and `SORRY_FAIL_ON_NEW=true`; `sorry-diff.py` and `Audit.lean` are asserted unmodified (D-08). |
| T-1-13 | Denial of service | destructive cleanup with an unset path variable | mitigate | Every path variable is guarded with `${VAR:?}` before use; teardown uses `git worktree remove`; the only `rm -rf` is the `mktemp -d` scratch trap from task 2, asserted never to reference a worktree, cache or repo path; the only `rm -f` is the fixed-name `.sorry-delta-comment.md`. |
| T-1-06 | Information disclosure | secrets in gate logs | mitigate | `set -x` is asserted absent; `GITHUB_STEP_SUMMARY` and `GITHUB_OUTPUT` are deliberately left unset; the script never reads a token. |
| T-1-14 | Elevation of privilege | scratch Lean file written to a predictable path | mitigate | `mktemp -d` (mode 0700) plus `trap … EXIT` cleanup (ASVS V12). |
| T-1-SC | Tampering | npm/pip/cargo installs | n/a | No packages installed. `shellcheck` is absent and deliberately **not** installed; `bash -n` is the fallback per RESEARCH.md § Environment Availability. RESEARCH.md § Package Legitimacy Audit records the phase as not applicable. |
</threat_model>

<verification>
Static only — this plan does not run the gates (that is plan 01-06, which needs a built tree):

1. `bash -n scripts/check-gates.sh && bash -n scripts/check-lint.sh` → exit 0
2. `./scripts/check-gates.sh --help` → exit 0, names all four flags
3. `grep -vc '^\s*#\|^\s*$' scripts/axiom-allowlist.txt` → `20`
4. `git status --porcelain -- .github scripts/Audit.lean scripts/sorry-diff.py SrcTranslated src` → empty
5. `git diff --name-only origin/main..HEAD -- '*.lean'` → empty
</verification>

<success_criteria>
- `scripts/check-gates.sh` exists, is executable, parses `--no-delta`,
  `--refresh-baseline`, `--clean`, `--allow-sorry` and positional theorem names, and
  implements gates 1, 2, 3a, 3b and 4 with a fail accumulator (INFRA-03)
- Gates 1 and 2 are byte-identical to the pre-rename `check-lint.sh` and to `lean.yml`
- `scripts/check-lint.sh` is a ≤6-line shim
- `scripts/axiom-allowlist.txt` holds exactly 20 grep-confirmed names with sources
- `scripts/README.md` has a `## Verification gates` section
- `.gitignore` ignores `.gate-cache/` and `.sorry-delta-comment.md`
- `lean.yml`, `Audit.lean`, `sorry-diff.py`, `SrcTranslated/` and `src/` are unmodified
</success_criteria>

<output>
Create `.planning/phases/01-gates-issues-and-deviation-decisions/01-03-SUMMARY.md` when done.
Record: the allowlist-entry grep-confirmation loop and its output, the byte-identity diff of
the gate 1 and gate 2 commands across the rename, and an explicit note that the gate-4
output patterns are provisional pending plan 01-06's empirical probe (RESEARCH.md A1/A2).
</output>
